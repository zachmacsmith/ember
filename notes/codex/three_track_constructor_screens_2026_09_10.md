# Three mechanism screens after architecture review

2026-09-10 UTC; fixed before complete calls. This continues the [completed-cycle decision](mechanism_decision_2026_09_10_0305.md). A061 remains the single retained algorithm. A066, B029 and C016 remain rejected; C017 is terminal. These exploratory screens cannot establish class-level superiority or select outputs from different algorithms.

| Track | Shared deficit and changed mechanism | Complete comparison | Evidence required to continue |
| --- | --- | --- | --- |
| A067 | Nearest-contact attachment can spend sites on paths that share few contacts; replace its local path score with distinct contacts per actual added site. This could affect sparse local structures and random graphs together. | Transfer004 g0301–g0309 and ER160/SBM160/wheel anchors g0007/g0011/g0015; seed0; A067/A066/A065/A061/MM; 60 calls on hyde06. | No success or Q loss against either A065/A066 on any row; strict Q gains against both controls on fresh inputs in at least three random-source families. Small isolated savings with persistent MM gaps and deadline-dominated cost are insufficient. |
| B030 | Connected intermediate chains can prevent contact-bearing regions from moving together; permit temporary fragmentation and translate multi-owner patches before reconnection. | g0013/g0301/g0302/g0305/g0308/g0309; seeds0/1; B030/connected-state ablation/A061/MM; 48 calls on hyde02. | All twelve candidate rows valid; complete Q gains over the ablation on two structures including fresh development, and below A061 on two structures. Preserve every A061/MM regression. Fragment/contact scores alone give no continuation. |
| C018 | Existing chain locations can prevent compact contact arrangements; release a whole connected territory and regrow it from a root anywhere on Z12. | g0013/g0302/g0305/g0307/g0309; seeds0/1; C018/old-root ablation/A061/MM; 40 calls on hyde03. | All ten candidate rows valid, no A061 Q regression, at least one-third closure of A061's positive MM Q gap on two structures, and complete-outcome benefit over the root ablation on at least one. |

The A criterion above fixes the design's three-family wording before execution: count fresh random families g0301–g0306 only, with a strict gain against both A065 and A066 on the same row. Relabels, controls and old anchors cannot inflate that count. For B/C, report each seed separately; a favorable mate cannot erase a loss. These are exploratory continuation criteria, not promotion rules. Failure rejects the fixed policy; a later bounded mechanism test requires a specific completed observation and a new frozen hypothesis.

Every call receives the same 60-second solver envelope, with separate cold processes and randomized paired method order on its host. This is an allocation for diagnosis, not a claim that 60 seconds meets MM's runtime order. Imports, compilation, initialization, failed proposals, final validation and discarded work remain charged in the applicable solver/process clocks. No new active work caps are introduced. A inherits its documented base/extension allocation; B/C use only wall deadlines and final reserves. Work counters are diagnostics. Sparse and dense time ratios remain separate; there is no universal multiplier gate.

Use first independently auditable valid-incumbent time/Q and all subsequent incumbent improvements to distinguish construction from search. A exposes a certified base handoff bound, not the hidden instant its inherited constructor first found validity. B/C expose first-valid certification and incumbent events. Contact debt, fragments, canonical-path access, acceptance and phase costs explain failures but cannot supply embedding credit. Saved event scalars alone do not certify every intermediate mapping. Completed validated outputs decide quality. Report flat late intervals, incomplete operations and unknown timing explicitly.

Reuse the original oracle and audited pilot. The pilot changes only by five constructor registrations; the freezer's allowlist adds those names. Its existing AST proof verifies unchanged dispatch, workers, isolation, scheduling and old registrations before each freeze. Transfer004 inputs are independently checked development data; control witnesses and MM maps never enter candidates. Source-only snapshots exclude generator scripts and evaluator-private files. The one-seed audit is reused for A; B/C require a reviewed isolated seed-dimension extension of the same analyzer, preserving its oracle and credit rules.

Root reviewed A's source, wrapper diff and all four tiny checks; C's full source/kernels/four checks plus the narrow runtime-error correction; B's full main/arrays/three checks. An independent peer also reviewed B. Root verified A's seventeen frozen bindings, C's eighteen original and twenty-three corrected completion bindings, and B's eleven bindings plus snapshot copies. Evidence: `results/codex/three-track-20260910/root_check_binding_review001.json`. First C/B implementations are preserved in commits `0011136c` and `997bc7d1`; runtime-error accounting corrections are separate changes with one new injected case each. No development constructor supplies this correctness evidence.

Concrete experiment identities, graph order, interpreter paths and reference hashes are in each evidence directory's `screen.json` and `freeze/freeze_result.json`: `a067-transfer`, `b030-constructor`, `c018-territories`. Those records, detached supervisor status and immutable result archives establish what actually ran. This note does not assert that any screen has started or passed.
