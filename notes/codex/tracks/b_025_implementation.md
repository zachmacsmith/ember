# B025 acceptance-only implementation gate

`nonincreasing_contact_construction.py:contact_embed` implements the accepted [memo](b_025_acceptance_discriminator.md) and [accounting detail](b_025_accounting.md). Descriptor: `nonincreasing-contact-tree`, config `{}`. Exact textual projection back to B023 passes; all28 functions outside `_action` and the public identity-bearing entry are text-identical. The public entry itself changes only its algorithm/version strings. No other constructor is imported.

Four focused stdlib groups pass in `results/codex/track-b025-nonincreasing-contact/checks001` (0.002 seconds unittest, 0.067 seconds process wall). Real path/contact proposals cover improving F5→4, neutral F4→4, and rejected uphill F4→5. The uphill proposal is valid privately, but final output remains uncredited because the incumbent still lacks its contact. The preceding edge-price increment survives. Work interruption during selected-winner recording and a final rejection-clock interruption preserve the incumbent and price, without incrementing completed-rejection counters. No failed check, public-constructor call, corpus call or prohibited import occurred.

The selected winner's connected chain and exact score/contact deltas are retained as diagnostic evidence. Recording and the gate share the original cumulative meter under `acceptance`; extra evidence cost stays in treatment wall/work. Complete rejections are distinguished from interrupted queries by `acceptance_rejected` and separate total/route/erase counters. `discarded_scored` includes all scored, unpublished alternatives in either case. Existing generation, rank, price updates, schedule advancement, publication deadline and final validator remain unchanged; no deferred proposal is retried privately.

The negative long-path limitation in the memo remains: this gate can prevent necessary growth within eight passes. Small checks establish its behavior, not usefulness or convergence. The next evidence is the fixed [27-call full-constructor comparison](b_025_screen_protocol.md), with every rejection, failure and time retained. Source and tests are stable for root review; no additional gate tests or tuning are planned.
