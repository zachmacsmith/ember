# B025 pre-code accounting detail

The accepted [memo](b_025_acceptance_discriminator.md) fixes the sole behavioral change: after complete unchanged winner selection, publish iff its exact F does not exceed the incumbent's post-price F. The descriptor is `nonincreasing-contact-tree`, standalone `nonincreasing_contact_construction.py:contact_embed`, config `{}`. Copy exact B023; only `_action`, diagnostic identity and the header change.

Record the selected proposal's owner, sites, Q/M/O/F and lost/restored original contacts, plus the incumbent acceptance threshold. Meter these copied records and the gate under an `acceptance` phase in the same cumulative work/deadline allowance. Sorting CPU and scalar administration remain in common wall time. This evidence cost is part of the treatment, not artificially added to B023. Proposal generation and ranking remain unchanged.

Perform the last rejection clock check before recording a completed acceptance rejection. A timely rejection retains all chains and the preceding price observation, increments separate total/route/erase acceptance-rejection counters and advances the unchanged schedule. `discarded_scored` counts all fully scored but unpublished alternatives; `acceptance_rejected` distinguishes a completed gate decision from interruption. The selected proposal is diagnostic evidence only, including any valid map rejected by this energy rule. A clock/work interruption during its materialization or the rejection check leaves the action incomplete and keeps the earlier incumbent and price update; it is not a completed rejection. Accepted publication still uses B023's unchanged atomic final-clock gate. No partial alternative or rejected valid proposal receives output credit.

Focused checks cover a genuinely improving route, a neutral route, an uphill route preserving the applied edge price, and a deadline at the rejection check retaining the same state/price with incomplete accounting. A static comparison binds every inherited body except `_action`; no old suite or public constructor is run. The fixed twenty-second/20M B9 treatment/B023/MM screen remains root-owned.
