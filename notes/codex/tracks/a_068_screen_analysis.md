# A068 passive screen analysis preparation

2026-09-10 UTC. **Before result reads or reader execution.** The root-owned
48-call ideal-Z12 screen compares footprint, virtual inactive booking, A061 and
MM on twelve Transfer004 development encodings/eleven structures, seed0, hyde06.
This reader does not launch, rerun or validate an embedding.

Use the unchanged original analyzer
`results/codex/three-track-20260910/analyze_multiseed.py`, SHA256
`a4c16e83ff90210d5ac9454010723ce0f6301c05fd4fef3eaa92c276855f7540`.
Only after its terminal PASS may the prepared
[mechanism reader](../../../results/codex/a068-footprint/mechanism.py) consume
its final-map credit, Q, ACL, within-chain variance and complete solver wall,
CPU and process times. The reader binds those inputs and candidate/booking/A061
raw diagnostics. It never opens an MM result map or any witness. Existing A067
comparison/outer-censor helpers and A064 finite/observed-sum helpers are reused
directly. No candidate imports, new minor validator or intermediate replay.

The outputs retain every graph/method call, every success/Q regression and
unknown timing field. Canonical family rows exclude BA relabel g0309 only from
aggregate structure votes; its individual result can still veto continuation.
Controls g0307/g0308 are reported separately. Across-seed variance is unknown
with one seed; mean within-chain variance is a different quantity.

The root clarified the already frozen criterion **before any outcome read**:
no success/Q loss against either A061 or booking; at least one-third of a
positive paired A061-to-MM Q gap closed in two ordinary source families; and
Q improvement over booking on two canonical independent structures. Controls
**may** support this latter mechanism condition, with their count exposed
separately; they cannot vote in the ordinary-family gap condition. Relabels
never add a structure or family. Integer arithmetic tests
`3*(Q_A061−Q_candidate) >= Q_A061−Q_MM` on positive reference gaps.
Both total and ordinary-only booking-gain counts are reported. This numerical
condition is necessary exploratory evidence, never automatic promotion.

Time/quality scopes remain distinct:

* Full-source handoff uses the new explicit wrapper timestamp, or the earlier
  A061 base-duration/operator-entry bounds. Internal first validity remains
  unknown. Handoff Q is already a full-source quantity.
* Initial native validity and constructed/pruned Q concern the **filled core**.
  Its new timestamp can be translated between native and wrapper clock origins,
  but core Q is never divided by the original-source node count or presented as
  full ACL. Raw pruning savings do not demonstrate complete-output benefit.
* Geometry exposes asks, last improving surrogate ask/time, active/virtual
  bookings, primary stop and simultaneous ask/deadline flags. Old A061's
  primary ask stop does not disprove an unrecorded simultaneous deadline.
  The 1000-ask first screen remains controlled exposure, not saturation.
* Conversion, completion, core validation, A053 stages, path/final validation
  and downstream reserve costs retain their scopes and missing values. Nested
  intervals are not added to enclosing solver wall. Reserve excess and original
  deadline excess stay separate. All observed failures remain charged.
* Path receipts supply an ordered sequence of reported committed Q values,
  without individual timestamps. Any eventual-final-Q time is only its saved
  handoff or path-return bound. No exact trajectory or early-stop guarantee is
  inferred. Outer TIMEOUT/WATCHDOG_TIMEOUT or late evidence remains censored
  even if the inner operator reports completion.

The paired full/booking comparison tests whether releasing inactive reservations
improves complete construction, given their identical physical-support policy.
Full/A061 measures the whole new construction package. Layout improvements
alone cannot isolate final packing benefit: accepted trajectories, lifting and
wall-limited later work can differ. A binding geometry deadline makes the arms
unequal in completed ask exposure and will be reported explicitly. Root decides
whether complete outcomes justify continuation or an independently frozen
allocation comparison; none is authorized by this preparation.
