# C009 implementation and bounded primitive evidence

The standalone `connected_domains.py:domain_embed` implements the [pre-code policy](c_009_connected_domain_policy.md). It imports only the standard library, uses supplied node order and original adjacency, and shares no A/B constructor or template mutation. It keeps at most eight connected alternatives per source vertex, coordinates probabilities simultaneously, and regenerates candidate trees over the entire target. Physical contact and owner overlap are separate quantities; expected agreement never earns embedding credit.

The three approved focused groups pass in **0.0649 seconds** under native Python 3.10.19, with zero public constructor calls and zero prohibited import attempts. [Evidence](../../../results/codex/track-c-009-checks/attempt001/summary.json) binds the tested bytes. Checks cover real-coupler versus shared-site semantics, connected generated trees, normalized simultaneous probabilities, rollback and late-copy non-credit. On the supplied three-owner fixture, the initial most-probable choice overlaps; eight updates select the independently known valid Q3 assignment. A triangle whose domains are the two singletons of one target edge has pairwise support but cannot select a valid minor. Neither fixture is a constructor input or performance result.

The first source/test hashes were `be9a0f94c43ade63014bbca64b3cca064cf334992eb647553443297a69b0f954` / `70609db1c6938973bd6cc4136419488d91302aceca65a49015e2417df6738b80`. Root then identified that an internal ValueError after a feasible epoch could disappear when the final gate accepted its incumbent. The correction makes internal exceptions fatal ERROR with retained exception diagnostics; only expected input ValueErrors remain FAILURE. A prior valid incumbent after an ordinary work/deadline stop remains eligible. The affected interruption/non-credit group alone passed again in 0.0541 seconds; the other groups were not repeated. Original bytes, evidence and the exact delta are [preserved](../../../results/codex/track-c-009-checks/revision001/freeze.json).

Final source/test hashes are `a59fb95b436e6a66f2e3e88bd717c430b1e43cf76ede80d10aeea41e867a0626` / `2fcab806b6907fd7f11473e7f92de3507cc80d78a353b1c9475b6e01113a77b4`. No mechanism, schedule or bound changed. Inputs, domain construction, compatibility, probabilities, routes and output materialization consume cumulative charged work. The last 0.5 seconds and up to one million remaining units are reserved for one safe deletion pass and final original validation. A failed private regeneration leaves the old domains untouched. A prior valid incumbent can survive search interruption, but still must pass timely final validation.

**Main foreseeable limitation:** directed source edges × candidate pairs × probability updates can consume most of twenty million units before routing is useful. The complete input alone entails about 8.2 million probability-contact lookups per full eight-update epoch when all domains have size eight, before other work. Record started/completed epochs, update counts, generated/committed domain batches and stage/work shares. A cost-censored run that never gets useful regenerated domains says little about attainable geometry or coordination. Pairwise-supported but invalid selected maps remain ambiguous between globally inconsistent domains and approximate selection failure. The fixed constructor still must meet its full-screen rule; no cap, temperature or domain-count adjustment follows automatically.

Registry requested from root: `connected-domains`, configuration `{}`, API `domain_embed(source, target, *, seed=0, timeout=15.0, deadline=None)`. Failure/late returns contain no credited embedding and preserve the latest available diagnostic map, completed domain/probability state, final normalized chains, stop reason and cumulative work. No public constructor, corpus call, source freeze or remote action was performed in this implementation task.
