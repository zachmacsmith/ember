# A066 bounded static peer review

2026-09-10 UTC. C-track peer review requested by root while C016 runs. **Static inspection only:** no candidate, test, reader or constructor execution, and no A-track source edits. Scope is the new exact pruning, pair publication, epoch invalidation and interruption behavior. This is not a performance result or an exhaustive re-audit of inherited infrastructure.

The reviewed operator SHA256 is `b873ee4562293b0aa160e90d188cf01984facef152ac8b34333cf7f50ad2b83f`; the construction wrapper is `d00793c59d62571b172e42cdcf256318a85c977ee34b500972e91de4aa1c8c9e`. Inspection also followed the relevant unchanged A063 preparation/reconstruction/certification/bundle and A064 visit/admission/cleanup paths, plus A065's compiled-context inheritance.

No incumbent-validity blocker was found in this scope:

- **Pruning:** unchanged preparation releases the focus and empty sites, plus removed donor sites. For the eligible two-site focus, the new assertions require zero removed donor sites and exact donor membership preservation. Unchanged reconstruction returns only the focus tree, and ordinary admission installs those prepared donors. Thus strict Q improvement requires a one-site focus. The prepared boundary enumerates all free physical sites touching each retained neighbor, so lack of complete contact coverage proves this particular strict-Q query impossible. The isolated-owner case correctly uses the free set rather than the empty boundary dictionary.
- **Pair domains and validity:** adding the singleton neighbor's old site to prepared free space releases exactly the two owners. Each new singleton domain intersects contacts to every outside source neighbor; an actual target coupler connects the pair. Distinct sites, unchanged outside chains, exact Q−1, full original-graph certification and a fresh ownership/contact bundle are checked before admission. Requiring both owners to change is consistent with the already empty direct singleton domain: keeping the neighbor fixed would contradict that proof.
- **Epoch state:** pair admission can occur before a cache exists for the current owner. Its receipt marks that fact explicitly, invalidates every existing query, clears every closed-owner mark and resets live payload in the same publication sequence as inherited commits. The inherited visit returns after preparation; the round continues at its next slot, with the changed epoch recorded. Ordinary inherited commits also invalidate prior exact-negative closures.
- **Interruptions:** all domain/certificate/bundle/receipt preparation remains private. A final meter check and direct clock comparison precede the inherited-style assignment of incumbent, epoch, cache and receipts. A deadline noticed during later cache disposal preserves the already admitted complete minor. The public wrapper retains its original final validation and late-result suppression.

One **diagnostic accounting edge** was sent to root and the A-track implementer: `prepare` assigns `pair_obstruction.status = 'committed'` only after `commit_pair` returns, but `commit_pair` can publish and then raise a deadline from `dispose(old_cache)`. The main committed record and incumbent remain correct; the nested status can remain `running`. Recommended correction is to mark that nested status alongside the committed record before disposal, or derive it from `record.committed` in the preparation `finally`. The existing interruption group should include post-publication disposal. This note records the reviewed version and recommendation; any correction requires a subsequent source hash and focused-check evidence from the A-track owner.

Follow-up static check: the A-track owner independently identified and corrected this edge before its check freeze. I inspected the added `record['pair_obstruction']['status'] = 'committed'` assignment immediately before disposal in operator SHA256 `e9280652f080218caef4bc8b149177217cf0b18e1250f30865c70511242bbae6`. This resolves the identified source-level accounting issue. The owner reports that its interruption group includes this case; execution evidence remains its responsibility and is not claimed here.

The pruning proof concerns the fixed prepared query, not globally optimal embedding. The pair move may still worsen later search opportunities despite its immediate Q gain. Those questions belong to the frozen complete-constructor comparison and pruning-only ablation.
