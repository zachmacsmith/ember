# Bounded neutral continuation under unchanged donor eligibility

**Hypothesis.** Strict separation descent discarded 217 valid exchanges that opened disconnected pockets. Twenty of 25 supplied queries began at the smallest positive anchored-region separation, two, so useful intermediate transport could not pass that rule. Allowing equal separation within the existing beam/depth/proposal limits may retain a useful rearrangement without accepting movement farther from the opposite region.

**Exact amendment and pseudocode.** Reuse the frozen [component-transport diagnostic](c_component_transport_diagnostic.md). Change only its post-exchange separation predicate: accept `new_separation <= old_separation`, rejecting increases. Continue equal-separation states through the identical ranking `(separation, -smaller_region_size, changed_donor_count, trace)` and exact changed-chain deduplication. Preserve endpoint access that was already open. Keep the smaller-region selection, release adjacency to that region, endpoint exclusion, exact donor domains and protected contacts unchanged. An overlapping pair of regions still requires an unused-only path and independent original-target certificate before success. An equality admission is neither a new contact nor a distance improvement; record it separately.

Use the same 25 independent queries from the same four supplied states. Keep depth 4, beam 8, four donors, 128 original donor sites, 64 proposals per state, 512 per query and 30 seconds per input, charging all domain/recomputation/sort/copy/failed-state work. No query, target scope, constructor budget or MM call is added. Preserve interrupted operations and selected queries that cannot begin before the common deadline.

**Tiny falsifier and eligibility distinction.** In the preceding isolated-pocket fixture, add target edge `(4,6)`. The first required relocation now opens a pocket with separation 2→2, and the second connects it with separation 2→0. Require strict-policy rejection, neutral-policy two-move certification, fixed endpoint chains, preserved original contacts and unchanged input. The older five-site two-exchange fixture remains unreachable: its necessary first released site 2 is not adjacent to the selected anchored region `{0}`, while the eligible donor at site 1 has no unused frontier. Equal-separation admission cannot change this generation failure. Test and report that rejection explicitly; do not expand donor eligibility to recover the witness.

**Self-critique and supplied-state falsifier.** Equal-separation states can consume the unchanged prefix and deadline through unproductive rearrangements. Region-size ranking may favor large pockets that expose no useful donor; non-increasing distance still excludes necessary detours, and the selected-boundary restriction can exclude preparatory moves entirely. Reject this fixed amendment if it repairs no previously failed query on the same supplied states, retaining all additional work and partial searches. Even a new local certificate would establish only supplied-state reach; whole-source validity, full-constructor quality and novelty remain unproved.
