# B020: paired versus single contact updates

2026-09-08, saved before control code or calls. The [B019 complete-constructor gate](b_019_results.md) resolved the tiny triangle's initial overlap, but a one-witness change can also resolve that state. It therefore supports movable contacts, without establishing a benefit from pairing them.

**Hypothesis.** On the fixed exposed B9 panel, revising two incident contact witnesses together improves completion or final physical Q enough to justify its additional routing work. The alternative is that single-primary updates cover more useful visits within the same allowance.

Preserve B019 source `86d5b6dcc3cfee449ae499b727f4d201e5730db451ee07e7553aaddb02a9d65b`. Create a standalone experimental copy, `movable_contact_single_construction.py:contact_embed`, changing only `_Context.partner` and diagnostic identity. Its complete scope rule is:

```
partner(state, primary):
    set meter stage to schedule
    charge one unit; return [primary]
```

It does not enumerate or rank unused partners. B019 retains its charged incident-edge scan. All initialization, seeded physical/edge orders, old-plus-three contact pools, attachment rules, prices, exact energy, strict feasible-Q acceptance, eight sweeps, final reserve and 20M/100k/60s/5s limits remain identical. A single query has at most four combinations versus the paired query's sixteen. Its affected owners/retained obligations and candidate region follow unchanged code from its smaller scope; this is not guaranteed to be a subset of the paired proposal set. Commits can subsequently change the whole trajectory. Equal caps are not equal total time or equal numbers of completed proposals.

Before the panel, check AST equality except that method and identity strings, and test direct scope selection against an object that forbids reading incident alternatives. Reuse B019's nine accepted checks without rerunning them. Make exactly one single-control full call on the already declared triangle-to-triangle input, seed 0 and five seconds. Preserve its initial Q4/O1 state and all failures; require independent original-graph validation of any claimed result. Do not change the pool if it misses the known one-witness solution.

**Critique/falsifier.** Both arms retain the potentially poor shared-BFS representation, greedy tree rebuilding and strict local acceptance. If neither finishes useful embeddings, the experiment rejects these complete policies without identifying which shared component caused failure. If pairing gives no additional timely success and no Q improvement on jointly successful inputs, while costing more or limiting more queries, its proposed advantage is falsified on this panel. A gain is evidence about this fixed neighborhood policy under equal budgets, not necessity of simultaneous changes or a general performance claim. Preserve success-only differences, all paired Q regressions, ACL1 ties and MM failures; never select an arm separately per input. Decide continue/redesign/retire after all 27 observations.
