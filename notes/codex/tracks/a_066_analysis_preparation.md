# A066 passive mechanism reader preparation

2026-09-10. Prepare only; root reviews the reader before execution. The running
45-call screen and common audit remain root-owned. Do not alter frozen sources,
algorithm contracts or screen notes.

Reuse the audited A064 receipt interpreter, its A063 censoring helpers and A065
compiled-cost reader. Adapt only the A066 version constants and new receipt
semantics: uncached pair publication, one certificate without an ordinary root
reconstruction, and exact-obstruction closure. Preserve ordinary query/epoch/Q,
active-time, work, censoring and cache checks. Save the generated interpreter
diff against the old function for review. Keep unchanged A065 interpretation.

Check all available A066 pair receipts, attempts and closures, rather than a
sample. Reconcile explicit pair kinds, both changed owners, Q−1, actual frozen
source adjacency and hardware couplers, neutral-first flags, and epoch cache
invalidation. A pair commit must not pretend to have a ranked root or normal
prepared query. Closed exact-obstruction records must not be treated as censored
ordinary-root searches. Interrupted attempts stay unknown. These are receipt
consistency checks: full intermediate embeddings and the donor-domain proof are
not replayed or independently revalidated. The unchanged original audit alone
supplies complete-minor credit and measured call times.

Retain all 45 common rows, including failures and unknown time fields. Compare
A066 against pruning-only, A065 and MM separately; preserve each success loss,
Q increase, variance change and runtime ratio. Continuation requires no A065 or
pruning-only success/Q loss and Q gains over pruning on at least two distinct
structures. Relabels cannot count as independent structures. No pooled algorithm
selection, new minor validator, candidate import, MM map read or constructor call.
