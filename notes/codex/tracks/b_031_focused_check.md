# B031 first focused packet

2026-09-10. **Operation evidence, not complete-constructor performance.** Root
reviewed source and the three focused risk groups before execution. The first
frozen packet passed once in the native isolated environment through the existing
`local_action.py`; no development constructor or complete 264-cut scan ran.

Evidence: `results/codex/b031-strip-transport/check001/`. Manifest SHA
`b953587bbe5081167fe418c67bd45c61a1e8ee0e053153910dba6a0ea697d62b`
binds eleven unchanged source/helper/oracle/target/contract snapshots. Production
SHA `e9f1a755562d899a4447ec0c60cdd5c4fe22059cf16c2c517f10eab3dc4deb15`;
check SHA `862266898bbdb1a629be6bb4c118b4afd6433c6f7552790093430c876f855344`;
summary SHA `25ecda035add0dd2c1ba6ea733dbe3308d04c4f04f6d791280c9f5bc2394a9af`.
Process wall 2.310831 s; check wall 2.264171 s / CPU 2.180556 s. Three groups,
zero failures/errors, zero prohibited import attempts. No retry.

The two rails had no free common singleton initially. Preserving their required
contacts added exactly two bridges, Q28→30; a new common singleton gave a valid
Q31 minor. This does not prove that every larger ordinary birth was blocked.
The sealed-owner fixture did establish ordinary clean-root failure. Transport
added zero bridges and enabled a singleton leaf, Q21→22. The original independent
oracle and unchanged structural validator accepted the resulting minors.
Odd/internal cut contacts passed in all four direction conjugates. Occupied far
bands, a missing required image coupler and an identical map were rejected.

Root's pre-execution review caught an equal-Q tie mismatch: enumeration by
bridge cost had implicitly taken precedence over direction/A/B. Before freezing,
the implementation was corrected to compare the declared full transaction key,
including equality-safe root and local bounds. The packet checks an equal-Q
replacement and retains the original contract.

An injected interruption after certification left the public entry unchanged.
Four wrapper calls replaced search with tiny fixed fixtures: full private state
could be certified/published before the deadline; partial private state remained
uncredited; a synthetic late result yielded TIMEOUT; and a post-valid fatal
ValueError yielded ERROR with no embedding despite a recorded valid state. The
last two cases add 60 seconds to a diagnostic clock; those fields are **not
measured execution costs**. Every attempt, status and error is preserved.

Proceed to the frozen 48-call small complete screen under
[the prospective contract](b_031_implementation_contract.md). Record geometry
enumeration/materialization/sorting separately from insertion: interruption
before the first birth query is censored cost evidence, not an empty geometric
domain. The open question is final compactness and timely validity across the
six encodings, including the ordinary-only ablation and all per-seed regressions.
