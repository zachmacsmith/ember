# B027 before-code hypothesis: exact recurrence during valid quality search

**Design only; B026 source/results remain unchanged and its screen is held.** B026's triangle→C4 call found optimal Q4 during initialization, then spent4.75s on25,512 neutral changes. The hypothesis is that detecting an exact repeated complete quality-sweep state can eliminate deterministic repetition without changing any construction or quality choice before that recurrence. An unchanged objective value alone supplies no certificate.

If authorized, copy frozen B026 source `64f5547357a74f5fc844476992acdf020dd7b09088ad880320a6bc4769e381ed` into isolated `factored/recurrence_mobile_tree_construction.py`, API `contact_embed`, descriptor `mobile-tree-recurrence`, configuration `{}`. Preserve initialization, visits, root/route selection, trimming, acceptance, prices, deadlines and final validation. Add only the quality-boundary key/check, its receipts and method identity. No new cap, price, tie, route or patience change.

**Exact state and placement of the check.** After entering quality, at the first point where a complete quality sweep will be scheduled, record the boundary state. After every fully completed quality sweep that made a changed commit, check its new boundary state against all recorded boundaries; stop with `quality_state_recurrence` on an exact match. Existing singleton-lower-bound and unchanged-sweep exits retain precedence. An interrupted sweep is never a recurrence observation. A feasibility-to-quality transition midway through an old feasibility order registers only when the next complete quality sweep begins.

Use immutable canonical tuples containing every chain's sites, every oriented shared physical witness, cached tree parents, occupancy sets, Q and O. Numeric source order and target tie order define serialization; no raw set/dict iteration becomes part of the key. Trees and occupancy are redundant for the logical proof but included conservatively. Use full tuple equality in a set/dictionary, **not digest equality**; ordinary hash collisions cannot cause termination. Also retain the first-seen sweep index for a recurrence receipt.

The source/target adjacency, ranks, quality flag, routing cost rule, absolute deadlines and frozen price map are unchanged across these boundaries. B026 retains no RNG object after initialization: its two seed-based permutations are fixed. The schedule is rebuilt from chain sizes/source rank at each boundary, with no persistent owner cursor. Visit counters, trace buffers, progress records and elapsed time are observers, not non-time proposal inputs. They must not enter the key, since their growth would prevent recurrence; implementation review must confirm no search decision reads them. The new seen-state collection is used solely for the stopping check.

**Why this is a certificate.** With deadlines nonbinding, one whole quality sweep is a deterministic function of the keyed state and fixed data. Repeated state therefore repeats future complete transitions. Actual Q is nonincreasing at every quality commit, so a cycle returning to the same Q contains no Q decrease, including within the cycle. Stopping cannot forgo a later actual-Q improvement on that unchanged deterministic trajectory. This is not a certificate that no better minor or unsearched move exists, nor a guarantee of the same final neutral embedding as a deadline-truncated B026 run.

**Charged publication.** Key construction, sorting, comparison and storage use the same live deadline and diagnostic meter, with a `recurrence` stage and no work cap. Build a complete private key; check the clock before inserting it into the seen table or announcing recurrence. Interrupted construction publishes neither a partial key nor a recurrence decision and proceeds to B026's timely original-graph finalization. Fatal exceptions still suppress output. Key overhead may change deadline censoring before recurrence, so prefix equivalence is claimed only through completed timely comparisons. Worst-case storage is proportional to the number of distinct completed quality boundaries times the state size; a60s call bounds execution but does not establish a small memory bound.

Record keys examined, distinct keys, key sites/edges/owners scanned, stage time/work, first/repeated boundary indices, cycle length and repeated Q. A diagnostic digest may identify a recorded key but never substitutes for exact equality. Retain first-valid and final validity/Q/time and every interruption.

**Focused comparison plan, pending authorization.** Check same-Q states with different chains/witnesses are distinct, an exact repeat matches, and interruption during key creation retains the incumbent without a false recurrence. Static comparison must show unchanged inherited methods and only boundary hooks in `run`. Then exactly four tiny full calls: frozen B026 and B027 on triangle→C4 and P3→P5, seed0/5s each. Identical read-only visit wrappers record chain/witness states through the treatment's first recurrence; compare the completed non-time prefix, while charging wrapper cost to both calls. Preserve the original uninstrumented B026 gates separately. P3/P5 checks that the existing ACL1 exit remains intact; no repeat of the four-case B026 suite is needed.

The positive falsifier requires an exact C4 recurrence, independently valid Q4, identical completed choices to that point and substantially less repeated sweep work; wall cost is reported rather than assumed. If the key does not recur or prefix choices differ under nonbinding limits, retain that failure without weakening the key or adding patience. If key overhead causes an earlier interruption, label a cost failure. This experiment addresses the observed deterministic computation cycle only; all B026 placement/neighborhood/quality limitations remain. Root decides whether any subsequent fresh complete screen is informative.
