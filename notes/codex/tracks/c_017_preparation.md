# C017 bounded diagnostic preparation

2026-09-10 UTC. Exploration only; C016 remains rejected. No development worker or constructor has run.

Host selection, before packet freeze: read-only observations found no tmux on spare hosts 04 and 05. No remote setup was attempted there. Root selected hyde03, whose previous C016 screen is terminal and retrieved, to reuse its existing detached supervision prerequisites. A new diagnostic-only environment on 03 will pin SciPy 1.15.3, bundled HiGHS 1.8.0, NumPy 2.2.6 and NetworkX 3.4.2. No authentication or existing environment changes are planned. Both quota modes use that same host.

The original C017 contract is preserved; the separately frozen interpretation addendum limits hard feasibility to an alternative missed by the realized trajectory and quota exclusion to final inequalities in the fixed model. Positive local evidence alone cannot revive C016 or justify a chain of local repairs.

The first targeted tiny check passed: three groups, eight mode evaluations, six actual MILP solves; zero development workers, constructor calls, failures, errors or prohibited imports. Test wall/CPU were 0.436276/0.301802 s, enclosing process wall 0.551464 s. Exact enumeration covered a forced connected tree, three-owner contact/disjointness, distinct finally-free boundary counting, hard/relaxed monotonicity and empty domains. No extensive old-infrastructure testing was added. The diagnostic guard implements the reused isolation policy; it is not byte-identical to the pilot guard despite the tiny test comment. The frozen test remains unchanged.

Self-review before input serialization: exact x/blocker/length assertions, original node-list order, outside-chain identity and audit bindings are explicit. Only private chains and graph nodes/edges enter solver inputs; the old published map contributes only the contract-specified length scalar and is not copied. No MM maps, known witnesses, family labels, candidate imports, domain construction or solve occurs during serialization.

The model uses binary site ownership and one selected root per changed chain. Bounded directed flow from the selected root supplies one unit at every occupied site, forcing connected nonempty chains within the fixed length bounds. Fixed source-neighbor contacts, changed-owner mutual contacts and disjointness are exact. Sound distance pruning admits every connected chain through the fixed bound. Continuous boundary witnesses certify distinct finally-free adjacent sites; hard mode adds only final quota inequalities to the common model, while relaxed mode removes only those inequalities. Both modes retain identical domain and core-model digests. Root and the independent B-track reviewer found no formulation blocker.

The dedicated host03 setup finished first attempt in 8.582049 s with Python 3.10.12 on x86_64, SciPy 1.15.3 / bundled HiGHS 1.8.0, NumPy 2.2.6 and NetworkX 3.4.2. No forbidden package is installed. All 21 retrieved setup artifacts match the terminal inventory; tmux is absent and supervisor exit is 0. The HiGHS extension SHA is `f2268408099f1c6fff9176c07f6b796e5fdd4e000015acf3f32a01b966dbedc9`. The solver uses one thread, seed 0, presolve, zero objective and remaining wall allowance. SciPy passes `threads` and `random_seed` to HiGHS with its documented warning, retained in logs. Unavoidable default integer limits are 2,147,483,647; no lower node, iteration or solution cap is introduced.

Input serialization passed once: 73 evidence bindings, exact private states with 55/33/83 introduced owners and Q 261/122/372; no model import or development solve. The frozen randomized order is ER100 relaxed, BA100 hard, ER80 relaxed, ER100 hard, ER80 hard, BA100 relaxed. Packet manifest SHA `0146536fe36217ea9d66d9183a16a1afb1c3e93f3c7d56e2e966544b83c7635f`; input freeze SHA `a291a43c68ed7cdb6032a1d82a1961583e5fe9785a4d43b3e1075100fc1bcf9f`.

Detached transport reuses the B029 packet runner with only C017 identities, host03/environment/package checks, six-arm order, removal of irrelevant NUMBA settings and a 660 s supervisor allowance for six 90 s outer bounds. Each cold worker retains its 60 s total budget, including imports, model construction, solving and validation; the solver receives only the remainder before a 0.5 s output reserve. Independent original-oracle validation and the unchanged capacity reader check every extracted witness, original labels, exact changed owners, lengths and unchanged outside chains. Expired or late results are UNKNOWN. Missing completion, outer termination and worker errors must remain explicitly accounted, with GNU time preserving full process time even when no worker result exists. Raw worker UNKNOWN remains raw UNKNOWN when relaxed infeasibility permits a separate logical inference about hard infeasibility.

Self-review of the runner: an exclusive launch marker prevents duplicate start, all six arms retain their outcomes even if earlier arms fail, no automatic restart or enlarged model exists, and SSH is observation/transport only. The staged packet passed first remote preflight: all 10 packet files checked, no launch marker, no matching process or tmux session, all six arms unstarted. No production source, registry, shared validator or harness was edited. Extensive reproducibility work and a constructor are outside this packet.

Final source/input/environment/check/transport bindings are in `results/codex/c017-conflict/launch_freeze001.json`. Root review is the remaining prerequisite to one six-worker start.
