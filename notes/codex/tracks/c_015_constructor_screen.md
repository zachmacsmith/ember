# C015 complete-constructor screen

2026-09-09, frozen before Z12 outcomes. The [before-code contract](c_015_constructor_contract.md), [three fixed-prefix routing results](c_014_routing_diagnostic_results.md), and [focused checks and tiny complete controls](c_015_implementation.md) support testing a complete constructor now. They do not establish success or ACL on Z12.

Hypothesis: C014 rejected feasible births because its root domain and chosen routes consumed scarce neighboring capacity. C015 changes the root domain, quota-aware routing, scratch capacity accounting and first-admissible selection together in one evolving construction. On three saved failed prefixes, the new routing found admissible births without changing old chains or quota rules. This screen tests whether that capability survives later construction. Failure after passing those births points to later representation, placement or retraction/acceptance limits; excessive time points to remaining computational cost. More accepted births without complete valid minors is not sufficient evidence to continue the policy.

Run `track-c-constrained-contact-015` on hyde03, with dabh and the existing isolated native/MM environments `4e1fb892db12754e`. Exactly eight Transfer003 encodings: g0001 (ER80), g0101 (ER100), g0102 (BA100), g0004 (WS80), g0013 (grid128), g0017 (hidden-witness singleton control80), g0202 (new SBM100) and g0203 (new planar100). The last two were generated before outcomes; other tracks may expose g0202 before this screen ends. These are development inputs. Witnesses and original graph metadata stay evaluator-private.

Exactly 24 independent calls: `constrained-contact-domain`, retained `native-branched-path` (A061), and pinned `mm`, each on eight inputs with seed 0 and a 60-second allowance. The unchanged pilot shuffles method order per input, charges cold imports/compilation, validates independently, and preserves every failure and all solver/process time. Pair timing within this same host; retain recorded load. No output selection or portfolio is permitted. The global wall allocation and final-validation reserve remain; no per-owner five-second gate, top-K root cap or universal MM ratio gate is introduced. Counters remain diagnostic.

Root reviewed C015 source and four risk checks, verified all 25 source/check/result bindings, and confirmed both authorized Z2 controls passed on their first attempts. Reuse the original oracle and harness without correctness changes. The sole pilot change is descriptor registration; initialization proves all prior 39 descriptors and all other pilot AST unchanged. The screen specification binds production source, risk checks, earlier evidence and input review before immutable staging.

For exploratory continuation, require valid completion on all three diagnosed failures (ER80, ER100, BA100), preserve all three included C014 successes (WS80, grid128 and singleton control80), and complete at least one of the two new structures. Report all eight cases, including grid, before any decision. A missed criterion rejects this fixed policy; a completed result may support a separate bounded explanatory experiment. Large ACL or runtime regressions can justify retiring the policy even when this exploratory success criterion passes. This is not promotion or a claim of class superiority.

Report success, Q/ACL, within-chain variance, maximum chain, solver/process wall and CPU, first-valid time, accepted births/retractions, root generation/examination, and obstruction types where recorded. Across-seed variance remains unknown. Historical C014 SBM/WS Q regressions and BA160/planar160 deadline failures remain deficits outside this small panel; they cannot be erased by omitted instances or one recovered success. Generalization needs subsequent fresh structures, sizes and relabelings, followed by an untouched confirmation set. The all-class goal remains unmet and A061 remains retained pending complete promotion evidence.
