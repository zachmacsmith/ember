# C018 implementation conventions fixed before code

2026-09-10 UTC. Root approved isolated implementation and one focused tiny check after reviewing [the design](c_018_contact_relaxed_territories_design.md), SHA `8d07652a358922f0a926ab53fbfc62cd9fcbe419157ea9bed9259aa9b438a1cf`. Root owns registration and the complete-screen launch after source review. No C017 artifact or shared infrastructure changes.

Use unchanged `variable_regions._initialize`, file SHA `696ab4a6882bf33fa48db733facd0517aba2fffbcc0a3445fc389d96df6dfa68`. Its dependencies remain pinned by their own imports: quotient helper `4fc12515fb088be951e8270dad0bc06d64b49a1b9db86037d4592776c507fec1`, structural helper `1f8d947b232ef458e4d908f138bce8d5d1a9b00d5cb4b983929f7feb5759d973`. Call only initialization, adjacency and existing structural-validation utilities, never an old constructor/search. The helper chooses the largest target component, the midpoint of its computed two-sweep BFS path, degree-first source BFS forest and seeded target BFS prefix. This midpoint is not an established graph center. Ideal Z12 is connected. Empty source returns the empty valid mapping without calling the nonempty initializer. After initialization, shuffle source and target rank arrays once with the same RNG; these ranks fix sweep ties and canonical path ties.

For each private replacement, availability is all free sites plus the released owner's old sites. All other owners remain fixed during that one proposal. Every available site is eligible as a fresh root, except in the single ablation where roots are restricted to that owner's old sites. The ablation changes **only root eligibility**, not path availability or any subsequent policy. Full target-distance arrays ignore occupancy and are versioned by the corresponding owner's geometry. Root score is `1 + Σ lambda_uv * max(distance(C[v],q)−1,0)`. Sample one root before validity using the design's normalized exponential distribution and one uniform draw; after validity take the minimum score with fixed target-rank ties. Retain the old state only as rollback, never as a competing lower-energy proposal.

Exact regrowth convention: start a connected singleton tree at the chosen root. At each round, run **one complete multi-source BFS** in the currently available target graph, seeding all current tree sites in target-rank order and scanning each adjacency row in that same fixed rank order. For every currently unmet original neighbor `v`, retain the first dequeued reached site that is physically adjacent to `C[v]`. That endpoint and the BFS parent pointers define one canonical shortest path from the growing tree to an actual contact with `v`. Ignore already-met neighbors as endpoints, but include them in scoring. A missing endpoint after completed BFS is a recorded free-access obstruction, not source infeasibility.

Score each resulting **single actual path** by its new-site count plus the changes in all incident source-edge gaps after union with the current tree. Do not add separate path costs or claim a sum describes their union. Choose the most negative exact ΔE, breaking ties by path length and fixed source-neighbor rank; append that path atomically to the private tree. Then recompute gaps and repeat. Stop when no canonical path has negative ΔE. Arbitrary branching and incidental contacts are permitted. This is greedy search over the stated canonical paths, not exhaustive Steiner search or a claim that no improving tree exists.

Use NumPy arrays and a compiled BFS kernel with resumable fixed-size batches solely to observe time. An unfinished frontier resumes intact; the batch size is not a work cap and changes no explored domain. No compiler cache or prewarming is added. Import, JIT, packing, distance refresh, root scoring, BFS, rejected proposals, publication, validation and output all count. Large array operations have checks before and after; no positive result is credited after a clock overrun.

The public-call origin is captured before lazy numerical/helper imports. Its absolute deadline is the earlier of caller deadline and origin plus timeout. Search uses the existing `min(0.5 s, timeout/10)` final reserve. Temperature elapsed time uses this same call origin and its search allowance. At initialization and after each accepted state, actual contact completeness triggers the unchanged structural validator before a first-valid/incumbent event is certified. Record event time after that validation and snapshot preparation, with original label conversion retained for independent audit. Once valid, freeze weights and require a complete strict-Q improvement; terminate on an unchanged complete owner sweep. Mid-sweep transition to validity resets the no-change accounting for the subsequent complete quality sweep.

Private proposal generation and interrupted validation cannot partially replace published ownership, chain membership, contacts or versions. A publication has one final budget check, updates all state bookkeeping without a cooperative interruption, then checks time again; an overrun retains coherent state but cannot earn a timely event or final success. A prior certified valid incumbent survives a later interrupted proposal. Final original-source validation and serialization share the absolute deadline; late maps are diagnostic only.

One focused check will exercise nonadjacent relocation, a real uphill proposal reaching acceptance, signed incident energy/actual contact counts, a shared branching path, unchanged cached distances for unmoved owners versus invalidation of the changed owner, and interrupted proposal/publication/final certification. Reuse the original independent oracle and dependency guard; no additional performance diagnostic or source-family screen before the approved complete-constructor screen.
