# B024 implementation gate

The standalone `joint_placement_construction.py:contact_embed` implements the [exact pre-code policy](b_024_policy.md). Its proposed descriptor is `joint-placement-tree`, config `{}`. The source comparison in `results/codex/track-b024-joint-placement/source_comparison.json` establishes that all **29 inherited functions are text-identical to B023**. The differences are one private preparation method, four entry/error-guard lines and diagnostic identity/header. No other constructor is imported.

Four targeted stdlib groups pass in `checks001` (0.003 seconds unittest, 0.052 seconds process wall). The fixed scrambled path reduces distance cost **5→3**, retains the exact four sites and recounts a valid Q4 singleton minor. Complete-source cost is invariant across the tiny permutation domain and the treatment accepts no exchanges. A second path requires full-target distances outside the initial site set and checks arbitrary integer owners and contact bookkeeping. Work interruption after a private exchange and a final publication-clock interruption preserve the live state; incomplete placement prevents final success credit even for a valid entry. No prohibited imports, public-constructor calls or corpus calls occurred, and no failed check or candidate-policy amendment was needed.

Preparation uses the existing cumulative meter and absolute deadline. Its distance table and permutation stay private until both full passes, final exact-cost recomputation and fresh ownership/contact recount complete. An error guard prevents fallback execution or credit after interruption. Preparation stage units and exclusive stage wall are retained; its whole preparation wall and initialization wall are overlapping totals, not additional costs. Routing, erasure, forced acceptance, pricing, cleanup and final validation then receive the published singleton state and the remaining original allowance.

These checks establish the bounded mechanism and accounting, not quality or speed. Distance ignores routing competition, the same initial footprint may be inadequate, and later repairs may erase a placement gain. The fixed [27-call comparison](b_024_screen_protocol.md) retains every failure and applies the existing complete-constructor gate. Source and checks are stable for root review; no additional tests or tuning are planned.
