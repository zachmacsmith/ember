# C011: isolated computation-allocation ablation

`wall_adaptive_clones.py:clone_embed` is an isolated copy of retired C010. The [accepted proposal](c_010_wall_budget_diagnostic_proposal.md) changes only whether counted work can stop execution. Search and final work denial are both disabled. All integer charges, the existing clock-check predicate/cadence, fifteen-second absolute deadline, 0.5-second final reserve and original validity/non-credit gates remain.

The [static comparison](../../../results/codex/track-c-011-checks/static_body_check.json) identifies changes only in `_Budget.__init__`, `_Budget.tick`, and constructor metadata. Every placement, support-ranking, revision, split, snapshot, original validation, rollback and final gate body is identical. The original C010 file remains byte-identical. No shared runner or registry was edited.

The first **actually charged** operations crossing nineteen and twenty million units record before/requested/after totals, phase, operation name and elapsed wall from budget construction. A clock-denied operation does not create a false crossing. Events are retained once, including when one charge crosses both references. Nineteen million is a search reference, not a treatment limit; if it is first crossed during finalization that phase is explicit. Active search/total work limits are `null`. Extra threshold-observation overhead is included in measured wall time but does not alter the inherited work counts or seeded choices.

Three focused new-risk groups passed in [focused001](../../../results/codex/track-c-011-checks/focused001/status.json), 0.108 seconds process time: exact control denial versus treatment continuation/charges, clock denial and private rollback after crossing, and final validation above twenty million with late/fatal non-credit. No public constructor or former fixture suite ran. A subsequent test-only portability correction loads the identical committed C010 file instead of its saved reference copy; byte equality was checked and only the budget group was repeated in [portable_control002](../../../results/codex/track-c-011-checks/portable_control002/status.json), passing in 0.119 seconds. Candidate code did not change.

Root read the full inherited diff and the three groups. The final reserve may still be insufficient after larger construction; late or interrupted outputs remain uncredited and memory/output costs remain outcomes. The [prepared 24-call screen](c_011_wall_budget_screen.md) tests complete construction with unused wall allowance, not partial-score progress. No local public-constructor, corpus or remote calls were made.
