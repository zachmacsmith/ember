# B029 implementation and focused checks

2026-09-10. The [before-code plan](b_029_union_construction_plan.md) is implemented in three isolated files: `union_cost_mobile_tree_construction.py`, `union_cost_joint_root.py` and `union_cost_trial.py`. Root read the full evaluator, full constructor/router diffs against B028, unchanged attachment/contact/trim code, integer bounds, and all three diagnostic helpers. B028 and shared correctness infrastructure remain unchanged.

The implementation retains finalized route arrays, evaluates an owner's constructed and trimmed site union in private scratch arrays, and descends through adjacent hardware roots by the declared objective. Feasibility root order is `(excess memberships, priced potential, total memberships)`; global visit acceptance remains inherited. Excluded neighbors with incomplete labels are recorded separately. The selected proposal alone receives full state materialization, agreement checks against the original Python pipeline and the existing certificate. Imports, compilation, array packing, materialization and failed work stay charged. The first-sixteen/last-visit trace retention is diagnostic, not an active work limit.

Four focused groups passed on their first and only execution: sixteen saved-root chain/witness/score comparisons across four states; occupancy of three owners and Python-integer fallback; 65 source contacts with explicit unavailable-label exclusions; and interruptions at attachment, trimming, materialization, certificate and publication. These checks run no genuine routing or complete constructor. Root reviewed the test code and actual unittest output and verified eight source-freeze and 31 postcheck bindings. Process wall was 3.272s, user CPU 2.541s and system CPU 0.184s. There was no source correction after a failing test.

The frozen four-state packet contains only earlier candidate states and finalized route maps, the checked new source and existing structural checker/oracle. Root verified all 19 file bindings plus its manifest, SHA256 `7f7987388b5995203636986eea695a4305b314ed16223355729a66588352aa1c`. Each worker has 60 seconds including cold setup, with a 90-second outer watchdog and a 480-second detached driver on hyde02. This is the saved-map falsifier, not a complete-constructor experiment. No MM maps, hidden witnesses, independent starts or complete-output selection are permitted.

The lifecycle changes only the old B028 run/session/manifest identities; root read both diffs and full scripts. One prepare/transfer/preflight/start sequence is authorized, with terminal inventory and digest verification before retrieval. Observation timeouts never authorize restart. After retrieval, freeze and self-review a passive projection using the unchanged checker/oracle; root will review the resulting evidence. Any censored stage, cost regression or local-neighborhood failure remains visible. This preparation does not establish an ACL gain or authorize a later constructor screen.

Evidence is under `results/codex/track-b029-union`: `source_freeze001`, `check001`, `preparation001`, `packet001` and `lifecycle001`. Production SHA256 values are respectively `f1eed24709480f428e5b5912f0d4936a074cdef4998d9dc7117e57e36c0e9dd4`, `dbdea1c1c020fadc0019043bd0c32ae0497dfcc53c7dc817a5a2fd0c03d35b76`, and `da9170ad35b8a1840d973169282f501a8099f4be8bdeb88f21c287ab2a9140cf`. B028 remains rejected; B029 performance is unmeasured here.
