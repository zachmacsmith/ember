# B023 implementation gate

The standalone `temporary_contact_construction.py:contact_embed` implements the [pre-code policy](b_023_policy.md), with no imports of another constructor or embedding package. Root registers method `temporary-contact-tree`, config `{}`. Valid minors belong to this and the earlier state representations; B023 changes which intermediate states may publish. It retains connected nonempty chains while allowing both overlap and missing contacts.

Eight focused stdlib groups pass in `results/codex/track-b023-temporary-contact/checks001` (0.004 seconds reported by unittest; 0.097 seconds process wall). No prohibited imports, public constructor calls or corpus runs occurred. No check failure, pool adjustment or changed candidate policy was needed.

The fixed four-vertex source path uses target edges `(0,1),(0,2),(0,3),(1,4),(3,4)` and chains `{0:{2},1:{0},2:{0,1},3:{3}}`. A scored erasure replaces chain2 with `{1}`, losing contact `(2,3)`: Q/M/O changes **5/0/1 -> 4/1/0**. The following route changes it to `{1,4}`, restores that original contact and produces a valid **5/0/0** map. The test uses an independent Cartesian physical-edge/contact and occupancy oracle. This is primitive reach, not a full-constructor outcome or evidence of optimal Q.

Other checks cover multiple overlapping owners and outside-edge counter updates; free-site and fully occupied singleton fallback; an energy-increasing path repair followed by valid pruning from Q5 to Q2; interleaving erasure before the second logical-edge visit; exact last-unit publication; interrupted second-direction and final-clock rollback preserving the earlier state; and rejection of an impossible triangle/path map even when its scalar counters are corrupted. A late original-graph validation receives no output credit.

Work units count enumerated source/site/adjacency/ownership records, heap operations and copied map records. Scalar administration and sorting CPU remain in the common wall time. Clock polling occurs every 64 charged units and explicitly at action/publication/final boundaries. Stage time is exclusive across the meter's named phases; action wall is an overlapping subtotal, not an additional cost. The one global search meter retains interrupted work and only changes to its reserved final limits once. All route/erasure alternatives are scored before publication; interrupted private work publishes no chain change. Price observations deliberately survive an interrupted subsequent repair, as specified. A valid completed cleanup prefix also survives interruption.

No full-constructor quality or runtime conclusion follows these checks. The next evidence is the fixed [eighteen-call screen](b_023_screen_protocol.md); root owns its freeze and execution. The principal risk remains cycling between contact creation and overlap clearance, with sparse chains still too long despite valid construction. No change is justified by partial progress alone.
