# B025 fixed acceptance comparison

Predeclared before corpus calls. Run `nonincreasing-contact-tree` (B025), exact `temporary-contact-tree` (B023), and fresh stock `mm` on the same nine exposed B inputs and ideal Z12 target: complete40/100, bipartite30×30, ER80/d8, regular80/d3, Watts–Strogatz80, grid8×8, honeycomb5×5 and king8×8. Seed0, twenty seconds, empty configuration: **27 calls on hyde02** in the existing isolated native/MM environments. B025 shares B023's 20M total/19M search work and final reserve; no B024 initialization stage is used.

`results/codex/track-b025-nonincreasing-contact/task_metadata.json` freezes the canonical task fields, raw input hashes and complete ordered vector. Retain B9 source order; per source shuffle `[nonincreasing-contact-tree, temporary-contact-tree, mm]` using `Random(f'{name}:0')`. Root owns registry, immutable source freeze, launch and quiescent retrieval. No local public constructor, extra seed, retry, favorable prefix, per-input policy selection or outcome-driven parameter adjustment.

Require the unchanged **>=7/9 valid outputs and >=3 common MM Q-win/tie gate**, including optimal ACL1 ties. Independently validate original graphs and Q; late, invalid or failed outputs receive no credit. Report all nine paired outcomes and all-attempt solver/CPU/process totals, including failures, with same-host load qualification and no historical time pooling.

Initial maps and source/target/edge orders should match contemporaneous B023 when both initializations finish. Audit exact initial/final Q/M/O/F. Retain every selected proposal, post-price threshold, complete acceptance rejection, interruption, price observation, discarded score, first-feasible/final Q and work/time subtotal. This directly distinguishes rejection from candidate-generation failure or incomplete publication; rejected valid proposals remain diagnostic only. Prior prices survive rejection, and subsequent trajectories may diverge. A partial energy gain or fewer route calls is not success. Failure of the complete gate retires this rule without changing its passes, prices, caps or neighborhoods.
