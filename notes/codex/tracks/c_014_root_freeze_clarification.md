# C014 pre-execution clarification

2026-09-09, before freezing or launching any C014 Z12 panel call.

The screen amendment's C014 / A061 / MM order is the declared method vector.
Actual execution uses the audited pilot's deterministic per-graph shuffle,
`random.Random(f'{name}:{seed}').shuffle(order)`. The frozen ordered task digest
binds that execution order. This clarification corrects the amendment's wording;
the harness remains unchanged apart from the previously reviewed registry entry.

The [variance objective clarification](../variance_objective_clarification.md)
applies prospectively. Mean ACL is primary, success and runtime deficits remain
explicit, and variance increases alone are not automatic vetoes. C014's fixed
exploratory continuation criterion, inputs, seed, 60-second allocation and
constructor bytes are unchanged. Both original notes and all check bindings
remain preserved.

Root authorizes the prepared 27-call screen on hyde03 after review of the full
constructor, five focused check groups, both optimal tiny complete controls,
the independent peer review and the exact input specification. This is an
exploratory mechanism test. The tiny controls exercised no retractions, so
successful coordinated reconstruction and diverse end-to-end quality remain
unproven. No confirmation set or algorithm promotion is claimed.
