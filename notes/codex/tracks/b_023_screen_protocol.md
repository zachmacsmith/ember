# B023 fixed complete-constructor gate

Pre-observation protocol: eighteen calls on the unchanged B9 sources and ideal-Z12 target bytes in `results/codex/track-b-contacts-020/`. Source order is complete_40, complete_100, bipartite_30_30, random_er_80_d8, regular_80_d3, watts_strogatz_80, grid_8x8, honeycomb_5x5, king_8x8. Names are evaluator identities, never constructor inputs.

For each source, start methods `[temporary-contact-tree, mm]`, shuffle once with the existing pilot's `Random(f'{name}:0')`, and call each once with seed0, timeout20 seconds and config `{}`. The [exact policy](b_023_policy.md) fixes 20M total/19M search units, eight interleaved edge passes, unit initial prices with per-selected-defect increments, and a one-second final reserve. There is no per-query work/time reset. Preserve all eighteen statuses and all failed work. `task_metadata.json` binds the exact canonical vector, with file-byte SHA values separately labeled. Root owns registration, source freeze, isolated fresh-process execution on hyde02, one-thread native/MM environments, staging, launch and retrieval. No local public-constructor calls precede this screen.

Credit only a timely successful output independently validated on the original source/target labels, with Q recomputed. Failure/timeout/private Q/M/O/F is diagnostic only. Retain process/solver wall and CPU, imports, host load, actual deadline returns and complete/limited actions. Measure first feasibility, cleanup savings, lost contacts subsequently restored, erasure and price counts, energy-increasing repairs, initial/final M/O/Q/F, and stage work/time. State explicitly if cost prevents any erasure, price update or full pass. Prices change, so endpoint F is not a common descent metric. Recorded intermediate reach is not neighborhood completeness, causal attribution or complete embedding quality.

Require **at least seven of nine timely valid candidate outputs and Q<=MM on at least three common timely successes**, including optimal ACL1 ties. Otherwise retire the policy without automatic changes to prices, roots, pass order or budgets. Report median common solver-time ratio and all-attempt totals separately; a quality pass does not establish MM-scale cost. All historical B policies stay frozen, and no historical timing is pooled. These are exposed development inputs, not held-out evidence.

After verified terminal retrieval, perform one minimal saved-data analysis using the existing B9/original-label validator, adapting only the new declared M/O/F and action schema. No exhaustive intermediate replay or additional solver call is required.
