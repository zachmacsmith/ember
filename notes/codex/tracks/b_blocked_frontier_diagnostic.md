# B010 diagnostic: classify the saved B009 ER obstruction

Predeclared before new diagnostic observations. B009 blocks after 63 placements without exhausting matching or global work. Hypothesis: the committed partial minor leaves incompatible pending-contact obligations, so detecting singleton capacity conflicts is insufficient without revising ownership. The alternative is that bounded root/path selection misses an available insertion.

Use only the original saved B009 ER input, target, diagnostic and partial embedding. Validate that partial minor with the existing original-graph validator. Reconstruct the pending/promoted sets and deterministic next vertex from the unchanged selection rule. Compute actual free connected components and each placed chain's free boundary. A fixed connected new chain requires some free component touching every required neighbor. Separately reserve each chain's sole free port when that chain still has an unplaced neighbor after this insertion; recompute contact reach without those ports. Retain competing owners and their original pending obligations, not merely counts.

These exact set calculations have a fixed five-second deadline and record adjacency scans/cost. A failed deadline gives an incomplete diagnostic, never an impossibility claim. Component reach is necessary; it does not by itself construct a chain preserving every multi-port obligation or prove the existing extension routine cannot help.

Only if these sets do not resolve the distinction, allow one read-only invocation of the frozen supplied-state insertion primitive, with at most 500,000 additional counted units and five seconds, and stage/root/path observations. It starts from the saved partial state, never reruns a constructor or MM, and cannot change an incumbent. A limited unsuccessful query proves nothing about existence. An explicit valid insertion witness can establish missed reach.

After classifying the obstruction, specify one general conflict response with pseudocode, bounded work, critique and a cheap witness. No response implementation or new benchmark is part of this diagnostic. Self-critique: sole-port protection detects only a narrow subset of simultaneous boundary conflicts; infeasibility with frozen ownership may disappear after even one useful chain extension or relocation. Preserve that scope and do not infer global graph infeasibility or tune a response to the input's name.
