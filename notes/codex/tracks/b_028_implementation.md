# B028 implementation and focused checks

Implemented the root-reviewed [acceleration-only policy](b_028_routing_policy.md), whose SHA remains `7532cf14cbc352ef03291b9e08b880f9ea376c2e8b35598837aa31c00cdba170`. **No complete constructor or remote timing packet has run. B027 remains rejected; B028 has no end-to-end evidence yet.**

The standalone wrapper copies B027 and preserves its original joint-root method as the arithmetic fallback. Its replacement loads only the sibling compiled module by path, inside the solver clock, avoiding unrelated package initialization. Static comparison verifies every inherited engine method and support class is unchanged, and the public wrapper differs only in identity and an acceleration receipt. Attachment, witness selection, trim, publication, pricing, recurrence and original validation still use the same Python methods.

The kernel uses Numba 0.65.1, actual target adjacency in inherited rank order, integer distance/predecessor arrays and the supported typed heap. It preserves complete dictionary contents **and insertion order**, root and predecessor ties, stale-entry rejection before strict stopping, and all route inputs seen by unchanged attachment. Unsafe fixed-width arithmetic invokes the original local Python query without comparing alternative outputs. The implementation uses checked array indexing, no disk cache and no parallel execution. It returns to Python every 256 heap pops solely to observe the live deadline; no search work count ends an otherwise timely query.

Costs are recorded in the existing stage meter and an `acceleration` receipt. `routing_import`, `routing_target_pack`, `routing_query_pack`, `routing_allocation`, `routing_seed`, `routing_kernel`, `routing_conversion` and `routing_arithmetic_fallback` distinguish charged components. `compile_dispatch_wall` includes first-call kernel work together with compilation and is not a pure compiler-time measurement. Typed-list setup can also incur compilation within the charged allocation stage. `max_warm_dispatch_wall` excludes dispatches that compile a new kernel signature; cold dispatch maxima retain them. No measured speedup is claimed from the local correctness suite.

## Preserved attempts

- [preparation001](../../../results/codex/track-b028-routing/preparation001/output/summary.json): all six input preparations failed before any route evaluation. The preparer selected hashes with a broad `/results/` substring, inadvertently including task-file hashes as well as raw-result hashes. Its source and results remain saved. The correction selects files whose immediate parent is `results`; case identities and selection policy did not change.
- [preparation002](../../../results/codex/track-b028-routing/preparation002/output/summary.json): all six unchanged queries prepared successfully, 1.242 s process time, zero route/constructor calls. Selected neighbor counts are 15, 9, 4, 3, 36 and 18 for g0001/g0005/g0013/g0014/g0016/g0019. Grid and honeycomb queries are in valid quality states; the other four are feasibility queries. Only saved B027 chains, witnesses, ownership and prices were used. Cached tree state is neither available nor reconstructed: B027's trim derives these local query inputs without reading it. This provides identical local inputs to both arms, not an identity claim about the original full search trajectory.
- [check001](../../../results/codex/track-b028-routing/check001/stderr): inheritance/dependency-isolation passed. Six routing groups encountered a Numba typing error because `not` was unsupported on the typed heap list. Process time was 2.270 s; the failed source and traces are preserved. No nonbinding semantic mismatch was observed before those compiler errors.
- [check002](../../../results/codex/track-b028-routing/check002/stderr): only the six affected groups reran after replacing heap truth conversion with explicit length comparison. All passed, 2.331 s process time / 1.978 s suite time. Source hashes were unchanged during both attempts. No policy, fixture, source-family choice or benchmark allowance was modified.

The focused groups cover seeded rank ties and strict predecessors; successive quality/price changes and explicit empty ownership patches; disconnected/empty boundaries and the untouched no-neighbor rule; source/target labels outside int64; arithmetic admission and fallback; direct stale-entry/equal-score stopping; exact attachment and full private proposal chains/witnesses/trees/ownership; and injected interruption after one unfinished 256-pop chunk, with the prior state untouched. Two supplied valid-state fixtures undergo six paired private proposal transitions, with resulting minors checked by the existing original-graph oracle. These local checks are not full-constructor successes or a universal equivalence proof. The original oracle SHA remains `e4718c2ec09f3ec80e035ef89648f81b45413f52c7580123ef129419b25612d9`.

## Root review packet

- Kernel: `packages/ember-qc/src/ember_qc/algorithms/factored/compiled_joint_root.py`, SHA `c729ccd815262ec9ebf18eaa24f33544719c53ca610ecd7d823fbc63456564c8`.
- Wrapper: `packages/ember-qc/src/ember_qc/algorithms/factored/compiled_mobile_tree_construction.py`, SHA `16ec9260a0971acd81a79cb4d8577fbbaf34653d784f6d21ddd488ab724775fe`.
- Tests: `tests/algorithms/test_compiled_mobile_tree_construction.py`, SHA `1abb05e90fa10b0abd56b6d654a143a5f6175e93ea48bcbf7893c2147d9775af`.
- [Frozen packet](../../../results/codex/track-b028-routing/packet001/manifest.json), SHA `58498fa9fc696fe4c79aa0c348dd9569899cf68f4b45c832f633c255498e99c7`: 12 bound files containing six queries, target, isolated sources, saved harness import blocker and the unexecuted runner. It prescribes one fresh B027 packet process followed by one fresh B028 packet process, paired on hyde02. Each has a 60 s query allowance and records every completed, failed or unattempted query. An outer process observer must record return status, elapsed process time, stdout/stderr and any watchdog termination; the inner 90 s alarm protects a stuck native extension. Root must review and authorize launch separately.

The root rechecked hyde02 package metadata in `results/codex/transfer-cycle-002/environment001/hyde02`: native Python 3.10.12, NumPy 2.2.6, Numba 0.65.1 and llvmlite 0.47.0, with MM absent; MM 0.2.22 exists only in the comparator environment. Reported load was 33.58 on 32 logical CPUs. No dependency installation is needed. Packet timing, if authorized, must include lazy import, all compilation and data adaptation; a subsequently reused kernel timing is secondary. Substantial acceleration would justify the separately frozen small complete-constructor screen in the policy, not promotion or a claim that cost explains all B027 failures.
