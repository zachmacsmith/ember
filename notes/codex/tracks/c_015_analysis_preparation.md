# C015 passive analysis preparation

2026-09-09. **Reader written; not executed.** Root owns the eight-input, 24-call Z12 screen and its common original-label audit. This preparation reads no new screen outcomes, imports no candidate module and performs no constructor call or cluster action. Screen/freeze and common-adapter identities must be added and reviewed before the reader can run.

Reader: `results/codex/c015-transfer-analysis/analyze.py`, SHA256 `9c24b244fdac401bded9311e480c06fdbbe4e66ebcbe4e50daf581a5920ce315`. Its syntax was AST-parsed without import or execution. It fixes the eight contracted graph IDs and the candidate/A061/MM method vector, verifies all 24 common-audit rows and task identities, and binds each candidate record to the passing common audit. The common audit alone supplies final credit, original-label validity, per-class comparisons and complete all-method time accounting; this reader never opens an MM embedding.

The implementation reuses the pinned C014 passive reader's scalar, capacity and snapshot projection helpers (`4f5f2681...`) and the unchanged original embedding oracle (`e4718c2e...`). Saved published, final and last-placement-entry states are projected using exact graph node-list order and checked by that oracle on the induced partial source graph. Historical intermediate minors are not replayed. Publication capacity ledgers and retraction counts are checked against source adjacency, without claiming an independent reconstruction of each historical transaction.

The report separates:

- Generated, started and completed roots. Successful first-admissible stopping has intentionally unstarted roots; it is not exhaustion. An interrupted root list can be unrecorded. Started-root counts for the unchanged contact guide are unknown.
- Completed policy failures, contact disconnection, capacity obstructions and deadline interruption. Exhausting the implemented policy is not proof that every connected tree fails.
- Published progress and the last saved placement-entry state. That entry may lag the latest publication after a successful birth; it is not automatically the deepest reached or currently failed state.
- Complete external call costs and nested placement/stage diagnostics. Nested intervals are not added into a second runtime total.

`preparation001.json` records this source and its fixed dependencies. A separate `identity.json` must contain the immutable screen/freeze/manifest/snapshot hashes and exact common input-adapter identity; the launch also supplies its expected SHA256. This allows source review now without inventing a freeze or accepting later unbound substitutions. No algorithm refinement or additional experiment follows from reader preparation.
