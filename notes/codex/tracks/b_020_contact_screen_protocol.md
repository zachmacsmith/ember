# B020: fixed B9 contact-neighborhood screen

2026-09-08. Pre-observation protocol; no panel call, registry change or remote launch yet. Compare three complete methods, each once per input: **movable-contact-trees** (frozen B019), **movable-contact-single** (B020 scope control), and fresh stock **mm**. This is 27 independent calls, never a selector or portfolio. The control hypothesis and its limits are in [the pre-code note](b_020_neighborhood_control.md).

Use the exact B011 source and ideal-Z12 target JSON bytes, retaining this source order: complete_40, complete_100, bipartite_30_30, random_er_80_d8, regular_80_d3, watts_strogatz_80, grid_8x8, honeycomb_5x5, king_8x8. Names identify frozen development inputs; they are not algorithm inputs or dispatch rules. Seed 0, 60 seconds, configuration `{}` for every arm. Within each source, pass method order `[movable-contact-trees,movable-contact-single,mm]` through the existing pilot's `Random(f'{name}:0').shuffle` once. Freeze the resulting 27-element vector before observations. No held-out data are exposed.

Both constructors retain eight sweeps, a 20M total/19M search work bound, 100k per query, nested five-second query deadline and a one-second final reserve at this timeout. All setup, routing failures, partner selection and finalization use their original meter/deadline. Single-primary removes unused partner selection and reduces at most sixteen combinations to four; no artificial unused work is charged to normalize timing. Edge visits and initialization are otherwise identical, while accepted moves may diverge.

Run serial fresh processes on hyde02 through the audited pilot/controller, one thread, empty per-task JIT directories. Use the already pinned isolated native and MM environments under `/home/dabh/ember-codex/envs/4e1fb892db12754e/`. Candidate imports must exclude MM/busclique; MM runs only in its separate comparator environment. The existing absolute candidate deadline includes caller graph copies. Preserve actual outer solver and process wall, CPU time, host load/affinity, versions, all raw outputs and process failures. A source snapshot and registry approval are still required before executable freeze; `prepared/manifest.json` is explicitly preparation-only. No controller is created by preparation.

Validate every claimed minor against the original input graphs with the existing independent pilot validator; recompute Q and ACL. Timely valid `SUCCESS` is required for credit. Retain late valid diagnostics, invalid/error/limited/overlapping outputs and missing records distinctly. Never turn a failed partial state's Q into an ACL. For both candidates, report initial versus final Q/O, whether success required resolving overlap, visited/completed/limited queries, combinations, commits, stage/query work, initialization/query/finalization time and exact stop reason. Match complete initial states across arms when both are available; an initialization interruption stays visible. Final energies at different prices are not comparable descent values.

Primary comparison is paired/single timely success on all nine, followed by per-input Q/ACL and wall/CPU on common successes. Also report each against fresh MM, including optimal ACL1 ties and comparator failure/timeout. Save one row per input with all three statuses; means use explicitly stated common denominators. Query completion/cap rates help distinguish a costly neighborhood from ineffective completed proposals, but cannot diagnose the shared representation or acceptance rule alone. No source, pool, cap or order changes follow intermediate results. After all 27 terminal observations, make one continue/redesign/retire decision; any follow-up is a new declared experiment.
