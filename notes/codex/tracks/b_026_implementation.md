# B026 implementation and four tiny gates

The isolated [constructor](/Users/dabh/ember/packages/ember-qc/src/ember_qc/algorithms/factored/mobile_tree_construction.py) implements the reviewed [contract](b_026_policy.md). It has no work, query or pass cap, and uses the supplied deadline without a20s clamp. Shared physical coupler endpoints protect retained contacts during simultaneous neighbor pruning; jointly settled distances select the exact declared root proxy, and shared paths rebuild the visited owner. Before feasibility, complete certified proposals may increase Q/O. After feasibility, valid equal-Q movement is permitted and larger-Q proposals are rejected. Fatal errors suppress output; a search deadline may return only a timely independently recertified incumbent.

Publication shallow-copies trusted chain, witness and occupancy maps, applies local deltas, and swaps one state pointer after the final check. This adds charged `O(n+m+occupied_sites)` copying per visit; it does not rebuild ownership by rescanning all chains or physical adjacency. Root was notified of this explicit bookkeeping choice before gates. Canonical new contact selection orders the visited owner's endpoint first, then the neighbor endpoint, while storage orientation follows numeric source rank. Unchanged other-edge witnesses remain protected at both endpoints.

The eight declared targeted groups passed in0.005s (process0.149s): actual Q5→3 stem relocation, shared neighbor–neighbor witness preservation, repeated terminals versus false overlap contact, independent complete tiny distance calculations, shared paths/outside-owner overlap deltas, feasibility/quality acceptance, interrupted private stages and fatal-error/deadline finalization. The public wrapper's fatal/deadline check uses a stubbed search, not additional full construction. Before any full gate, review found that witness-only reassignment must count as state change to avoid a false fixed-point stop. The initial source/test bytes were preserved under `check001`; the corrected neutral/publication group, including a two-coupler witness fixture, passed separately. No failed test was hidden and no gate outcome triggered a policy change.

Exactly four prescribed seed0 full constructor calls then ran with5s each and the import guard. An independent original-edge/set validator checked every returned map. No MM, busclique, native constructor, corpus or remote call occurred.

| Fixed case | Result | Initial Q/O | Final Q/O | Caller wall | Work |
|---|---|---:|---:|---:|---:|
| P3→P5 | SUCCESS, Q3 | 3/0 | 3/0 | 0.000513s | 308 |
| K1,3→K1,3 | SUCCESS, Q4 | 4/0 | 4/0 | 0.000570s | 425 |
| Triangle→C4 | SUCCESS, Q4 | 4/0 | 4/0 | 4.750076s | 6,174,476 |
| Triangle→P4 | FAILURE | 4/1 | 4/1 | 4.750048s | 5,962,479 |

All positive gates were already feasible after initialization. They therefore do **not** establish overlap resolution by later sweeps. The supplied deletion-minimal stem fixture separately demonstrates a real boundary move and Q reduction.

Triangle→C4 reached its known optimum Q4 in0.000478s, then made25,512 neutral changes over12,756 completed quality sweeps without reducing Q. The search deadline discarded one complete private proposal and retained the independently recertified valid incumbent. This is a concrete acceptance/scheduling cost limitation, not a routing interruption explanation for poor feasibility. The impossible triangle→P4 retained overlap1 after11,701 feasibility sweeps; failure there is correct and not evidence against representable cases. Both long calls used the explicit0.25s finalization reserve. These tiny CPU/wall values are not MM timing comparisons.

The implementation, targeted evidence and all four outcomes are stable for root review. No cycle rule, price, root, cap or stopping adjustment has been made. The root-owned22-input B026/A061/MM screen remains unexecuted by this agent; its decision must use complete fresh-input validity/Q and all-attempt cost, not these intermediate move counts.

[Raw outcomes and summary](/Users/dabh/ember/results/codex/track-b026-mobile-tree/gates001/summary.json), [gate invocation](/Users/dabh/ember/results/codex/track-b026-mobile-tree/gates001-invocation.json), [focused tests](/Users/dabh/ember/tests/algorithms/test_mobile_tree_construction.py), and [artifact manifest](/Users/dabh/ember/results/codex/track-b026-mobile-tree/manifest.json) retain exact source/input identities and timing.
