# C008: fixed full-constructor compound-transfer screen

The [approved proposal](c_compound_transfer_constructor_proposal.md) tests whether a private transfer/reconnection/Q-compensation move improves complete construction. Published chains remain connected and disjoint. The preceding saved-state gate had two local witnesses, 24 successful connected controls and one lost grid observation; it provides no full-minor or speed result.

Freeze **24 separate calls** on the same eight original development inputs used by C007: path 2030, tree 5058, grid 1584, planar 31536, ER 6450, regular 14334, complete 1041 and SBM 30736, in that order. Each gets stock MM, unchanged atomic-regions and compound-regions, seed 0, fifteen seconds, sequentially on **hyde03**, using the existing `4e1fb892db12754e` native/MM Python 3.10.12 environments. Per-input arm order is fixed by `Random(f'{name}:0').shuffle(['mm','atomic-regions','compound-regions'])` before observations. These are independent experimental arms, never a fallback or portfolio.

The source/target bytes, singleton initialization, original ordinary proposals, RNG conventions, four-kind schedule, 16-score cap, acceptance, global visit cap, cleanup and final validation reserve remain as specified. The compound addition permits one bounded interior-screening attempt per transfer visit, 128 per constructor, 25,000 records/0.05 seconds each, one bridge and at most 64 compensation visits. A compound score can displace one ordinary score within the shared 16-score prefix; the focused test demonstrates this explicitly. Its private finish timestamp excludes subsequent ordinary scoring. Root reviewed the final source and four passing focused groups; no new constructor calls were used in those checks.

**Mandatory continuation threshold:** compound must achieve **at least four timely original-valid completions out of eight and retain every atomic-control success**. Otherwise retire it, regardless of partial missing-edge improvements. Passing this threshold does not establish superiority to MM. Report all 24 statuses, original Q/ACL, solver/process times, and noncredited partial or late maps. No one-seed variance, family mean, generalization or cross-run timing claim is supported.

The minimal saved-data analysis will verify immutable input/result identities and the original-label validator, then compare fixed arms on every input. Compound records separate screening exclusions, no bridge, inflation, local/global limits, certificates, selection and commits, including work and actual wall time. Independently recount final M/Q and require successful records to have return code zero and finite nonnegative solver time within fifteen seconds; audit errors and missing outcomes receive no quality credit. Preserve all raw observations before schema assertions.

The [comparison plan](../../../results/codex/track-c-008-protocol/comparison_plan.json) binds exact input hashes and 24 task templates. The [initializer argv](../../../results/codex/track-c-008-protocol/initialize_invocation.json) is prepared for root only; source snapshot, staging and launch are not performed by this preparation.
