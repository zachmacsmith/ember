# C014: exact Z12 constructor screen amendment

2026-09-09, **before panel execution**. The first C014 focused gate and both actual complete tiny controls passed; root and independent peer source reviews found no blocker. This separately replaces the held C013 experiment in the [Transfer002 protocol](../transfer_cycle_002_protocol.md), preserving that protocol and every C013 byte/outcome. It does not authorize a C013 rerun. Root will register, freeze and launch this packet; no panel call has been made by the C014 agent.

**Frozen mechanism:** `zephyr_capacity_contact.py:capacity_contact_embed`, descriptor `capacity-contact-domain`, empty configuration. Module SHA256 `3b452c6167707bb8a351aa34028fd821586d4f7f253504bd81489ace593a2a64`; policy SHA256 `647b47f99fea5ce58a3a6d177ea944f716e27d70445341a5aa97d96aa34dd3bc`. Use the already audited inputs in `results/codex/transfer-cycle-002/panel.json`, in this exact order:

| Key | Structure | n | m | Exposure |
|---|---|---:|---:|---|
| g0001 | ER80 | 80 | 323 | reused Transfer001 development |
| g0005 | SBM80 | 80 | 195 | reused Transfer001 development |
| g0008 | BA160 | 160 | 624 | reused Transfer001 development |
| g0004 | Watts–Strogatz80 | 80 | 240 | reused Transfer001 development |
| g0012 | planar160 | 160 | 474 | reused Transfer001 development |
| g0017 | hidden singleton control80 | 80 | 488 | reused control |
| g0013 | grid128 / Ember1584 | 128 | 232 | reused anchor |
| g0101 | ER100, p0.08 | 100 | 378 | newly generated development |
| g0102 | BA100, m3 | 100 | 291 | newly generated development |

For each input, run methods in fixed order **C014 / A061 / MM**, separately: descriptors `capacity-contact-domain` / `native-branched-path` / `mm`, seed0, timeout60 seconds, configuration `{}`, all on **hyde03**. Total **27 fresh calls**; no warmup, retries or independent-output selection. Use ideal Z12 and the existing detached supervision. Timing controls are fresh on the same host, never substituted from earlier runs. The final staged task vector and source/helper hashes are root's pre-execution authority.

Reuse the audited pilot, isolated native environment, pinned MM comparator environment and independent original-graph oracle. Sanitize runtime inputs exactly as before; evaluator originals, family metadata, exposure records and witnesses remain outside candidate bundles. Keep wall/CPU, full process time, stage and root costs, partial publications, rejected growth, private rebuild failures and deadline censoring. No artificial work cap is introduced. Sixty seconds is this experiment's fixed allocation, not a universal promotion gate or a claim that 60 s resembles MM runtime.

**Hypothesis resolved by this screen:** actual-boundary capacity can remove premature contact shortages across source structures while permitting low-degree singleton choices. The tiny star already shows that growth removes C013's specific singleton exclusion; the new screen must show useful complete-constructor transfer. Attribute failure carefully: a shortage despite maintained individual counts implicates simultaneous geometry/representation; quota blockers without successful coordinated movement implicate the neighborhood; generated capacity-safe alternatives discarded by score implicate acceptance; useful incomplete construction cut off by measured placement/rebuild costs implicates computation. Receipts may support more than one explanation; they do not prove it uniquely.

Continue only if C014 has lower complete Q than A061 on **two independently generated development structures from different families**, at least one no worse than timely MM, and loses no A061 success on the hidden singleton control. The two new ER/BA inputs and independently generated Transfer001 structures can supply that evidence, with exposure stated explicitly; grid and the control are not newly generated evidence. Preserve every success, Q, variance and runtime regression; this small screen cannot promote an algorithm or establish the final class-level objective. Evaluate slower runtime against the user's roughly-MM-order-of-magnitude target with quality first, without adding a universal ratio gate. Report per-input and per-family success, mean ACL, within-embedding chain variance and timing; do not let dense-graph aggregate gains conceal sparse regressions. One seed cannot estimate across-seed variability.

Both tiny complete calls used **zero retractions**. The screen therefore also establishes whether coordinated movement actually occurs and helps complete construction. Intermediate domain/capacity gains or more retries alone do not justify continuation. If the criterion fails, reject the fixed policy; any bounded follow-up needs a specific discriminating observation and a separate before-code policy. If it passes, promptly test broader development sizes, labels and seeds, leaving confirmation untouched.

Self-critique: seven inputs have already influenced design, only two structures are new, and there are no paired relabelings here. Tiny optimality and the necessary fixed-chain capacity condition do not establish class-level generality. The [C014 result note](c_014_implementation.md) records this exploratory scope; the [policy](c_014_capacity_policy.md) also corrects the frozen protocol's Z2 maximum degree from 20 to 19, without modifying its old bindings.
