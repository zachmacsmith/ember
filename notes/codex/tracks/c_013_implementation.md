# C013 implementation and failed tiny gate

**Hold the panel.** The [reviewed policy](c_013_contact_domain_policy.md) is implemented in [zephyr_contact_domain.py](../../../packages/ember-qc/src/ember_qc/algorithms/zephyr_contact_domain.py), with the three root clarifications recorded before code. Four targeted risk groups passed on their first execution. The six-call complete-constructor group exposed a policy failure on the degree-21 star. No code or policy was changed after that outcome; no checks were rerun and no panel, MM or remote candidate call occurred.

The standalone API is `contact_domain_embed(source, target, *, seed=0, timeout=60., deadline=None)`, proposed descriptor `contact-domain`, config `{}`. It constructs a single partial original minor using full-target contact domains and coordinated private retraction. Source and target rank integers determine ties. A growing obstruction set permits movement of closed owners; private rebuilds publish only with the identical old introduced set plus the new source vertex. Counters never deny work. The module loads the unchanged C012 graph/Zephyr parsing and budget helpers and the existing backend's `_is_valid_embedding` directly by file path; it calls neither prior constructor nor package registration initializers. No shared file or registry was edited.

All five declared groups ran once under isolated native Python with prohibited-import guards. The actual six tiny constructors used seed 0 and five seconds per call. Five used ideal Z2; the negative used four sites on an actual Zephyr course with their supplied path couplers. Each returned map was checked by the existing independent original-edge oracle.

| Actual complete call | Status | Credited Q / ACL | Wall / CPU, ms |
|---|---|---:|---:|
| Path, 8 vertices | SUCCESS | 8 / 1 | 9.728 / 9.718 |
| Binary tree, 7 vertices | SUCCESS | 7 / 1 | 8.800 / 8.748 |
| Star, 22 vertices | FAILURE | — | 41.157 / 41.042 |
| Grid, 3×3 | SUCCESS | 9 / 1 | 9.560 / 9.532 |
| Path of 3 plus two isolates | SUCCESS | 5 / 1 | 10.465 / 10.449 |
| Triangle into path of 4 | FAILURE, expected | — | 0.586 / 0.570 |

No call raised an implementation error, exceeded its allowance or attempted a prohibited import. The four credited outputs achieve the vertex-count lower bound. Their tiny performance does not establish transfer or MM superiority. The suite took 0.128 seconds and its subprocess 0.605 wall / 0.451 CPU seconds; the saved [attempt001](../../../results/codex/track-c-013-checks/attempt001/status.json) records exit 1 and retains stdout/stderr, exact invocation, before-execution sources and every call, including the failed star. The deprecation warning and failed unittest assertion remain intact.

The supplied-state checks establish specific implementation behavior: empty singleton domains can still yield a multi-site tree; moving a previously fixed chain invalidates the conditional Q bound; root removal retains required contacts; missing hardware couplers remain absent. The declared path fixture moves a closed isolate and permits Q4 completion. A second fixture expands the conflict set from owners `{0,1}` to `{0,1,3}` through an actual occupied guide site, then completes Q4 while preserving the entry state. An impossible private rebuild rolls back without a published prefix. Four separate injected wrapper calls check a certified complete private result during a censored root scan, interruption before certification, fatal-error noncredit and late-valid noncredit. Those injected-clock times are not performance observations. No supplied-state outcome is counted among the six real complete calls.

**Mechanism resolved by the failed star.** The final published state contains 20/22 vertices and Q20; the hub remains a singleton. The last ordinary placement empties both remaining leaves' domains. Its completed coordinated rebuild loses the score comparison. The next placement has no fixed-chain extension; a rebuild retracting every introduced owner again places the hub as a singleton and fails with no outside owner left to add. All scans involved complete, and no deadline or diagnostic work limit stops the search. Retraction consumes 21.561 ms, ordinary placement 12.807 ms. The declared singleton-first representation cannot grow an early hub while it still has singleton placement options, and retraction recreates that restriction. A degree-21 source hub cannot be a singleton anywhere on this Z2 target, whose maximum degree is 20. More time under this policy cannot repair that omission.

This is stronger evidence against the fixed policy than the positive domain counters are evidence for it. Domain cardinality does not express simultaneously required distinct neighboring chains. A bounded subsequent design could test capacity-aware growth while placing a vertex, using remaining original incidences and actual free/occupied neighbor structure rather than a source-family rule. That would need its own pre-code policy and a general mechanism for shared-tree growth; merely increasing the retraction group or changing acceptance would leave this representational exclusion. No such follow-up is implemented or authorized by this note. Preserve C013 and the failed gate for root's next mechanism decision.

The [freeze manifest](../../../results/codex/track-c-013-checks/freeze.json) binds implementation, policy, tests, reused helpers/oracle and the first-attempt records. Root's separate Transfer002 input and eventual full-result validators remain authoritative; this packet contains no development-screen evidence.
