# B031 passive complete-screen reading plan

2026-09-10, before outcome reading. The 48 calls are frozen: six encodings,
seeds 0/1, strip transport / ordinary-only / A061 / MM, 60 seconds on hyde02.
Screen SHA `d5e8e91aa1f6642718fb0bbdccd036f8e034028d44daecca93eb6395ae17ee21`;
source snapshot `3b6b60cc7a64f1c080bf9bf542001d299effb74e04a2df54e6052dc6f50878fc`;
manifest `ebbf7365fd534dfcda5dc46a2a5b84ea99c8b5d056a91187e315949d5955e2b9`.

The reader consumes PASS outputs of the unchanged shared
`results/codex/three-track-20260910/analyze_multiseed.py` (SHA `a4c16e83...`).
That audit alone assigns validity, success, Q, ACL and within-chain variance.
No validator, generator, candidate, witness or alternative audit is imported.
Root owns terminal retrieval and both invocations; this script requires review
before its first execution and preserves reader failures without retry.

Keep all 48 calls, every paired seed result, all failure/unknown time fields,
and two-seed support for means and sample variances. Show every lost success
and common-success Q regression, including the ordinary arm and A061–MM gaps.
Relabel g0309 is paired transfer evidence, not another independent structure.
Runtime ratios on unsuccessful calls describe observed expenditure, not speedup.

For both B arms, preserve all recorded scalar birth, transport, recovery, prefix
and private-candidate events. Full first-valid/incumbent Q is separate from prefix
Q; a native valid event on a late/fatal call earns no audit credit. Count blocked
ordinary births, actually prepared maps, occupied-band rejections, identical maps,
and distinct observed empty `(birth,direction,B)` bands. A missing or interrupted
receipt is unknown. Geometry enumeration/materialization/sorting and time before
the first insertion must be visible: censorship there is not domain exhaustion.
Phase wall is exclusive; query/geometry receipt times overlap those phases and
must not be added again. Public-prefix certification is a native receipt, not
an independently replayed intermediate minor.

Apply the frozen continuation conditions: all twelve full calls credited;
published transport recovery after a completed blocked ordinary query on two
distinct structures including fresh development; and closure of at least one
third of the positive A061–MM two-seed mean-Q gap on two structures (when A061
already ties/beats MM, require strict improvement over A061). Require all three
methods' two-seed credit for each gap comparison. Show unsupported cases and
every seed regression, even when a mean passes. Report numerical evidence for
root's decision; do not convert this exploratory criterion into promotion.

Ordinary-only divergence follows the first blocked placement; later trajectories
do not isolate a single transport's final-Q effect. Useful local recovery with
poor complete embeddings rejects this fixed construction policy, without proving
that every contact-preserving deformation is useless.
