B022 is implemented in the new standalone `whole_incidence_construction.py`, entrypoint `contact_embed(source,target,*,timeout=60.0,deadline=None,seed=0)`. It uses stdlib only. B019/B020 source bytes are unchanged. The registry descriptor is root-owned; no corpus or public-constructor call has run from this author task.

The query releases both complete selected trees and every incident binding. Root pairs and connected growth share a four-state beam; frozen-neighbor and internal contact paths follow the predeclared order. Contacts are recomputed from actual target adjacency. Complete children choose new witnesses, prune while retaining their endpoints, and receive exact Q/O/energy plus original-edge certification. The old partial state stays untouched until the full staged replacement passes its deadline check.

Unlike B019's narrower scope, outside owners can receive different witness endpoints even though their chains stay fixed. Commit therefore updates their terminal multiplicities as well as the conflict totals of all owners sharing changed sites. The weighted affected-owner check explicitly verifies both. Query bookkeeping counts changed witnesses before return under the same query allowance; an outer rejection retains an honest discarded-eligible count.

All eight targeted stdlib checks passed in `checks001` (0.009 seconds reported test time; 0.164-second harness process). The independent physical oracle confirms the fixed joint witness Q5/O1/E6→Q4/O0/E4, with unchanged outside chains. Enumerating only the 63 possible nonempty subsets for each of its two single-owner replacements confirms minimum E6. Other checks cover triangle-on-path rejection, internal path cuts, weighted outside terminal/conflict updates, exact final work unit, discarded eligible proposals after work/deadline/staging failures, nonmutation, and strict feasible-Q acceptance. No forbidden embedding import was attempted. No failed test attempt or policy retuning occurred.

The only policy clarification made during implementation is inherited price-update semantics: a sweep is complete when all owner visits return, including locally limited queries; a global interruption prevents the update. `all_queries_complete` controls the feasible fixed-point termination. This was disclosed to root before tests/outcomes. Exact tree deduplication occurs before evaluation within each layer. A contact-complete state count is distinct from `certified_proposals`; only the latter has finished pruning and certification.

API metadata: algorithm `whole-incidence-trees`, version `B022`; queries expose `selected`, full `edges`, `scope='all_incident_bindings'`, `root_pools`, `routes`, `layers`, certified `proposals`, `eligible_proposals`, `discarded_eligible`, actual acceptance and shared work/time offsets. Proposal arrays contain exact scores, while route arrays retain completed path receipts and partial interruption work. Root should freeze this code and the fixed [18-call protocol](b_022_screen_protocol.md) before its only full screen. The continuation threshold remains seven valid results and three common-success Q wins/ties; local success alone is insufficient.
