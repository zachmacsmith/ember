# B029 complete-constructor screen

Before descriptor/wrapper code and before execution, 2026-09-10. This freezes
the next bounded question after the [saved-map result](b_029_saved_map_results.md).
The implemented [algorithm plan](b_029_union_construction_plan.md) is unchanged.

**Hypothesis.** Actual trimmed-chain union scoring plus local root movement can
reduce overlap often enough to yield complete minors across source structures,
while array retention and inexpensive additional trials amortize charged cold
cost. The diagnosed target is proposal ranking and repeated materialization
cost. Fixed-anchor and B028 controls distinguish root refinement from compact
evaluation/array retention. Continued failure despite more visits and lower
local overlap rejects this fixed policy; it does not justify another sequence
of local repairs on intermediate scores alone.

Pseudocode remains: start empty; construct and update one embedding state;
remove one owner and trim its neighbors; route once; evaluate the actual
attached/trimmed owner at the anchor; descend over strict-improving adjacent
hardware roots with complete labels; materialize and certify the selected
proposal; retain the existing global acceptance and congestion update rules;
return only a timely recertified disjoint minor. The fixed-anchor ablation
executes the same code with `refine_roots=False`. A named wrapper forwards the
unchanged source/target/timeout/deadline/seed arguments and this single constant
to `contact_embed`; it performs no work before that call. No engine or existing
algorithm code changes. Existing focused checks already exercise the fixed-root
branch and selected-state agreement; only wrapper forwarding is a new risk.

**Exact panel and arms.** Use ideal Z12 and Transfer003, panel SHA256
`83e16a8b3144f9e81ab4811b4bfe5fb2e869ddec49b11570f450572d72599dec`.
The nine graph IDs in declared order are g0001, g0005, g0013, g0017, g0101,
g0102, g0201, g0202 and g0203: ER80, SBM80, grid128, hidden singleton80,
ER100, BA100, WS100, SBM100 and planar100. Retain all six B028 exposures,
including successful SBM80/grid128; the three Transfer003 structures are new to
the B constructor sequence but have been exposed by other tracks. This is nine
structures, two ER and two SBM sizes, no relabeling and no confirmation set.

Run these six separate arms at seed 0 and 60 s each: `union-cost-mobile-tree`
(B029), `union-cost-fixed-anchor` (its ablation), `compiled-mobile-tree` (B028),
`native-branched-path` (retained A061), `native-interleaved-boundary` (unpromoted
A064), and pinned `mm`. Exactly 54 fresh isolated workers; no output combination,
independent starts within a candidate, saved-map initialization or witness
access. Pair all timing on hyde02 with the existing native/MM environments
`4e1fb892db12754e`, sequential supervised workers and recorded host load. Root
freezes the source snapshot, descriptors and task vector through the existing
harness before its single launch. Nothing may change in a running experiment.

Every import, JIT compilation, packing step, trial, failure and finalization
stays charged. The existing one-second finalization reserve leaves 59 s search
at this allowance. There is no pass, query, root-count or work cap; the 256-unit
clock polling and first-sixteen/last-visit detailed traces are bookkeeping.
The existing exact quality-state recurrence stop remains active because it
ends a repeated deterministic state cycle; record its stop and cost. No
unexplained 3× or other universal multiplier gates this experiment.

**Self-critique.** The packet starts from saved full overlapping states and
cannot establish sound choices during initialization. Overlap priority already
lengthens two chains sets; it may suppress useful priced-congestion transfers
or leave poor ACL after feasibility. Cold overhead remains substantial. A local
root optimum can miss better roots, and incomplete route labels can hide some
adjacent choices. Root refinement and compiled array retention change two
complementary mechanisms together; the fixed-anchor arm is essential for
attribution. Greater search exposure is not itself a success.

**Cheap complete falsifier and decision.** Broaden B029 only if it retains both
paired B028 successes and recovers success over paired B028 in at least two
source families, including a Transfer003 structure. Preserve every failure and
Q/ACL/runtime/variability regression against every comparator. Inspect actual
Q and cold time against A061/A064/MM before further allocation; this exploratory
continuation rule is not promotion or an all-class claim. If fixed-anchor alone
recovers the successes, do not credit root movement. If both remain invalid
despite greater exposure, stop this policy and identify an acceptance or
representation experiment supported by the recorded failure. If fresh valid
outputs occur but high ACL persists, use their complete structures to identify
the cause before modifying the algorithm. No broader run follows solely from
local O/Phi gains or higher visit counts.

Reuse the original-graph audit and original minor oracle without changed credit
rules. Report success, Q/ACL, within-embedding chain variance, first-valid time,
solver wall/CPU, process wall/CPU, root/descent/compilation and routing cost,
completed sweeps and every terminal/censored status separately by input/arm.
Seed variance remains unestimated. Detailed visit traces are sampled; aggregate
counters cover every visit. Do not sum overlapping timing stages or treat an
overlapping partial state as a valid minor. A060-series dense-graph gain cannot
hide a B029 regression. This screen leaves the final all-class objective open.
