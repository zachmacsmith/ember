# Atomic growth implementation and bounded gate

Root approved the [one-neighborhood proposal](c_atomic_growth_constructor_proposal.md) after reviewing the elementary failure and explicit completion certificate. The separate `atomic_regions.py:atomic_embed` loads the exact elementary source into a private namespace. Only its addition proposer and multi-site commit support are replaced; the entry, search, acceptance, other three moves and deletion cleanup definitions are reused unchanged. The original elementary module and its failed call artifacts remain intact.

The addition uses the same frontier shuffle and full-target distance ordering, then one BFS over unused sites. It stages the entire owner-region union and exact changes to physical contact multiplicities. Multiple couplers to one source neighbor contribute only one newly realized source edge. A pre-publication deadline check precedes complete ownership/contact/version updates; no cooperative interruption occurs inside publication. The unchanged search records committed work before its following deadline check. BFS/scoring prefixes remain diagnostic on interruption. Wrapper setup and final metadata fit the same absolute deadline.

The two authorized new checks passed once under the native interpreter: a multi-site addition that realizes three distinct contacts, including two physical couplers to one neighbor, with cache refresh; and interruption during BFS, score construction and immediately before publication, retaining the unchanged live assignment. No full constructor was called by these checks. The four original tiny input records, seed 0 and five-second caps are frozen separately before calls. Their runner also compares the triangle's initialized state and first two visits with the saved elementary call, then records the first differing addition and independently replays every committed state. This is a mechanism check, not a timing claim or a panel launch.
