# A066 remaining-transfer reader preparation

2026-09-10. Prepare only; no running outcomes are read. Reuse the corrected,
first-passing A066 reader and its unchanged generated interpreter on the frozen
17-encoding/68-call screen. Root owns lifecycle, terminal retrieval and the common
original-graph audit.

Change only screen/manifest/adapter/preparation identities, cardinalities,
method/comparison selection and the declared summary criterion. Preserve all
pair receipts, exact-obstruction, cache invalidation, pre-Search unknown-state and
outer-censoring checks. There is no pruning-only arm here: compare the coordinated
A066 package against A065, with independent A061/MM calls. Do not attribute a
package gain solely to pair search on this broader panel.

Retain all 68 calls and failures/times. Continuation requires no lost A065 success
or Q regression and Q gains in at least two source families outside the initial
grid/WS/planar gain families. Resolve nested parent aliases for distinct structure
reporting; family/graph labels belong only to analysis, never candidate decisions.
No new validator, constructor, candidate module import, algorithm refinement or
confirmation claim. Freeze the exact adaptation and inspect its source/AST diff
before any execution; unchanged receipt checks do not require another test run.
