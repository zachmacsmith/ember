# B028 small complete-constructor screen, before calls

Prepared after the [paired packet result](b_028_packet_results.md); **root owns final freezing and launch**. The exact six-query routing equivalence and measured 3.819-times postcompilation routing/adaptation reduction justify testing complete construction. The cold packet improves only 1.060 times and contains three query-level regressions, all preserved. No additional implementation refinement is proposed before this screen.

**Hypothesis and alternatives.** B027's expensive exact routing starves initialization and congestion-price updates. The unchanged faster search should complete more feasibility sweeps and recover valid embeddings within 60 s. If sufficient additional exposure still leaves overlap or poor ACL, computation is not the sole cause; the existing root/proposal objective or move/acceptance mechanism needs redesign. This experiment does not independently distinguish those remaining explanations. It also tests whether cold JIT and per-query array adaptation negate useful work on easy/grid inputs.

Freeze the following six input encodings, in this order, from `results/codex/transfer-cycle-002/panel.json` (SHA `d4654e1bfa8bc0451d3f87020b2c6b28b4f260c24e0f8c95bd7fe0f4df250cca`). The panel binds each raw graph and independently relabeled original:

| Key | Source | n | m | Purpose |
|---|---|---:|---:|---|
| g0001 | ER 80 | 80 | 323 | Prior failure without one full feasibility sweep |
| g0005 | SBM 80 | 80 | 195 | Prior failure despite four sweeps |
| g0013 | Grid 128 | 128 | 232 | Prior B027 success and inexpensive-route regression risk |
| g0017 | Hidden singleton control 80 | 80 | 488 | Known feasible at ACL 1; witness stays evaluator-only |
| g0101 | Fresh ER 100, p=0.08 | 100 | 378 | New instance and intermediate size |
| g0102 | Fresh BA 100, m=3 | 100 | 291 | New structure with heterogeneous source degree |

Evaluate **B028, B027, A061 and MM separately**, seed 0, configuration `{}`, ideal Z12, 60 s per fresh worker, on **hyde02**: 24 calls total. Use audited pilot method ordering/shuffling and independent original-graph validation. Exact descriptors are `compiled-mobile-tree`, `mobile-tree-recurrence`, `native-branched-path`, `mm`. B028 kernel SHA is `c729ccd815262ec9ebf18eaa24f33544719c53ca610ecd7d823fbc63456564c8`; wrapper SHA `16ec9260a0971acd81a79cb4d8577fbbaf34653d784f6d21ddd488ab724775fe`. B027 remains SHA `b6f7f9dc1cb155cbe9a764ce249fa210bb6dea72e43aac886e9916aa584b7cf8`. Root's source snapshot and task vector must bind these identities before staging.

All candidates start empty and receive only source graph, target graph, seed and deadline. The prior packet's candidate-state maps and hidden witnesses are excluded from every constructor bundle/input. Native dependency isolation remains in force; only the separate MM arm can call MM. There is no busclique call, source-family dispatch or choice among independently constructed outputs. No omitted warmup, JIT caching or allowance change is introduced; compile/import/graph copies/packing/conversion are charged as in the B028 policy. Keep 60 s as this experiment's allocation, not an unexplained universal runtime gate.

Record every attempt's terminal status, solver wall/CPU, complete process time, original validation and Q/ACL, plus within-chain variance where valid. Failure ACL stays undefined. Across-seed ACL variance remains unestimated. Compare fresh and reused inputs separately; keep per-input A061/MM gaps visible rather than pooling dense gains over losses. Inherited trajectory receipts should distinguish incomplete initialization, completed initialization with unresolved overlap, overlap resolution, first-valid time/Q, quality changes and exact recurrence. Report price updates, complete feasibility sweeps, the fraction of time in each routing/packing/compilation stage and every interruption; these observations are not independent trajectory certificates.

**Decision after all 24 outcomes.** A broader B screen requires recovered original-validated successes over paired B027 on at least two source families, including a newly generated instance, while retaining the paired B027 grid success. Actual Q/ACL, A061/MM success gaps, cold runtime and the hidden-control outcome decide whether further generalization testing is justified. Any regression remains explicit and prevents promotion; a mechanism follow-up needs a specific new explanation supported by this completed screen. Success confined to reused examples, or only increased sweeps/labels without valid outputs, is insufficient. If routing acceleration itself collapses after complete-call overhead, classify the implementation-cost hypothesis as unsupported and stop this allocation. If exposure increases substantially but failures persist, stop the fixed acceleration-only constructor and identify what search change would distinguish the remaining causes before another implementation.

**Self-critique.** Six structures and one seed cannot establish class-level superiority or ACL variability. ER appears twice, and the grid/control cases were chosen for known failure mechanisms, so aggregate means are especially easy to misread. The two new development instances can already be evaluated by other independent tracks; none belongs to an untouched confirmation set. Full outputs must be interpreted per input, then a promising mechanism must face broader development sizes, relabelings/seeds and separate confirmation. This small screen does not alter the final all-class objective or promote B028 on local-query speed alone.
