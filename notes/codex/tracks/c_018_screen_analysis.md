# C018 passive screen analysis preparation

2026-09-10 UTC. Preparation only: no constructor, test, result reader or archive audit has been executed. Root owns registration, identity freeze, remote lifecycle and all analysis execution. The fixed screen is `c018-territory-relocation-transfer` on hyde03: Transfer004 g0013/g0302/g0305/g0307/g0309, seeds 0 and 1, 60 s cold calls, methods `territory-relocation`, `territory-old-roots`, `native-branched-path` (retained A061), and `mm`. Forty calls represent five encodings of four original structures; the BA relabel is not an independent structure.

The existing original-label analyzer and pure-stdlib original-minor oracle remain the correctness basis. The pinned analyzer assumes seed 0 throughout; the isolated two-seed adapter adds seed to task identity, row keys, paired comparisons, relabel comparisons and complete expected-task coverage. Input roots change to Transfer004. Source, target, private original-label mappings, interpreter identities, cold deadlines, controller finalization and error/credit checks retain the original rules. Root reviewed the original and exact diff and approved the adapter at `results/codex/three-track-20260910/analyze_multiseed.py`, SHA `a4c16e83ff90210d5ac9454010723ce0f6301c05fd4fef3eaa92c276855f7540`, for reuse by B030 and C018. Its exact diff is alongside it; no shared validator changes. The actual exhaustive expected task grid checks the new seed-collision risk, so no additional synthetic suite is required.

After authoritative terminal verification and one archive retrieval, first require the original-label audit to pass. Every attempt stays in the denominator. Unknown times remain unknown, errors remain errors, and late or merely diagnostic embeddings earn no credit. Root's frozen source/manifest/screen hashes must be supplied externally before audit; the current preparation does not guess them. No candidate module is imported by any passive reader. No known-embedding witness may become an algorithm input.

Report the complete outcomes before intermediate statistics:

1. For every graph/seed/method: status, independent credit, Q/ACL, within-chain variance, solver wall/CPU, process wall, memory and any missing time field. Retain all ten full-C018 rows, all regressions, and every control/reference failure.
2. For each encoding and method: 0/1/2 successes; both observed ACL values; mean ACL and sample variance only with the exact successful-seed support shown. Across-seed ACL variability and within-chain length variance remain separate. With fewer than two successes, sample variance is undefined. These two seeds provide descriptive exploration, not a reliable variance claim.
3. For each matched graph/seed, compare full C018 separately with old roots, A061 and MM. Compute Q/ACL differences only for two credited outputs. Report success gain/loss separately. Show all-call time totals and coverage, and matched same-host solver/process ratios; a failed call is never a zero-time observation. No universal 3× gate or aggregate dense-graph score is introduced.
4. Relabel sensitivity: pair g0309 with its g0302 parent separately for each seed and method. Keep the relabel in row-wise regression checks, but count its topology once when judging cross-structure gains.

The passive mechanism projection should retain first-valid and every strict-Q event, completed/interrupted sweeps and visits, terminal Q/missing contacts, all observed stage wall/CPU and diagnostic work counters. A saved event is the constructor's timely-validator receipt; without a saved historical map it is not a new independent reconstruction of that earlier embedding. Final returned maps receive the existing original audit. Complete terminal maps without certification remain diagnostic only. Partial maps may be checked against their realized source-contact subgraph using the same oracle, with actual omitted original edges reported explicitly; they must not be labeled complete minors.

The prepared reader is `results/codex/c018-territories/read_mechanism.py`. It binds the reviewed common audit and rechecks its input/source bindings, preserves all 40 rows/10 comparisons/8 relabel comparisons, and projects 20 C-arm receipt sets. It reports outer and constructor statuses separately, flags missing diagnostics, and counts observed/missing visit wall and CPU fields; absent receipts after an outer kill are not evidence of zero mechanism use. Root read the reader against the frozen constructor's normalization, snapshots, events and visits. Its positive gap-closure calculation uses matched Q values and an exact integer one-third comparison. It emits observations for the declared decision, not automatic continuation.

Frozen screen identity is supplied in `analysis_identity.json`: screen `ccb538b5cb6fde182193056e876408a530819420d53f9df66cf057b56a7c8f25`, manifest `f8d41179ed29eff5b2f351ac5a169982ce59e4df766dc2ab4bac9e02d1ab3da0`, source snapshot `1b123236393082641750d6f8e28fa83a182c4de96f64f840e1d8a837b652aa6e`. This preparation has read only those identity artifacts, not live results. After terminal retrieval and audit PASS, root may run the reader with arguments `analysis_identity.json archive audit001/output mechanism001/output`, using the existing recorded local-action wrapper. No analysis or test was executed during preparation.

Distinguishing observations are fixed before looking at results:

| Explanation | Evidence to extract; interpretation limit |
| --- | --- |
| Root movement / neighborhood | Completed and accepted roots outside old territories; full versus old-root complete outcomes at the same input/seed. Diverging trajectories are expected. A full-arm win supports this root-domain change in the coherent policy, not a proof that every old-root history fails. |
| Proposal versus acceptance | Completed positive/zero/negative ΔE proposals, acceptance draws and rates, actual ΔQ and contacts broken/gained. Distinguish no useful proposal from a rejected useful proposal using recorded proposed Q/contact changes; those local changes alone do not establish eventual usefulness. |
| Geometric score versus free access | Recorded free-endpoint obstructions, actual path energy/length, number of newly contacted incident neighbors and sampled root proxy versus its minimum. Small geometric gaps with no free endpoint expose occupancy hidden by the score. They do not prove infeasibility of the representation or of all tree neighborhoods. |
| Computational cost | Import/JIT/setup, distance refresh, root scoring, regrowth, validation, visit/sweep completion and rejected/interrupted work. Few visits before censoring expose cost; many relocations with persistent defects expose ineffective search. No counter becomes a new work cap. |
| Constructor quality | First-valid Q/time, final Q/time, strict-Q trajectory and time after the final improvement. Poor first-valid Q on every valid case rejects the claimed compact construction benefit, even if later cleanup improves it. |

Apply the already declared exploratory continuation rule without redefining it after results: full C018 must succeed on all ten rows, regress against A061 on none, close at least one-third of a **positive** A061-to-MM Q gap on two distinct original structures at matched input/seed, and beat the old-root ablation in complete outcome on at least one of those structures. Show both seeds for every qualifying structure; a gain on one seed cannot hide its mate's regression. Optimal ACL-1 ties are allowed. A zero/negative reference gap has no gap-closure denominator. The numerical result informs a research decision; it is not class-level promotion.

The central falsifier remains failure on the known-feasible control, persistent invalidity on two original structures after contact-breaking moves, or uniformly worse Q than A061 on valid outputs. If the mechanism was barely exercised, distinguish an acceptance/cost limitation rather than claiming the whole representation disproved. No continuation follows from improved missing-edge totals, distance scores, experiment counts or a local diagnostic alone. Preserve prior C003–C017 rejections. The final milestone should name the shared deficit tested, complete-constructor evidence, relabel transfer, and a reason to continue, redesign or stop.
