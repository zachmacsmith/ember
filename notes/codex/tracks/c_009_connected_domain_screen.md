# C009: fixed complete-constructor domain screen

The [implementation](c_009_connected_domain_implementation.md) passed only its three focused primitive groups. Freeze **sixteen separate calls**, candidate `connected-domains` and fresh stock MM, on the same eight C development inputs: path2030, tree5058, grid1584, planar31536, ER6450, regular14334, complete1041 and SBM30736. Preserve the exact original/source-map/target bytes already used by C008. Seed is zero and timeout is fifteen seconds for both methods, sequentially on hyde03 using the existing `4e1fb892db12754e` native/MM Python 3.10.12 environments. Within each input, start from `['mm','connected-domains']` and shuffle with `Random(f'{graph}:0')` before outcomes.

The candidate is one domain process, without another constructor, MM/busclique, exact optimizer, family dispatch or fallback. The six epochs/eight updates, eight domains, four roots, penalty schedule, twenty-million cumulative cap and final reserve remain fixed by the pre-code policy. Initial domains are generated anew from graph structure/seed; no diagnostic witness or historical embedding seeds the constructor.

**Continue only if all three conditions hold:** at least **6/8 timely original-valid** outputs; Q no larger than fresh MM on at least **three common timely inputs**, with optimal ACL1 ties allowed; and the **median solver-time ratio at most three** over every common timely pair. Missing/late/invalid outputs receive no Q credit. No common-pair median is a failure to establish that requirement. Failure retires this fixed policy; soft-contact or partial-map improvements alone do not justify tuning it.

The minimal saved-data analysis will verify frozen identities, all sixteen outcomes against original labels/edges, and actual Q/ACL, solver/process time, late maps and stop reasons. For each candidate preserve completed epochs/updates, probability/domain shapes, root/generation interruptions, selected-map M/overlap, first feasible/final Q and work/stage totals. Explain incomplete domain generation as cost censoring; pairwise support cannot distinguish globally inconsistent domains from coordination failure. All timing comparisons remain within this same-host run. This is repeated development, not held-out confirmation; one seed establishes neither family means nor ACL variance.

Root owns registry review, source freeze and all cluster actions. The [exact initializer argv and plan](../../../results/codex/track-c-009-protocol/initialize_invocation.json) are prepared, not executed. No additional local constructor calls or test expansion are part of this preparation.
