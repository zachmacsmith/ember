# B022: fixed B9 whole-incidence constructor gate

Pre-observation protocol. Exactly 18 calls on the unchanged nine B9 source graphs and ideal-Z12 target bytes from `results/codex/track-b-contacts-020/`: complete_40, complete_100, bipartite_30_30, random_er_80_d8, regular_80_d3, watts_strogatz_80, grid_8x8, honeycomb_5x5, king_8x8. These names identify inputs; no name or class is supplied as algorithm logic.

For each source, call `whole-incidence-tree` and fresh stock `mm` once, seed 0, timeout **20 seconds**, config `{}`. Start with that method order, then apply the existing pilot's `Random(f'{name}:0').shuffle` independently within each source. Freeze all 18 identities before observations. B022 keeps 20M global/19M search units, 100k/five-second nested queries, four roots per owner, beam four, at most three path alternatives, eight owner sweeps, and a one-second final reserve at this timeout. The exact policy is [b_022_whole_incidence_policy.md](b_022_whole_incidence_policy.md).

Run serial fresh processes on hyde02, one thread and separate pinned native/MM environments, using the existing pilot/controller and original-input validator. Root owns source freeze, registration, staging, transport and launch. `task_metadata.json` is preparation-only until root binds the source snapshot and creates tasks; it launches nothing. No additional local public-constructor invocation precedes the screen.

Credit only an independently valid, timely `SUCCESS`, with Q recomputed from original-label chains. Retain all other statuses, original outputs, process/solver wall and CPU, host load, dependency imports, work counts and deadline overruns. Failed partial Q/O is diagnostic only. Report initial/final Q/O/E and price epochs; energy is comparable only at identical prices. Report root/route/contact/certificate work, completed and limited queries, certified eligible proposals, actual commits, and eligible proposals discarded by query or outer-publication interruption. Such losses indicate a cost/publication restriction, not neighborhood infeasibility.

The fixed continuation threshold is **at least seven of nine valid timely B022 embeddings**, plus **Q no larger than fresh MM on at least three common-success inputs**. Optimal ACL1 ties count. If either condition fails, retire this fixed policy without changes to roots, routes, beam, cap or order based on intermediate results. A pass authorizes considering further complete-constructor measurement, not promotion; runtime remains a separate practical concern. B019/B020 source and their 60-second outcomes remain intact. This shorter contemporaneous 20-second screen makes no historical timing comparison and is exposed development evidence, not held-out validation.

One saved-data screen after terminal retrieval will preserve all 18 statuses and per-input quality/time comparisons. Reuse the original-label/minor oracle and the existing B9 accounting pattern, adapting only B022's declared query schema. No exhaustive trajectory replay or new solver call is needed to interpret the threshold.
