# C011: fixed work-limit versus wall-limit screen

Use all eight unchanged C development inputs and ideal Z12, seed0, fifteen seconds on hyde03. Run fresh methods in the declared list **`[adaptive-clones, wall-adaptive-clones, mm]`**, shuffled per input by the audited `Random(f'{graph}:0')` rule: **24 separate calls**, same native/MM environments and original validation. Root owns descriptor registration, source freeze and lifecycle. No run is initialized by this note.

The [comparison plan](../../../results/codex/track-c-011-protocol/comparison_plan.json) binds the exact reused C010 selection, graph/target bytes, labels, evaluator originals and task templates. Retired C010 remains the unchanged nineteen/twenty-million control; C011 keeps every placement choice and charge but allows the existing absolute wall deadline to stop search. Reference crossing events are not active limits. Preserve failures, missing/late records, partial clone assignments and all solver/CPU/process costs. There is no fallback or per-input selection of returned embeddings.

The narrow allocation question is whether treatment completes any additional timely original-valid input that fresh control does not. Report those gained and lost input IDs, overall coverage, Q and elapsed costs. No new timely completion rejects unused-runtime recovery on this panel; partial placements, more splits or fewer missing edges are insufficient. Compare only fully recorded decisions and charges in the common prefix; no unsaved state equality or reconstructed trajectory is claimed. Unexpected prefix disagreement makes the controlled interpretation uncertain.

Continuation separately requires **>=6/8 timely valid treatment outputs**, **>=3 Q wins/ties against common timely MM** (optimal ACL=1 ties allowed), and **median solver-time ratio over all treatment/MM common timely pairs<=3**. Report every condition, including undefined comparisons, and do not promote the retired control. Additional completions alone support only the narrow allocation claim. Neither arm changes its 0.5-second final reserve or fifteen-second deadline; late validation/output cannot become quality credit.

This one-seed reused-development screen is not a family-mean, variance, generalization or novelty result. No constructor rerun, further allowance/placement adjustment or subsequent screen follows automatically.
