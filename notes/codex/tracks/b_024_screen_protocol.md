# B024 fixed placement comparison

Predeclared before corpus calls. Compare `joint-placement-tree` (B024), unchanged `temporary-contact-tree` (B023), and fresh stock `mm` on exactly the existing B9 inputs: complete40, complete100, bipartite30×30, ER80/d8, regular80/d3, Watts–Strogatz80, grid8×8, honeycomb5×5 and king8×8, with the same ideal Z12 target. All are exposed development inputs. Use seed0, twenty seconds and empty configuration: **27 calls on hyde02**, through the existing isolated native/MM environments and original-graph validator. The candidate has the unchanged 20M total work allowance, 19M search allowance and final reserve; placement shares these limits.

The exact ordered task vector and canonical graph/target digests are in `results/codex/track-b024-joint-placement/task_metadata.json`. Preserve B9 source order and shuffle the initial method vector `[joint-placement-tree, temporary-contact-tree, mm]` using `Random(f'{name}:0')` independently per input. Raw input-file hashes remain separately recorded. Root owns the descriptor, immutable source snapshot, freeze, detached launch and terminal retrieval; this preparation does not launch anything. No restart, repeated seed, partial-prefix selection or post-result parameter change.

The fixed continuation gate is **at least 7/9 valid B024 outputs and Q no greater than MM on at least three common successes**, counting optimal ACL1 ties. Report all failures and all-attempt solver/CPU/process time, with same-host timing comparisons qualified by load. Historical B023 times are not pooled. Independently validate the original logical edges, connectivity/disjointness, and Q; late or failed output receives no credit.

Compare the contemporaneous B024/B023 initial footprint, source/target/edge orders, initial and final M/O/Q/F, first-feasible Q, routing/erasure work and final quality. B024 additionally records entry/proposed singleton maps, exact initial/final distance sum, accepted exchanges, completed roots/passes, work and wall by preparation stage. Recompute the published singleton contacts. Preserve partial preparation receipts; interrupted preparation cannot run construction or credit its retained original map.

Lower distance followed by lower final Q on several sparse inputs supports the placement hypothesis. Lower distance without better complete quality rejects this proxy/footprint/repair combination. No swaps leaves the chosen neighborhood or fixed footprint unresolved. Preparation exhaustion is a cost failure; identical complete-source assignment with extra preparation is a prespecified negative control. Any downstream divergence under binding limits remains part of this complete scoring-and-cost policy comparison. Nothing here proves distance is an ACL surrogate or separates every effect of later forced acceptance. No numerical refinement follows an intermediate gain.
