# B024 exact pre-code policy

2026-09-08. This implements the accepted [controlled placement memo](b_024_joint_placement_memo.md), before code or observations. It makes no novelty claim. Freeze B023's source `7fd89929…`; copy it into a new standalone module, adding only this placement stage and B024 diagnostic identity. Original routing, erasure, pricing, scoring, search, cleanup and finalization functions must remain text-identical. No other constructor is imported or called.

**Initialization and metric.** Run B023's exact normalization and BFS singleton initialization. Record the source-ID-ordered bijection pi and the set S of occupied sites; require exactly one distinct site per source vertex. Preserve that set throughout placement. Process roots in seeded target-rank order. For each root, BFS the full target in B023's adjacency order, recording distances when a site is first discovered. Stop immediately when all sites in S have been discovered; every recorded distance is then exact. A disconnected/unreachable case is an explicit error, not an approximate distance. Store an n-by-n private distance table and compute `C=sum_(u,v in E(G)) D(pi[u],pi[v])` with integer arithmetic.

**Fixed optimization.** Make exactly two passes, each visiting every unordered pair in increasing source-ID order. The delta for exchanging pi[u] and pi[v] is the sum over `w in N(u)-{v}` of `D(pi[v],pi[w])-D(pi[u],pi[w])`, plus the corresponding reversed term over `N(v)-{u}`. The edge (u,v) is invariant; shared-neighbor contributions cancel without being omitted incorrectly. Visit neighbors in source-ID order. Immediately apply a private exchange iff delta<0; zero ties retain the current assignment. Maintain the exact cost by the delta, then independently recompute the complete edge-sum at the end and require equality. No early pass termination, new random draw, alternative start or partial-prefix selection.

**Work and deadlines.** Use B023's existing cumulative meter: all normalization, placement and construction share the same 19M search units and twenty-second common allowance with its unchanged final reserve. There is no placement-only reset or uncharged setup. Charge each copied source/site/table record, BFS queue removal, examined adjacency, pair visit, incident-edge visit, distance-table lookup and materialized output record. Scalar bookkeeping and sorting CPU remain in wall time as in B023. Distance-table lookup is explicitly a charged operation in cost/delta evaluation. Private permutation updates require no global owner-map copy per swap. All timers use the same absolute deadline; explicit clock checks surround BFS roots, complete passes and final publication, in addition to the inherited 64-unit polling. Record all partially completed work on interruption.

**Atomic publication and no fallback.** The live constructor retains the original BFS state until both passes, the final cost check, contact/ownership recount and diagnostic materialization finish. Build the proposed state using B023's unchanged `install` on a private shallow context sharing immutable source/target data; its fresh state never mutates the live chains or maps. Check the fixed site set, Q=n and O=0, then publish with one state-pointer change after the last timely clock check. An interrupted/error preparation never runs routing/erasure or credits the original BFS map as a treatment output. Set an explicit incomplete-placement error before beginning preparation and clear it only after the complete publication returns; B023's unchanged final error guard then preserves diagnostic state without success credit. No new physical embedding is selected from preparation prefixes.

**Diagnostics.** Preserve entry/proposed source-site maps, target-rank-ordered S, initial/final integer C, actual missing-contact counts before/after, visited pairs, completed BFS roots/passes, all accepted exchanges with signed delta and cost, and preparation work/wall. Interrupted rows retain the active root/pair/pass, stage and best private cost as diagnostic evidence only. Counts for source/target distance searches and scored swaps stay distinguishable. On success, B023's normal `initial_state` is the newly published singleton map; its original `initialization_orders` and the placement entry map preserve the BFS comparison. Record the larger initialization cost including placement. The ordinary move list still begins with construction, and its behavior remains unmodified apart from the input placement and remaining shared allowance.

**Targeted checks only.** The fixed scrambled four-vertex path must reach C=3 from C=5 while keeping exactly its four sites and correctly recounting contacts. A complete source must have zero accepted exchanges and unchanged cost under every permutation. An outside-root path fixture checks that distances use the full target, not the induced root graph. Inject interruption after a completed private exchange and during recount/publication; require live-state/nonmutation preservation and no final success credit. An exact source comparison must bind all unchanged B023 routines. No public constructor or corpus runs locally, and no broad oracle suite.

The risk remains unchanged: distance ignores competition and same-owner path sharing, pair descent may miss useful longer permutations, and the fixed initial site set or later forced repairs may dominate. The fixed next screen is all B9 sources, methods B024/B023/fresh MM, seed0, twenty seconds: 27 calls with the existing >=7/9 validity and >=3 common MM Q-win/tie threshold. Root owns registration, freezing and launch. No cap, price, pass or routing adjustment follows an intermediate cost gain.
