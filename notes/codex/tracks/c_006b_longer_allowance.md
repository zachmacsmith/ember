# C006b: unchanged C006 with a uniform 60-second allowance

Hypothesis: C006's distinct-neighbor initialization leaves useful continued convergence beyond the 15-second screen. Its complete and regular inputs ended with 146 and 337 missing edges, respectively. A longer trajectory may complete one of the four harder inputs; it may instead plateau. This is a time-budget experiment on the same algorithm, not a new mechanism.

Freeze the exact C006 source bundle, canonical inputs, target, method configurations, seed 0 and paired method order. Change only every task's allowance from 15 to 60 seconds, recomputing its identity in a fresh run. Run all eight inputs on hyde04 with fresh separate MM and candidate processes, using the existing isolated environments and audited detached controller. No independent restarts, alternate policies or per-family budgets. The candidate retains its existing half-second final-validation reserve and early-success return.

Pseudocode: for each frozen graph and its frozen method order, invoke that single method once with seed 0 and timeout 60; preserve every raw/finalized output; independently validate against the original labeled source and complete target; compare timely paired quality and same-run solver/process costs. Compare C006 history only for algorithmic state/work consistency, with no pooled runtime estimates.

Falsifier: if none of ER, regular, complete or SBM obtains a timely valid embedding, reject increasing the allowance to 60 seconds as a sufficient remedy. Any new success justifies examining its quality and cost; it does not establish MM superiority. Early-success embeddings are expected to match C006 exactly. Compare initial maps, complete saved improvement trajectories and counters, and final/best missing counts. Recount every partial partition's original-target connectivity, disjointness and missing contacts, explicitly requiring its total Q to equal the unchanged active target allocation. Keep late valid outputs diagnostic only.

Self-critique: this extension was chosen after inspecting repeated development outcomes. It provides no holdout evidence or family means/variance, and a 60-second failure cannot prove permanent stagnation. Deterministic trajectory prefixes may differ in length because stopping is wall-clock based; compare overlap by visit count, not elapsed time. Extra time cannot repair the known sparse quality losses if those runs return the same early successes. No novelty claim follows from a longer allowance.

Reuse C006's accepted targeted checks and original-label validator. No constructor, MM or initialization replay is needed for preparation; copy only immutable input bytes into a new identity. The saved-data auditor will be frozen before any C006b result read, with a small timeout binding and explicit partial-Q assertion. Full costs and provenance will remain in result artifacts; report every input.
