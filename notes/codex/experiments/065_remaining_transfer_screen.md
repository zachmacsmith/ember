# A065 transfer to the remaining development inputs

2026-09-10, before execution. The [nine-encoding complete screen](065_constructor_results.md) passes its frozen exploratory continuation criterion: three Q improvements over A064 on BA160 and new WS100/SBM100, six ties, all nine successes retained. A065 still loses seven of eight common MM comparisons and spends about 59 seconds per call. This next experiment measures the unchanged policy on the exact complement of its tested Transfer001 encodings. It introduces no algorithm refinement or new work/time limit.

Hypothesis: cheaper exact root ranking exposes useful strict-Q moves on additional source structures and sizes while preserving complete construction. The falsifier is a complete screen, not another count of improved local queries. Evaluate every previously unmeasured Transfer001 encoding: g0002, g0003, g0004, g0005, g0007, g0009, g0010, g0011, g0012, g0014, g0015, g0018, g0019, g0020 and g0022. This is 15 encodings, 14 distinct structures with a nested BA80 relabeling. All are development inputs already exposed to predecessor algorithms; they are new to this A065 policy, not fresh or confirmation inputs. With the completed screen, they cover all 22 Transfer001 encodings and the two new Transfer003 inputs without rerunning the first seven encodings.

Freeze `065-compiled-boundary-remaining-transfer` on hyde06 with unchanged production source. Four methods separately: A065 `native-compiled-boundary`, A064 `native-interleaved-boundary`, retained A061 `native-branched-path`, pinned MM. Seed 0, 60 seconds per call, exactly 60 calls. Preserve existing isolated native/MM environments `3c7aa36653394e61`, shuffled method order, cold computation, original-graph validation and complete failure/solver/process time accounting. All within-input timing comparisons occur on the same host. No witness, saved prefix or independent candidate result initializes a constructor; no output selection is permitted.

Use the same pseudocode as the frozen A065 policy: construct one A061 state, run A064's interleaved whole-chain search with exact compiled root ordering, and retain only validated strict-Q improvements of that state. The self-critique now has complete evidence: most gained computation goes to rejected reconstructed trees. More time or faster ranking cannot be assumed to overcome a missing move or acceptance mechanism. The earlier base Q is identical but exact maps are being independently recovered; that causal diagnostic is separate and cannot change this policy or subset.

For broader exploratory continuation, require all A064 successes retained and Q improvements over A064 on at least three additional structures across at least two source families. List every Q, variance and timing regression, including MM losses. Passing this criterion makes the compiled implementation useful for later mechanism work; it does not promote a single-seed partial class record to an all-class result. Failure rejects the declared transfer expectation and requires a specific explanatory finding before further follow-up. Sparse timing deficits can motivate redesign despite this quality criterion passing. No combined aggregate may erase an individual input or family regression.

Report all 15 rows and the combined, disjoint 24-encoding development record with nested relabelings separated. Keep A061's full retained class table authoritative. These screens leave across-seed variance, additional Ember classes, confirmation and publication claims unresolved. Before another A refinement, use the measured reconstruction costs to distinguish cost from neighborhood/acceptance limits; do not optimize root ranking further solely because its kernel can be made faster.
