# A065 constructor launch and passive analysis preparation

2026-09-09. The frozen [36-call screen](065_constructor_screen.md) was staged and
launched once on hyde06 using unchanged `transfer_cluster_action.py` and
`cluster.py`. Stage001 and start001 succeeded. Start created detached tmux session
`ember-codex-c7e3ed45e963216fa27a` under the existing 3540-second supervisor.
Status001 independently observed tmux live, controller PID1470824 running,
active worker PID1471159, busy controller lock and 1/36 finalized tasks. This
records that observation, not a claim about the run's later state. No partial
result maps or quality/timing records were read. Root owns subsequent observation,
terminal retrieval and the original audit.

Two local analysis adapters are prepared **but not executed** in
`results/codex/a065-constructor`:

- `analyze.py` imports the pinned unchanged original analyzer and changes only
  public/private input directories to Transfer003. After a passing original audit,
  it adds accurate development/nested-relabeling scope metadata. SHA256
  `7aac9f33902b85bccd4a6fd1c9f15c1171faa27f991d4074c6956e440bfa1fb2`.
- `mechanism.py` binds all 36 task identities, frozen source/manifest/screen,
  passing common audit and candidate/A064 raw records. It reuses the unchanged
  A064 receipt interpreter for A064 and an in-memory AST copy for A065, changing
  **only its two diagnostic version constants**. The original source is preserved.
  SHA256 `526e76652433dcd9c21532efc9992aa404cff4d205fbe89e0c3791a8a0844b79`.

The new cost projection checks complete-ranking agreement, dispatch counts,
uncached/checked execution, and nesting of import/packing/allocation/compilation/
kernel/export costs inside observed root intervals. Interrupted and absent fields
remain unknown; payload bytes are not RSS. It imports no candidate algorithm,
replays no constructor and changes no embedding credit. A064 receives the
unchanged receipt checks. Comparisons expose inherited-base Q differences;
base-map equality is explicitly unknown here because equal Q is insufficient.

`analysis_preparation002.json` binds the current adapters and supporting readers.
The initial reader draft is preserved: before any execution or outcome read,
the only follow-up tightened the audit-adapter binding to a fixed SHA256 instead
of merely matching the audit's reported adapter hash. Both preparations remain.
Root review precedes execution. Run the unchanged original audit first after
terminal verified retrieval; only its passing output may enter this reader.
