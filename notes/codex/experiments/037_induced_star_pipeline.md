# Full-pipeline induced-star comparison

Saved before integration or any037 embedding call, on2026-09-08. This replaces
the unexecuted saved-incumbent component draft. The existing pilot supports a
complete construction/refinement comparison with frozen identities, isolated
workers, inherited controller locks and watchdogs. Use it to test the complete
cumulative effect directly. No prior matching benchmark result caused this
protocol revision; the treatment policy and numeric settings are unchanged.

## Input and treatment matrix

Use all34 distinct original017 readiness sources, the exact idealZ12 target,
solver seed0 and60 seconds per full native call. Run68 calls on one host,
sequentially, pairing the two arms by source with the pilot's existing fixed
input/seed arm-order shuffle. Frozen source must include corrected conversion.

The control is `native-search-joint1-contacts-spectral`, with legacy singleton
policy and the new star policy off. The treatment is that same configuration
with only the new matching-star policy enabled. Each call performs its own
single construction and evolves its own incumbent. Neither receives saved
embeddings, MM outputs, family labels, source IDs or competitor measurements.
No selection between methods is part of the algorithm.

Preserve1000 placement evaluations, four refinement passes,512 total ordinary
groups, sizes1–4, beam1, three alternatives, halo2, region cap512,500000 global
work,50000 per visit, two reconstruction orders,16 boundary sites, round-robin
scheduling, greedy trees and `qubits_contacts`. The matching treatment follows
the exact [implementation specification](../induced_star_implementation_spec.md):
after failed ordinary visits, once per center per pass, two or more independent
leaves, strict Q reduction,5% auxiliary share,2048 query units, charged cache
maintenance and no extra group visits. Direct singleton relocation remains off.

## Required checks before any launch

Complete the core oracle/ownership/interrupt/certificate review and the native
and scheduler integration tests. Check unknown policy and incompatible policy
combinations explicitly. The off path must retain current behavior and
diagnostics. Freeze a committed source snapshot, complete task matrix, source
and target records, and verify the isolated environment contains no forbidden
embedding dependency. Obtain an independent preflight before root launches.

## Full results and decision

Independently validate every final embedding against the original source and
target, recheck all frozen source/task/input hashes, and recompute qubits, ACL,
maximum chain, within-chain variance and actual logical-edge redundancy. Keep
failures/timeouts separate; do not credit partial or late outputs with quality.
Report all34 input pairs, including zero-gain and ineligible inputs, and count
the shared king/frustrated-square source only once in aggregate statistics.

Compare saved initialization/layout diagnostics and constructed/pruned counts
within each pair. Equality is a stage-consistency check, not a complete hidden
state trace. Replay the control against036 as a separate provenance/quality
check; do not assume that identical seeds guarantee exact floating-point states.
No source or parameter change is allowed during the run.

Count complete solver/process time, candidate setup, all ordinary and matching
work, cache events, considered centers, selection failures, root/matching work,
truncation and accepted trajectories. Reconcile reported Q/R deltas with final
original-graph counts. Show matching block sizes3–4,5–8,9–21 descriptively,
without making them runtime dispatch classes. Operator-specific savings are
already included in treatment totals and do not establish comparative gains.

Use same-run control/treatment timing. Any MM quality reference from032 or an
older experiment is explicitly historical; no timing is pooled across hosts.
One solver seed on inherited development graphs provides neither across-seed
ACL variance nor evidence of unseen-instance generalization. No novel-matching
claim follows from the documented LAD reduction.

## Self-critique

Matching may find useful strict reductions but change later opportunities or
consume global work needed by ordinary groups. Its cache can disable when an
accepted ordinary move leaves insufficient maintenance allowance. Successful
ordinary equal-Q moves suppress attempts, while a greedy independent subset
can exclude a useful smaller block. All these are part of the fixed algorithm,
and their costs or regressions must remain visible.

The singleton-center restriction cannot address every high-degree chain.
Passing this screen would justify carrying one fixed revision forward and
testing new source instances; it would not satisfy the full research goal.
If the treatment does not improve cumulative quality at acceptable cost,
retain the complete negative evidence before revising it. Do not retune by
family, source identity or favorable seed, and do not publish a best-of score.

Status: protocol only. Implementation review, integration, source freeze,
independent preflight and launch remain outstanding.

## Completed implementation gate

The specified core and scheduler were reviewed independently before source
freeze. Root passed 85 core tests, repeated 96 independent tiny-minor oracle
cases (89,280 assignments) and 4,494 interruption checks, and verified 12 exact
old-source off-path replays. Final contact/native/pilot regression passed
110 tests, including 27 new integration tests and one fixed small native
correctness smoke, with no MM/busclique import attempts. The treatment method
name is `native-search-joint1-contacts-spectral-stars`; its only configuration
difference is `polish_star_policy='matching'`.

Use hyde03's prepared native environment for both arms. The historical 036
control ran on local macOS/Python 3.10.19, whereas hyde03 uses Linux/Python
3.10.12. Its replay is therefore a cross-platform provenance/quality diagnostic:
do not require exact output equality or form timing ratios from those runs.
The within-037 comparison retains the original fixed protocol.

Status at this checkpoint: implementation and required checks are complete;
committed source freeze, independent preflight and launch remain outstanding.

## Frozen source and staging

Committed implementation: `13876c22b0576c3d41d54bffe5ec429e37f5d08c`.
Source snapshot: `5dfc63cfb5bb2a674cb3e556e13d2b4d18a1857c7a8f296ba27cc2dd5075abc9`
(47 source files). Target:
`38cde794d3c1461054a45b5a660d157737b019c19cb9a7b38e2027730e3d5938`.
The 68 tasks are frozen in `results/codex/037-induced-star-pipeline`.

The first staging attempt correctly failed before run publication because the
manifest still contained local interpreter paths. No solver attempt occurred.
The original manifest and stderr are saved in `results/codex/037-launch`.
Only `candidate_python` and `mm_python` were corrected to the prepared hyde03
interpreters under `/home/dabh/ember-codex/envs/4e1fb892db12754e/`; source,
target, graph, task and configuration bytes remain unchanged. Original manifest
SHA256: `fcfc453835ae0e5289d569416f22ef2d6438b92157bd0463c1c0afac04ff8b43`;
corrected manifest: `4adea4d37bbb419bc82c1d86505ca4bca7b38668dec87600fc57c2efd8c1001e`.

Corrected staging completed successfully to
`/home/dabh/ember-codex/runs/037-induced-star-pipeline`, transport identity
`55064e25db44189b2ddd0f77d0285f17ae4de3292656b7b55e624ceffd24e2e9`.
Independent remote preflight remains required before root starts the controller.

## Verified launch

Combined independent preflight passed; see [its review](037_preflight_review.md).
Root inspected the local audit and successful remote probe. The initial probe's
metadata-display-name error is preserved separately; the corrected check uses
`importlib.metadata.version` and changes no environment. It confirms all frozen
bytes, exact pins, no forbidden candidate packages/imports, no prior attempts,
and the prior 032 run quiescent. Empty import-created cache directories contain
no compiled files and do not supply a warmed per-task JIT cache.

Root launched at 2026-09-08 04:45:37 UTC. Controller 134553 records start
`1788842737.938521`; the detached tmux supervisor has a 6,420-second outer
watchdog. A fresh status connection confirms controller running, worker 134596,
held run lock, live tmux and three of 68 finalized SUCCESS. This is launch
verification, not a complete result or quality comparison. Raw start/status
outputs are under `results/codex/037-launch`. The independent benchmark reviewer
owns monitoring, quiescent retrieval and the complete artifact audit. Do not
restart the run after a transient observer connection failure.
