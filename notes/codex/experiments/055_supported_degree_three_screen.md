# 055: supported degree-three reduction, nine-input screen

Prospective exploratory protocol. Fixed A053 is compared with the isolated
A055 source-representation rule: degree≤2 remains eligible; degree3 requires
an edge within its current filled neighborhood. The [accepted policy](055_supported_degree_three_hypothesis.md)
and [mechanism diagnosis](055_source_gate_and_diagnosis.md) precede physical
outcomes. Source-only counts alone do not justify retaining the candidate.

All34 source-only records completed under5s/2M operations each, with consistent
old journals and valid composed minor witnesses. Eight reductions differ from
A053. Six targeted synthetic checks pass, including independent full-rescan
order, older-fill contraction, nonlocal activation and interrupted reduction
without partial-core admission. No additional reach suite or native call was
run. One synthetic adapter dispatch passed.

**Prospective scope amendment:** the user requested diverse complete outcomes
and visible regressions before any055 physical result. Include **all eight**
source-changed inputs plus the first unchanged input, rather than the original
first-six changed rule. This adds only the two previously omitted changed
inputs/four paired calls. Preserve the original selection unchanged. The
reason, UTC timestamp, prior-selection hash, new full34 ledger and summary
hash are in `results/codex/055-preparation/selection.json`. That record
supersedes only the seven-input scope in the initial policy/diagnosis.

The fixed order is `ember_22972` (unchanged), then `ember_34404`, `ember_37761`,
`ember_1584`, `ember_31357`, `ember_4083`, `ember_30736`, `ember_32367`,
`ember_33219` (changed), preserving original manifest order. Nine structures,
nine memberships,25 excluded structures. Selection uses no055 embedding
outcome and no family dispatch.

Use the audited pilot/controller on hyde03: `native-site-transfer` versus
`native-supported-degree-three`, both `{}`, seed0,60 seconds,18 serial fresh
processes. Preserve the unchanged053/042 graph, target and corpus-selection
bytes, explicit subset, per-input seeded arm ordering, dependency guard,
common deadline and failure schema. Retain at most one native core call and
all inherited transfer/repair/work limits. No MM call, fallback output,
parameter adjustment or source-family choice. Root owns immutable freeze,
stage/start/observe/fetch; failed observation never authorizes restart.

Validate every complete result against original labels/edges using the
unchanged042 oracle. Validate all original maps against the full053 archive,
then selected graph/target byte equality. Reconstruct A055 eligibility and
exact journal order, including nonlocal updates; compare to its saved source-
only record. Preserve complete/failing reductions, raw costs and partial
requirements, all statuses, Q/ACL, native-core versus lifting cost, transfer/
repair receipts, nonlocal activations and added reduction work. These work
counts overlap global scans and exclude native-internal work. Compare the
unchanged control's non-time construction receipts; added reduction counters
are expected, and any physical difference remains explicit.

The target mechanism is representation: a smaller artificial requirement
burden may retain useful simplification. Physical neighborhoods and acceptance
rules are unchanged, so this is not an acceptance-policy ablation. Changed
core/lifting excess is descriptive, not causal. A completed full constructor
must improve final quality/coverage/cost to justify further work. Any invalid
output, lost reach or unfavorable aggregate tradeoff rejects this fixed
prototype; retain every regression rather than selecting favorable inputs.
A positive selected result requires broader follow-up. Report nine-input
scope,25 exclusions and missing outcomes honestly; no34-input mean, pooled
historical timing, seed-variance, all-class or MM superiority claim follows.
