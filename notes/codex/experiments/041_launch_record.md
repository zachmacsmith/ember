# 041 endpoint-support comparison: launch record

2026-09-08 UTC. Root started the frozen comparison once on hyde03. Controller
150200 began at **09:07:16.175 UTC**. A fresh SSH observation at 09:07:17.070
confirmed that controller and worker 150212 had matching live process arguments.
The inherited lock was busy and the detached tmux session was present, with
zero of 68 results finalized. This records execution, not an ACL or speed result.

The run is `/home/dabh/ember-codex/runs/041-endpoint-support-pipeline`.
Preparation is `results/codex/041-endpoint-support-pipeline`; root lifecycle
records are under `results/codex/041-launch`. Do not repeat start after a lost
connection, observer error, partial output or unfavorable outcome. Reconnect
to inspect the same run. Retrieve after process, lock, session and supervisor
exit observations establish quiescence, preserving every result and failure.

## Frozen experiment

| Item | Identity |
| --- | --- |
| Production commit | `9d262fd4efa891308c5049d9e9375b28aa3385f8` |
| Source map, 50 files | `ecd67cbed55a96a616d1aca018eb132d39c58e828394a2b2e5caad83859ed893` |
| Manifest SHA256 | `0e8bdff10210f119599973f26e04da3554debdec5a09cba47538467939630805` |
| Transport map, 156 files | `b600e025b3d89d7696e17a11144695bc13c95c50b24e9b78208d91b3c1d45bf7` |
| Input-order SHA256 | `9b2dde6634114d8e229219098d394c334f452f21b594fc48d3b8243048db2e74` |
| Ideal Z12 record | `38cde794d3c1461054a45b5a660d157737b019c19cb9a7b38e2027730e3d5938` |

The accepted protocol is `041_endpoint_support_protocol.md`. All 34 original
development structures and 35 memberships are retained, with the king/frustrated
alias counted once in aggregate and the original Sudoku absence explicit.
There are two serial fresh-process calls per structure, seed zero, one thread,
60 seconds for the native pipeline and a 90-second process watchdog. The outer
supervisor is bounded to 6,420 seconds plus five seconds of kill grace.

Control is `native-search-joint1-contacts-spectral`; treatment is
`native-search-joint1-endpoint-support-spectral`. Their sole configuration
change is `polish_objective='qubits_endpoint_support'`, including its lazy
scoring and deadline handling. This is a complete scoring-policy comparison,
not a pure objective ablation. No MM task or new final deletion pass is present.
The separately reviewed deletion module is unused in both frozen arms.
Current workspace integration cannot affect these copied source files.

The native interpreter is
`/home/dabh/ember-codex/envs/4e1fb892db12754e/native/bin/python`, Python 3.10.12,
with the exact prepared dependencies and no MM/busclique packages. The shared
host is not reserved exclusively. No cross-host or historical MM timing claim
is planned. One seed cannot estimate across-seed variance or establish a
family-population superiority claim.

## Prelaunch checks and preserved failure

The independent preflight note has SHA256
`18c7f1bc5136bf7ff1b4e51007f55dc5fce928ea09ce695bc87d085c8c560ea5`.
Root read it and verified all 72 manifest files and 13 references. Local frozen
inputs, remote exact bytes/environment/zero attempts, and the nine-group
independent synthetic suite in the actual target environment all passed.
The six synthetic evidence files were retrieved and checked exactly; map digest
`1a6e127b22dd2241fa3a7892dac8454d101fc3c34f6787fab19caefe540e61d4`.
Those test timings are excluded from benchmark populations.

One root-local acceptance script initially resolved manifest-relative file
names against the repository root. It failed before saving acceptance; its
dependent start wrapper therefore stopped immediately before any remote call.
The incident is preserved as `root-acceptance-attempt001.json`. Resolving files
against the manifest directory and references against the repository fixed the
reader. All hashes then passed, root saved `root-prelaunch-acceptance.json`,
and the sole remote start returned `launch='started'`, exit zero. No candidate
or experiment was repeated because of that local bookkeeping failure.

The supervisor uses tmux with timeout and `KillUserProcesses=false`, independent
of the observing SSH connection. A fresh connection verified live processes
and lock/session state after launch. This tests reconnection, not an actual
Wi-Fi network switch. Outcome validation and accounting remain pending.
