# A065 complete-constructor screen — before execution

2026-09-09. The exact six-query test passes: ordered roots, immutable entry states
and BFS counts agree. Complete cold process wall is 8.615 seconds for A064 and
3.483 for A065; the first compiled query regresses, with compilation charged.
Root reviewed the new production code, wrapper diff and passive reader. This
supports the complete screen proposed in the before-code policy, not an ACL or
constructor-speed claim.

Freeze these nine Transfer003 encodings: ER80 g0001, BA160 g0008, planar80 g0006,
grid128 g0013, K100 g0016, singleton-control80 g0017, fresh WS100 g0201,
fresh SBM100 g0202, and nested grid relabeling g0021. This is eight structures
and one relabeling. Panel SHA256 is
`83e16a8b3144f9e81ab4811b4bfe5fb2e869ddec49b11570f450572d72599dec`.
No diagnostic embedding or hidden witness enters a constructor.

Run A065 (`native-compiled-boundary`), A064 (`native-interleaved-boundary`),
retained A061 (`native-branched-path`) and pinned MM separately on hyde06,
seed 0, each with a 60-second allocation: 36 calls. A061 is an added control
relative to the suggested three-method screen, declared before initialization.
It supplies a fresh retained-policy reference and allows the original audit to
remain unchanged. All four methods use the existing per-graph shuffled order,
isolated dependencies, detached supervision and complete failure/time accounting.
No warm-up, independent-output selection or work cap is introduced.

The hypothesis is computational: cheaper exact ranking permits useful later
search within the unchanged neighborhood. Compare complete success and Q/ACL
first, then charged wall/CPU/process time, time to admitted improvements, owner
coverage and final-epoch closure. Compare base maps/Q before attributing a
change to the added stage. Every MM loss and within-chain variance tradeoff
remains visible. One seed cannot estimate across-run variability.

Continuation requires complete quality gains over A064 on several structurally
different inputs including a fresh one, with no concealed lost success. More
rankings without any final Q improvement rejects the quality hypothesis; earlier
natural completion at the same quality is a separate speed result. Report
regressions individually and explain any subsequent bounded follow-up. No
aggregate dense-graph benefit, local counter or universal runtime-ratio gate
substitutes for the final class-level objective. This screen does not promote
A065 or consume confirmation data.
