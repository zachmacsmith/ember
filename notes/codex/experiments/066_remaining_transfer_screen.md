# A066 unchanged-policy transfer screen

2026-09-10 UTC, before freeze or execution. The completed A066 screen passes its declared exploratory criterion: all nine successes retained, no Q increase versus A065 or pruning alone, and gains on three distinct structures (grid, WS100, planar100). The grid relabel also improves. All nine still lose MM; WS and planar chain-length variance increase. This supports one transfer measurement of the unchanged policy, not another neighborhood refinement or promotion.

Hypothesis: exact futile-query pruning plus coordinated adjacent-owner shortening remains useful beyond the first screen, including source sizes and structures where A065 already wins MM. The complete-output falsifier is lost success or increased Q against fresh A065. A result confined to the previously observed graph structures would limit generalization even if aggregate Q improves. These are development inputs already exposed to other policies, not fresh graphs or confirmation.

Freeze the remaining Transfer003 encodings already evaluated by A065: g0002, g0003, g0004, g0005, g0007, g0008, g0009, g0010, g0011, g0012, g0014, g0015, g0016, g0018, g0019, g0020 and g0022. This includes BA160 as well as the previous remaining-transfer set and K100. Seventeen encodings, sixteen structures with a nested BA80 relabel. Combined with the first A066 screen, this covers26 encodings/24 structures; ER100 g0101 is outside this declared A065-exposed complement and remains explicitly unmeasured by A066.

Run `066-paired-boundary-remaining-transfer` on hyde06 using idealZ12 and existing isolated native/MM environments3c7aa36653394e61. Four separate methods: native-paired-boundary (A066), native-compiled-boundary (A065), native-branched-path (retainedA061), pinnedmm. Seed0,60s each, exactly68 cold calls, shuffled sequential method order. Preserve every attempt, failure, solverwall/CPU and processwall; no warmup exclusion or output combination. This is the coordinated A066 package versus A065, not a new pruning-versus-pair causal ablation.

Pseudocode and configuration are unchanged: construct one fresh A061 embedding; evolve that state with strict-Q whole-chain reconstruction, exact two-site impossibility checks and atomic pair shortening; publish only independently certified timely complete minors. No saved map, MM output, hidden witness, source-family label, instance-specific rule, new work cap or new operator enters the candidate. Reuse the audited freezer, original validator and analysis infrastructure; no new algorithm tests are required because no candidate source changes.

Report every row and per-family common-success meanACL alongside individual wins/losses, within-chain variance and all-attempt costs. Exclude relabel aliases from independent structure totals. Across-seed variance remains unknown. Broad exploration continues only if A065 successes survive, no A065 Q regression occurs, and final gains extend to at least two source families outside the first screen's grid/WS/planar gain families. Preserve failures of this expectation; a later bounded follow-up needs a specific measured cause. Passing does not establish all-class coverage, acceptable sparse runtime, novelty or publication readiness. A061 remains the full-class retained comparator until a separate promotion audit.

Self-critique: the move is deliberately small and cannot be expected to repair every spatial obstruction; many dense chains do not trigger it. A065's unused successful search opportunities may change with deadline truncation, so scalar base agreement is not whole-trajectory equivalence. Sixty seconds remains much slower than MM on easy sparse cases. This screen measures transfer and regressions; it cannot justify another series of small local repairs solely from more pair commits.

Authority: results/codex/a066-constructor/audit001/output/summary.json (firstPASS, root234bindings) and mechanism001/output/summary.json (firstPASS, root56bindings); exact result and source identities are frozen in the screen descriptor. No constructor is rerun to repair a failed record. The initial nine-encoding result, all older failures and MM deficits remain unchanged.
