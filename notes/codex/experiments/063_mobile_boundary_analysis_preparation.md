# A063 saved-output mechanism reader

2026-09-09 UTC. Prepared before archive access or execution. Root's shared
`scripts/codex/transfer_analyze.py` remains the only source of original-label
validity, credited Q, failures, timing and the fixed continuation decision.
The new reader imports no algorithm or validator and runs no constructor.

Run `results/codex/a063-transfer-analysis/mechanism.py` with `--archive` pointing
to the root-verified quiescent bundle, `--validated` to the shared analyzer's
completed output directory, and `--out` to a new exclusive directory. Root must
first supply the retrieved archive and review this preparation. Nothing has
been executed. No new correctness suite or candidate call is requested.

The reader binds frozen manifest
`2a96e95e608bda8eccdbe6c5c6eaae29edc954fe1da932c19d6a93e3ae57c14c`,
source snapshot `c183efaeec904db944874eb5b805d204b2acce3f454b4c3d95ba81e7255dafc2`,
the exact A063 modules and shared analysis script. It requires the shared
analysis's 66 task IDs and exact raw-result hash bindings, then retains all 22
candidate rows with their paired A061/MM comparisons. This does not replace the
shared analyzer's complete 66-row output or its 20-structure/two-relabel scope.

Interpretation has two separate dimensions:

| Returned output | Saved stage progress |
|---|---|
| Inherited constructor failure or late return | Stage absent; no inference about this operator's reach. |
| Timely unchanged incumbent | Distinguish no remaining stage allowance, completed no-gain pass, and deadline-censored setup/search/certificate. |
| Independently credited strict Q gain | Retain every saved commit; a later deadline stop still censors unvisited alternatives. |
| Uncredited final output or fatal error | Preserve earlier receipt gains diagnostically; never convert them to output credit. |

Completed `no_gain` queries require complete ranking and all eligible roots
examined, completed and rejected on actual Q. The lower-bound skip is counted
separately. A complete no-improvement pass establishes only the fixed heuristic's
no-gain result; it does not prove optimality over arbitrary trees, donor choices
or contact assignments. Interrupted queries and unsaved alternatives stay unknown.
The old-root reach invariant is not a smaller-Q guarantee.

Receipt checks are limited to strict-Q continuity, accepted/query/commit counts,
changed-owner site-count conservation, recorded timely admissions and saved event
and phase totals. There is no intermediate physical reconstruction or witness
replay. Inherited base Q versus fresh A061 Q is a separate signed difference
from the mobile stage's savings; neither comparison substitutes another output.

Wrapper base/stage/final-validation intervals are disjoint. Operator phase costs
partition its elapsed work *inside* the wrapper stage. Per-query, full-target
root-generation and root-reconstruction costs are nested receipts and are never
added again to exclusive totals. Failed/nonimproving, smaller-Q and interrupted
root reconstruction remain separate. Residual wrapper time is named only as a
residual, with null components visible; it is not attributed to compilation or
another unrecorded cause. Work counters are event counts with no denial cap, not
CPU-equivalent units. Missing readout costs are never converted to zero-cost work.

`preparation.json` in the reader directory binds this note, the script and frozen
references. Execution remains pending root's archive notice and approval; any
readout/binding failure will be retained without candidate retries or refinement.
