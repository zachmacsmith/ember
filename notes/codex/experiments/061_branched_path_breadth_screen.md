# 061: unchanged branched-path breadth measurement

Approved prospectively after060. Freeze the unchanged059 algorithms and policy
for one fresh34-structure/35-membership readiness-panel comparison: fixed A053
`native-site-transfer`, `native-branched-path`, and stock `mm`, all configuration
`{}`, seed0,60s,102 serial fresh-process calls. Use exactly042's source/target/
selection bytes and original manifest graph order. The six059 originals are
predeclared repeats;059/060 remain separate observations and are never replaced
or pooled into this run's timing estimates.

Host amendment before outcomes: root selected HYDE06, replacing the proposal's
suggested03 because03 is reserved for C009. All three arms for every input use
06's existing3c7aa36653394e61 native/MM Python3.11.2 environments, one thread,
empty per-task JIT caches, unchanged import guard and process supervision.
This is a host choice, not a source/order/budget change. Methods are supplied
A053,branched,MM and shuffled by the unchanged `Random(f'{graph}:0')` rule.
The exact102-row vector and input identities are saved before runtime freeze.
Root owns source/transport freeze, readiness checks, one detached start and
terminal verified retrieval. No launch occurs during this preparation.

Hypothesis: retained side branches make the same bounded local proposal useful
on structures outside the six studied families. The global connected-branch-set
state already expresses arbitrary valid minor mappings; this tests a local move
restriction and complete-constructor applicability. It is not another local
repair, change to acceptance, family dispatch or selection of finished outputs.
One A053 construction is followed by the identical stage: eight seeds,128
owners/path,512 route sites,32 attempts/path,1M units and the same local/original
deadline convention. No new correctness tests or numerical diagnostic precedes
this measurement; the code, native dependencies and validator remain unchanged.

The fixed breadth gate requires no loss of a fresh A053 timely valid success,
no paired per-structure Q regression, and strict Q gains on at least two
previously untested noncycle structures outside the original six (1829,33219,
32122,1584,34404,4083). Count each normalized input once; the linked king/
frustrated memberships cannot supply two gains. Undefined comparisons cannot
supply a gain, and all missing/failed/late outputs remain in denominators.
Audit errors prevent advancement. A coverage/quality regression or insufficient
new-structure reach blocks a broad-use claim; do not retune this operator in
response. A passed gate still cannot establish MM superiority while class
quality, failure or >10× runtime gaps remain.

Reuse the unchanged042 original-label gate,059 stage receipt arithmetic and060
status/three-arm reporting. Retain all102 statuses, original-valid Q/ACL,
maximum chain, within-chain variance, solver/CPU/process times, every MM gap
and >10× ratio. Show34 unique inputs and35 memberships without double-counting
totals. Separate wrapper base Q versus fresh A053 from admitted stage savings;
report all work/cost/certificate/commit/normal-cap and deadline diagnostics.
For >10×MM rows, reuse saved inherited A053/core/layout/expansion scalars versus
new-stage cost; do not profile or assign unexplained core time to JIT. Report
all-attempt time, both paired common-success denominators, and optimal ACL1
ties. This single seed supplies no across-seed variance estimate; per-chain
variance has a different meaning. No exhaustive unsaved physical trace replay.

Self-critique: the inputs are all exposed archived development records; broad
family coverage is not class-population or held-out evidence. Existing core/
lifting failures remain outside a complete-output stage. Positive aggregate
savings can coexist with large MM deficits; they must remain visible. The
purpose is one bounded applicability decision before any separately critiqued
upstream mechanism, not a chain of local corrections.
