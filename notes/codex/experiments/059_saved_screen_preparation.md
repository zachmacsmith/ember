# 059 saved-data screen preparation

The analyzer is prepared before observations. It reuses the unchanged042
original-label inverse/validator and the056 finalized status, dependency,
quality and time checker. The042 originals gate still checks all34 reference
inputs against the frozen053 archive; the new six records and target must
match those bytes exactly. It checks the059 manifest/source/transport/archive
identities and all eighteen predeclared graph/arm/seed/config rows. It imports
only saved-data helpers and the frozen pilot task checker, never constructors.

The only new receipt checker reconciles wrapper base Q, operator entry/retained
Q, strict committed deltas, distinct certification/admission counts, branch
component partitions and accounting domains. It preserves local late/work
stops rather than conflating them with final deadline rejection. Returned
partial/diagnostic maps remain uncredited; any full-minor validity on them is
diagnostic, not proof about a smaller placed requirement graph. Missing
operator fields are unknown, not fabricated zero work.

Each input has fresh A053, treatment and MM metrics/times. The paired identity
is `final_treatmentQ - fresh_A053Q = wrapper_baseQ - fresh_A053Q - stage_savings`.
This exposes prefix variation separately from local savings. Solver/process
ratios use only this new same-host run; no historical MM is pooled. All six
outcomes, regressions, failures, MM gaps and advancement conditions are saved.
Branch/site totals count processed path records and can count a physical site
again on another path. Full raw diagnostics and receipt errors remain in rows.

One small synthetic saved-record check passed: a local late stop keeps an
earlier committed gain while preserving a later certificate-only proposal;
four arithmetic corruptions are rejected; a failed base has no invented stage
savings. There were no candidate imports or constructor calls. This is not a
new trace auditor: it does not reconstruct unsaved intermediate minors or
exact admission timestamps, and does not repeat inherited physical audits.
The original final embedding is independently validated. Root reviews the
prepared bytes before one execution on its terminal hash-verified archive.
