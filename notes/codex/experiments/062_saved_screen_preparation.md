# A062 saved-result screen preparation

This prepares one analysis of the fixed HYDE06 archive after root supplies its
terminal verified digest and reviews the analyzer. No candidate calls, outcome
reads, test expansion or source changes occur in preparation. All36 task rows
and all12 structure/seed triplets remain in the report.

`062-results-review/screen.py` reuses the unchanged042 original minor oracle,
056 status/timing checks,057 additional-original label verification and059
branch receipt checker. The four original and two additional records must match
their plan-bound original/normalized/label bytes. Full runtime source, exact
manifest/task order and quiescent retrieval inventory are checked. The known
wrapper identity is checked before a private tag adaptation lets both arms use
the unchanged059 branch arithmetic; raw diagnostics remain untouched.

The small journal adaptation retains the fixed A053 reducer reconstruction and
checks each committed actual journal index, dependency readiness, unique fill
removal and resulting active R. It checks final R against the original source.
Saved committed partial maps use the original oracle against reconstructed R;
unadopted core maps remain separately diagnostic. Successful pre-branch base
maps are unsaved, so their Q/R are checked as receipts and their physical
validity is not independently asserted. Final post-branch outputs receive full
original-label validation. Failed-row pending counts use that row's private
fill release without adopting it into committed R.

The scheduler checker reconstructs ready sets and checks complete versus partial
key prefixes, reverse-index tie breaks, no selection from unfinished keys,
selected/committed row identities, first departure and retirement accounting.
Whole-target work is distinguished from completed bigint operations; all
scheduler work/wall remains a subtotal of expansion. Exact physical counts,
intermediate owner maps and certificate timestamps are unsaved. Transfer,
ordinary and repair implementations are unchanged; their full raw records and
cost/acceptance counters are retained without another physical trace replay.

Both arms' base Q and branch savings are reported separately:
`final_delta = base_delta - branch_savings_delta`. The predeclared gate requires
no lost fresh control success, no common-seed structure mean-Q regression,
a fresh timely control lifting failure recovered, a mean-Q gain on another
noncycle structure, and no solver/process ratio above10. An absent common mean
or ratio is shown explicitly; missing cost ratios prevent advancement. Core
failures and timing/error results do not count as recovered lifting failures.
All MM and per-seed regressions remain visible. This is six exposed records,
not a broader class or held-out claim.
