# Independent 041 preflight

2026-09-08. The local input/committed-runtime gate, remote byte/environment/
zero-attempt gate and target-environment synthetic checks all pass. Root staged
the exact runtime and separate synthetic bundle, ran the suite once and returned
the complete stable evidence for independent inspection. This review made no SSH,
transport, launch, constructor or corpus execution call, and has read no 041
outcome. Root alone owns those lifecycle actions.

## Local inputs and frozen source

[`inputs001.json`](../../../results/codex/041-independent-review/inputs001.json)
checks all 105 copied evaluator/ledger files under `041-protocol/evaluation`.
The original 103 graphs, inverse label maps, normalized expectations and target
match audited 039 exactly; the two selection ledgers match the verified 039
archive. Every normalized graph is independently reconstructed from its original
integer-labelled graph and inverse map, retaining all isolates and edges and
discarding solver-visible family/embedding metadata. There are 34 distinct
normalized topologies and 35 retained family memberships. The King's/frustrated
square alias has one aggregate structure weight. Original Sudoku remains absent;
the separate supplement is not an input. Actual ideal Z12 counts remain 4,800
vertices, 45,864 edges and maximum degree 20.

The graph shuffle and each adjacent method-pair shuffle reproduce the declared
order exactly. The immutable order SHA256 is
`9b2dde6634114d8e229219098d394c334f452f21b594fc48d3b8243048db2e74`.
A restricted literal AST interpreter checks the pilot configuration definitions
without importing or executing the pilot. All old configurations are unchanged;
the new arm only changes `polish_objective` to `qubits_endpoint_support`.

The final local gate is
[`frozen002.json`](../../../results/codex/041-independent-review/frozen002.json),
SHA256 `ce137c3cf1dbdcf0d267d52d125fc2feee0bb55eaa055847503289f85d8ff90e`.

| Item | Verified identity/count |
| --- | --- |
| Committed revision | `9d262fd4efa891308c5049d9e9375b28aa3385f8` |
| Runtime source map, 50 files | `ecd67cbed55a96a616d1aca018eb132d39c58e828394a2b2e5caad83859ed893` |
| Runtime manifest | `0e8bdff10210f119599973f26e04da3554debdec5a09cba47538467939630805` |
| Expected runtime transport map, 156 files | `b600e025b3d89d7696e17a11144695bc13c95c50b24e9b78208d91b3c1d45bf7` |
| Target record | `38cde794d3c1461054a45b5a660d157737b019c19cb9a7b38e2027730e3d5938` |
| Evaluator manifest, 105 input files | `ac6dc2e0ec67dd5f12625ac8b9d1a68e4eab2a2e6800511480148b6180ef411b` |
| Evaluator file-map digest | `b8062ff8fd013c10190afc7525999341eeeb094a2448dc787d153396e5ffe208` |

Every runtime file agrees with its committed Git bytes. Relative to 039, exactly
contact repair, native and pilot change; endpoint support and the isolated
`deletion_closure.py` are added. The latter has root-declared SHA256
`66a2bc21af6c035ca0b03f9065e2ce780ca0503b9be1af95487eb19e6be67d8b`,
and no other production source references that module. Neither arm contains a
new final deletion pass. This gate identifies the unused file; it does not claim
an independent review of that separate algorithm's correctness.

The exact accepted endpoint implementation review, its six evidence files and
14 references are rechecked. The four integrated production hashes must match
that review, independently of the new runtime manifest. All 68 task digests,
fields, source hashes, seed-zero assignments, 60-second budgets, pair order and
fixed configurations are recomputed. The target and all worker graph bytes agree
with the evaluator expectations. Defaults retain greedy trees, legacy singletons,
stars off, 500,000 routing units, 50,000 per visit, two orders and the existing
region/alternative rules. Source and target metadata do not supply embeddings.
All local result, worker-result, claim, log and task-cache directories were empty,
with no execution record when the gate ran.

The verifier is
[`preflight.py`](../../../results/codex/041-independent-review/preflight.py),
SHA256 `2cc51d53542e519d281afd50cd1ebcd4a17680e8081357d3adc04f05c49c9c7a`.
It reuses the exact previously reviewed 039 structural/original-label helper,
SHA256 `4405c7048f59d2a12ba6228f9903ff9d2f2668cd19fa60c4acfd754d8a401e32`.
`frozen001.json` and the initial verifier are preserved; the final revision adds
explicit binding to the accepted implementation-review manifest and evidence,
rather than accepting a newly supplied source map by itself. Both local checks
passed. These are read-only checks, not repeat candidate executions.

## Separate target-environment synthetic bundle

The immutable bundle, staged and executed once by root, is
`results/codex/041-independent-review/target-check-bundle`, intended for
`/home/dabh/ember-codex/checks/041-endpoint-support`. It contains 57 files plus its
manifest: all 50 exact runtime source dependencies, the frozen specification,
three pre-edit source references, the accepted reviewer harness/hash map and a
small process wrapper. Packaged graph manifest/preset metadata are import
dependencies; no corpus adjacency input, target instance, result or saved
embedding is copied. All 50 runtime source bytes match 041.

The bundle file-map digest is
`782b2f3c1380cc11e65ff956ba9603bf3ac0ac0ffefddc6a58fc3e40d0571a19`;
its manifest SHA256 is
`0a9ec8ee9be176d20a293b337c6bd30eb8e92d0bd3bfebbf95b38e8bf6328a6d`.
The wrapper SHA256 is
`6e1594fd6ab35a730462344abb142db71f9b3248d26cbeca0a9ed30bd5a1fa04`.
Root verified the complete staged map, including the manifest, and the four
candidate hashes match the runtime freeze.

The wrapper requires an exclusive output directory **outside** the immutable
bundle. It rejects extra/missing bundle files and checks every hash before and
after the suite. It checks dependency pins and the absence of MM/busclique
modules and distributions, preserves the pinned interpreter path, uses one
thread and a private initially empty cache, and runs the reviewed nine-group
unittest suite under `-I -B` with a 45-second subprocess timeout. No pytest or
corpus solver is needed. Logs, invocation/environment data, test output and
summary are retained. A failing or timed-out execution is not a pass and cannot
reuse the same output identity. Test duration must not enter benchmark timings.

Root's one-time target command was:

```sh
/home/dabh/ember-codex/envs/4e1fb892db12754e/native/bin/python -I -B /home/dabh/ember-codex/checks/041-endpoint-support/run_target_checks.py --bundle /home/dabh/ember-codex/checks/041-endpoint-support --bundle-digest 782b2f3c1380cc11e65ff956ba9603bf3ac0ac0ffefddc6a58fc3e40d0571a19 --output /home/dabh/ember-codex/check-results/041-endpoint-support
```

No additional local suite repeat was performed, since the accepted original
review already passed on these four candidate hashes. Preparation's initial
wrapper and manifest are retained outside the final bundle; the final
pre-execution revision adds exact inventory checking and requires outputs
outside the bundle. No synthetic execution preceded that revision.

The optional read-only remote runtime metadata/zero-attempt probe is
`results/codex/041-independent-review/remote_preflight.py`, SHA256
`060d65402d0bb590ee0f9f18f9198ed49bd9d1bba4821fc9737fa0dae6586ef6`.
It differs from the reviewed 039 probe only in the declared source count (50)
and experiment labels. Root can execute it with the exact source, transport and
manifest identities above. It checks pinned venv metadata without importing a
candidate, staged hashes, empty task/cache directories, free inherited lock,
supervisor state and the existing disconnect-resistant execution route.
Remote success is a separately inspected observation, as recorded below.

## Observed remote gates and synthetic result

Root's remote metadata probe exited zero with empty stderr. Its observation at
Unix time `1788858129.4890878` reports the exact 156 staged input files, all 50
source files and all 68 task identities, with zero attempts and empty result,
worker-result, claim, log and task-cache directories. The inherited run lock was
free; no matching active worker or live supervisor was found. The eight prior
project runtime identities inspected by this probe were quiescent. These are
prelaunch observations, not statements about later execution.

The native interpreter remains
`/home/dabh/ember-codex/envs/4e1fb892db12754e/native/bin/python`, with Python
3.10.12 and every installed distribution agreeing with its prepared record.
The fixed NetworkX/NumPy/SciPy/Numba/dwave-networkx pins match. MM, its extension
and alternate module names, and busclique are absent in the native environment.
The separately inspected MM environment retains 0.2.22 but supplies no 041 task.
Metadata inspection itself imported no candidate. The recorded execution route
is tmux with timeout: `Linger=no`, `KillUserProcesses=false`, and tmux/timeout
available. This establishes the retained disconnection route, not reserved CPU
capacity or cold operating-system caches.

The nine-group independent synthetic suite ran once on hyde03 and passed all
groups, including 121,800 exact oracle score comparisons, all interruption
prefixes and sixteen legacy replay pairs. Candidate bytes were unchanged,
prohibited imports/loaded modules were empty, and the interpreter was the pinned
native venv. Suite wall was 2.51213 seconds; this is correctness-test time and
must not enter benchmark timing populations. The enclosing wrapper exited zero
without timeout and verified the immutable bundle after execution.

All six retrieved synthetic evidence files were independently rehashed against
their remote inventory. Their map digest is
`1a6e127b22dd2241fa3a7892dac8454d101fc3c34f6787fab19caefe540e61d4`.
The suite-summary SHA256 is
`fddde890de94f0410e2bcdca9c8defb67439a1b1fc6eed1b43c13e82ebf210c2`;
the wrapper-summary SHA256 is
`4c45f0e23ff7578ab8a878d0629c8c316d560d467c99ae886c91d30bb685c7bf`.
The combined independent gate is
[`preflight_final.json`](../../../results/codex/041-independent-review/preflight_final.json),
which binds local/remote observations and the full evidence inventory. No
preflight blocker remains for the fixed root-owned start. Future outcome
validity, deadline and diagnostic accounting remain post-run obligations.

## Frozen comparison expectations

Before any 041 observation was read,
[`comparison_expectations.json`](../../../results/codex/041-independent-review/comparison_expectations.json)
was saved with SHA256
`e742611e2a33b1251e20baea17855fcbdd3cb86d6f522cda92422c08c4cad09a`.
It binds all inputs, memberships, order, policies and degree-capacity bounds, and
requires all 68 outcomes to remain visible. Only independently valid, timely
successes enter explicitly identified quality populations. Historical control
replay is consistency evidence only; no timings are pooled across runs.

Signed R and histogram changes remain integers when complete and null otherwise.
Known partial sums cannot replace an unknown net change. Histogram JSON keys
must be interpreted as integer bins. Within-group equal-Q best updates, final
equal-Q group commits, and net-unknown committed groups have distinct meanings.
Only the last interruption stage is recorded. Setup, score, comparison and
proposal-validation costs remain separate and inside total solver time.
Unsaved intermediate chains prevent independently certifying every intermediate
score or delta; the audit can independently recount final physical validity,
Q, R and endpoint support. One seed supplies neither across-seed variance nor
family-population superiority, and this remains a development scoring-policy
comparison with no concurrent MM arm.
