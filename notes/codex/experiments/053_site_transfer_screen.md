# 053: one-site transfer during filled-core expansion

Exploratory protocol, saved before the corpus screen on 2026-09-08. The
[pre-code policy](053_site_transfer_hypothesis.md) and
[implementation/reach record](../site_transfer_implementation.md) precede this
experiment. Eight focused groups pass and all eight paired reach outputs are
original-valid. Cycle improves155→148 Q, wheel worsens189→191, and star and
subdivision tie. Root accepts that explicit regression as a reason to measure
the broader tradeoff, not to promote the candidate. No ordering, limit or
acceptance change follows these results. The deadline-receipt-only correction
and its one targeted check are preserved separately; no reach call was repeated.

Run fixed A050 `native-reduced-core-repair` and A053 `native-site-transfer`,
both configuration `{}`, as separate isolated processes on hyde03. Reuse the
exact042 bytes for34 development structures/35 memberships, target Z12 and
selection; seed0,60 seconds,68 calls in the audited paired task order. No MM
arm or alternate constructor output is used. This isolates the transfer query
and its effects on subsequent construction, including its charged overhead.

Reuse the audited dependency guard, worker deadline, original embedding
validators and detached cluster controller. Freeze source/task identities
before stage/start. Observe the same remote handle after a network loss;
an observation failure never authorizes restart. Preserve every failure,
timeout, error, valid late output and partial prefix, with solver and process
time separately. Form timing ratios only from these fresh same-host pairs.

The saved-result screen independently validates original complete and partial
minors and reuses the existing filled-journal, core-call and repair checks.
For A053, reconcile transfer queries, scans, local/global limits, certificates,
returns and actual commits. Raw transfers must have constant occupied Q;
following inherited cleanup and the final construction can change Q. Do not
infer intermediate ownership from aggregate receipts or add overlapping
transfer/repair wall to total solver wall. No exhaustive intermediate replay
or shared validator change is required for this exploratory screen.

Report every input's outcome/Q/ACL, common timely-pair macro ACL, total Q,
wins/losses/ties and all attempted work/time. Saved048 is a historical quality
reference only. Any completion loss prevents promotion; preserved completion
and lower mean ACL warrant a later seed/generalization experiment, not an
all-class claim. Runtime costs and unchanged filled-core excess can reject
this policy despite a local gain. No graph-specific acceptance or pooling.
