# 061 saved breadth screen preparation

The minimal analyzer is a small adaptation of059's approved three-arm screen.
It imports the exact unchanged059 stage receipt checker and existing056 row/
timing helpers;042's original-label/physical oracle remains unchanged. Scope
changes to34 unique inputs,35 memberships and102 predeclared seed0 tasks.
It reports each class membership while counting the shared king/frustrated
input once in totals and the new-structure gain threshold. No new numerical
correctness tests, candidate imports or calls occurred during preparation.

Bind the actual061 manifest/snapshot. Every059 source-map entry must be unchanged;
new unrelated modules may appear only in the separately declared added-file map,
and every frozen file hash must match. This accommodates the new uncalled
connected-domain module without claiming whole-snapshot identity. Inputs,
selection and target remain exact042/053 bytes. Root supplies the final runtime
manifest and terminal hash-verified archive before one approved saved analysis.

The unchanged physical/status gates preserve all missing, failed, invalid or
late results and all attempted times. The new fixed gate requires no loss of
fresh A053 timely success, no paired per-structure Q regression and at least
two distinct newly studied noncycle structure gains outside the prior six.
Audit errors block advancement; undefined comparisons cannot count as gains.
Every MM/runtime gap remains visible even if this relative breadth gate passes.
The analyzer keeps wrapper base-versus-fresh-control Q separate from stage
savings. Work, cost, certificates versus commits, finite caps and local stops
use exactly059's saved receipt arithmetic; there is no unsaved physical replay.

Outputs retain all102 rows,34 paired three-arm rows and35 class memberships.
Own-success means and matched-success MM macroACLs have separate denominators;
no historical or best-seed pooling occurs. Single-seed class/sample variance is
not estimated; within-chain variance remains a different metric. Reporting
will reuse060's saved scalar cost decomposition for >10×MM rows, without new
profiling or attributing residual time to JIT. Full invocation/log/status and
exclusive analysis001 artifacts will be preserved. No source or registry edits.
