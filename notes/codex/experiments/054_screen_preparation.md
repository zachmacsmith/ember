# 054 saved-result screen preparation

The checker is frozen for the root-selected 16 structures: all 13 changed
reductions plus the first three unchanged controls, in original order.
It expects 32 finalized tasks and 17 evaluator memberships. The full
34-row selection ledger and source-only comparison determine this scope;
18 omitted structures receive no inferred outcome or quality contribution.

The shared 042 originals checker runs against the complete frozen 053
archive. Exact new graph/target bytes must match that reference before its
original-label maps and unchanged physical oracle are used. Existing
journal, repair and transfer checks are copied with narrow threshold/identity
changes. Each journal is reconstructed under its arm's threshold and also
compared with the already saved source-only record. Both arms retain all
repair/transfer accounting. Three unchanged pair comparisons and historical
053 control replay fields retain any differences without assigning a cause.
Historical 048 quality, if reported, covers only these same 16 structures.

Eight **saved** reach records passed the adapted checks, plus three directed
corruptions. The first calibration harness stopped when a deliberate unknown
journal vertex raised `KeyError`; its script, log and hash record remain.
The second harness correctly counts a caught malformed-record exception as
rejection, as the full screen already does. No helper correction, constructor
call or corpus analysis was performed. Native core work remains outside the
wrapper scan count; overlapping stage subtotals are not summed twice.

Bindings and diffs are in `results/codex/054-results-review/preparation.json`.
Execution awaits root's verified terminal archive and retrieval digest:

```sh
.venv/codex-native/bin/python -I -B results/codex/054-results-review/screen.py --archive results/codex/retrieved/hyde03/054-degree-two-focused-pipeline --archive-digest ROOT_SUPPLIED_DIGEST --out results/codex/054-results-review/screen001
```
