# A064 passive mechanism analysis contract

2026-09-09 UTC, before any screen-result read or reader execution by this agent.
Root supplies the terminal 36-task archive and a passing independent
`transfer_analyze002.py` audit. The mechanism reader imports no constructor,
generator, validator or MM implementation, and does not replay physical moves.
All final validity, success, Q/ACL, variance, failures and elapsed costs come
from that audit. It reads raw candidate/A063 diagnostic receipts only after
binding them to the audit's exact file hashes. It never reads MM embedding maps.

Bind frozen screen, manifest, source snapshot, both added-stage source versions,
the original A063 reader and shared audit version before interpretation. Reuse
the existing A063 `interpret` function for the fresh predecessor's saved
receipts. Adapt its disjoint wrapper-cost, exclusive operator-phase, actual-Q
commit-prefix and censoring calculations to A064; do not reuse its sequential
query-order assumption. A064 queries are indexed by `(epoch, owner)`, and an
interrupted resumed query need not be the last-created query.

For each A064 input, retain paired A063/A061/MM results and independently creditable
stage gain separately from inherited-base Q. Report unique owners reached,
owners with rankings/completed roots, rounds, per-epoch root work and closures,
invalidated query preparation/active cost, repeated owner preparation, cache
accounting/disposal and existing process peak RSS. Query active intervals are
nested in stage time; their first/last elapsed spans include parking and cannot
be added as computation. Invalidated work was spent on a previous incumbent;
its scalar total does not prove all that computation was avoidable waste.

Reconcile strict-Q receipts with actual before/after owner sizes, matching query
and epoch identities, and final independently credited Q. Report admission time
relative to both operator and wrapper starts, derived from their recorded
end-minus-wall intervals. These are receipt times, not physical replays or
externally observed time-to-optimality measurements. A committed receipt on an
uncredited call remains diagnostic and supplies no benchmark win.

Only same-current-epoch closure of every owner establishes exhaustion of this
fixed heuristic search. A completed interleaving round without commits does not.
Deadline stops preserve unknown unvisited alternatives; a subsequent cleanup
deadline must not hide the first stop phase. Fatal errors, absent diagnostics,
unknown time, invalid/late maps and every regression remain explicit. Across-seed
ACL variance is unmeasured. The reader emits observations and consistency errors;
it does not automatically promote A064 or authorize another refinement.

Save source and authority hashes before invocation. Execute once, only after
root's archive/audit authorization, in a fresh recorded attempt directory with
stdout/stderr/status. No candidate call or new graph generation is part of this
analysis. Root decides continuation from complete-constructor outcomes under
the fixed A064 policy, including the still-untested wheel risk.
