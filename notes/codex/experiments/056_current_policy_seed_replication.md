# 056: current A053/MM seed replication for class gaps

Prospective measurement within track A, not a new algorithm. Freeze the
retained A053 `native-site-transfer` policy, config {}, against stock MM0.2.22
on all34 already exposed readiness structures /35 memberships, seeds0–3,
ideal Z12,60seconds per call:272 serial fresh-process calls on hyde06.
The source/target/corpus bytes must match053/042. No source selection, best
seed, fallback output or candidate parameter changes occur during this run.

The hypothesis to resolve is whether current A053's observed class deficits,
optimal ties and completion persist across solver seeds, and what their
same-host cost and sample variability are. This measures the fixed policy;
it does not identify a causal search mechanism or supply new source instances.
A reversal on matched successful seeds disproves a robust one-seed quality
interpretation. Failed/late calls remain in denominators and all-attempt time.
One source per represented class cannot establish a class-population mean.

Reuse the audited pilot, dependency import guard, immutable source freeze,
original-label042 validator and detached cluster supervision. Candidate and
MM use separate environments under `/home/dabh/ember-codex/envs/3c7aa36653394e61`
(`native/bin/python`, `mm/bin/python`), Python3.11.2 and the existing pinned
requirements. Both arms run on06, with seeded arm ordering and thread limits
unchanged. This host/interpreter differs from prior03 measurements: compare
ratios only within this new run and report any seed-zero quality differences.
Fresh-process/JIT cost remains included; no warm-cache credit is introduced.

Preparation on06 used the existing environment builder and completed once.
Read-only inventory found128 logical CPUs, one-minute load117.10 and about
88GiB available RAM; these are observations, not exclusive-resource claims.
The existing remote launcher checks logout persistence before launch. Record
one controller handle and reconnect to that handle after network loss; never
restart because observation failed. Root owns freeze/start/status/fetch.

Report per source and per class membership: successes/attempts, successful
ACL mean and sample variance (denominator k−1, absent below two successes),
matched-success mean differences, optimal ACL-one ties, every regression,
solver/process wall and paired ratios, all failures and deadline overrun.
Do not substitute within-embedding chain-length variance for seed variability.
Do not compare unequal successful-seed means as paired estimates. Show all
34structures separately; the shared king/frustrated input counts once in
aggregates. Dense gains cannot discharge another class's deficit.

This requires no new correctness infrastructure or algorithm test suite.
Reuse existing frozen-result identity checks and original validation, then
standard-library arithmetic over their outputs. Investigate invalid results
or dependency violations before interpreting quality; do not rerun failures
for a favorable replacement. All current mechanism development remains on
02/03/04 independently. No outcome from this replication is available yet.

Self-critique: four seeds remain few, all sources are exposed, and a new host
can change runtime and numerical trajectories. This is useful gap measurement,
not publication-grade reproducibility or a guarantee about future A055/B/C
versions. It does not justify promoting A053 while class deficits remain.
