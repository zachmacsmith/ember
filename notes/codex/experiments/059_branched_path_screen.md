# 059: retained branches during complete-constructor path refinement

Prospective, fixed eighteen-call development screen on hyde06: six existing
inputs in order `ember_1829` (cycle), `ember_33219` (cubic), `ember_32122`
(kagome), `ember_1584` (grid), `ember_34404` (planted), `ember_4083` (Petersen).
Use their exact audited053/042 source bytes and ideal-Z12 target, seed0 and
60 seconds per call. Methods are fixed A053 `native-site-transfer`, isolated
`native-branched-path`, and fresh `mm`, all configuration `{}`. The method-list
order is exactly that order; the audited pilot shuffles it independently with
`random.Random(f'{graph}:0')`. The complete resulting eighteen-row order is
saved before execution in `results/codex/059-preparation/comparison_plan.json`.

The hypothesis is that keeping side-contact branches removes the pure path's
demonstrated representation exclusion and permits useful strict-Q changes
beyond cycle. The new wrapper calls A053 once and refines that same evolving
minor; it has no fallback output, portfolio, MM call or free-site rerouting.
Unchanged local bounds are eight seeds, 128 path owners, 512 physical route
sites, 32 attempts per path and 1M metered work. All branch setup, failed
questions and certificates consume that work and the stage deadline
`min(original_D, stage_start + min(1s, .2 * elapsed_before_stage))`.
Previously certified maps may survive a later local stop. Final validation and
bookkeeping consume the original deadline, with no renewed stage search.
Failed A053 construction remains failed and skips this stage.

Reuse existing fresh-process pilot/controller behavior, one thread and empty
per-task JIT caches. Both candidate and MM environments are the existing
Python3.11.2 environments under `/home/dabh/ember-codex/envs/3c7aa36653394e61`
(`native/bin/python`, `mm/bin/python`); no cross-host or historical timing
comparison. Cold imports/JIT stay in process time, and implementation imports
precede solver timing as in the baseline. MM is isolated and unavailable to
candidate imports. Root freezes inputs, all source/dependencies and task
identities, verifies remote readiness/quiescence, starts once, and retrieves
only the terminal verified archive. Observation failure never authorizes a
restart, task retry, later state substitution, or replacement result.

Reuse saved-result task identity checks and the unchanged original-label042
validator. Retain every status, late/partial output and attempted wall time.
Report all six matched A053/new/MM outcomes with Q, ACL, maximum chain length,
within-embedding chain-length variance, solver/process wall and paired ratios.
Within-chain variance is not across-seed variability. Show each local stage's
before/after Q, accepted/certified counts, work/phase totals, branch counts,
normal attempt/work limits, deadline stops and interrupted certificates.
Reconcile final Q against the wrapper's base Q; expose fresh A053 versus
wrapper-base differences without attributing all final differences to the
operator. No exhaustive unsaved intermediate trace reconstruction is planned.

Advance only if the new full constructor is timely original-valid on all six,
improves cycle plus at least one other input versus fresh A053, loses no
successful coverage, has no aggregate-Q regression and no >10× solver or
process-time regression versus fresh A053. Missing/late/error output does not
satisfy that gate. A normal finite-budget stop with a timely valid retained
map is a measured result; its unvisited alternatives remain unknown. Fresh MM
quality and runtime are shown separately, including every remaining gap;
favorable A053-relative results do not establish MM superiority.

Self-critique: fixed residues can lock the same boundaries or retain excess
qubits; one occupied witness still excludes other routes and wider relocation.
This is one seed on six exposed structures, selected before this operator's
complete outcomes. It cannot support a34-input mean, seed-stability, class
population, new-source or novelty claim. Cycle-only improvement rejects the
broader-use hypothesis. The known A057 incomplete constructions are explicitly
outside this stage's reach. No extra saved-final gate, allowance change,
parameter tuning or additional reduction variant precedes these calls.
