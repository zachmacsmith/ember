# 060: unchanged retained-branch replication

Prospective follow-up to059's four gains/two ties, with no source, acceptance,
route, ordering, budget or stage-allowance change. Run fixed A053
`native-site-transfer`, `native-branched-path`, and stock `mm`, configuration
`{}`, seeds0 and1,60s per call. There are two separate cohorts of36 fresh calls:
original six on hyde06, corresponding additional057 representatives on hyde03.
The original seed0 calls are an explicitly planned repeat;059 remains a separate
observation and is never replaced. The earlier54-call outline (original seed1
only) is superseded before any calls because the audited pilot implements
contiguous `range(seeds)`. Adding the complete original seed0 block preserves
that infrastructure; no selective replay or outcome substitution occurs.

Use the following fixed corresponding-family order within both cohorts. The
additional mapping comes directly from057's immutable012 provenance, not its
embedding outcomes. Each pair has the same family label, not an asserted
isomorphism or matched difficulty.

| Family | Original ID; n/m | Additional057 ID; n/m |
|---|---|---|
| Cycle |1829;126/126|1827;101/101|
| Cubic lattice |33219;125/300|33222;125/375 (periodic)|
| Kagome |32122;131/236|32115;144/288 (periodic)|
| Grid |1584;128/232|1590;200/370|
| Planted solution |34404;133/175|34374;115/196|
| Generalized Petersen |4083;126/189|4018;88/132|

Both cohorts use the exact frozen source graphs and ideal-Z12 target; no
constructor, graph generator or saved embedding supplies an input. Root freezes
runtime identities with the059 algorithm/validator dependencies unchanged.
All arms for an input/seed use that cohort's host:03's existing4e1fb892 native/MM
environments and06's existing3c7aa366 native/MM environments. Keep isolated
fresh processes, one thread, empty per-task JIT caches, unchanged import guard,
solver deadline and process supervision. Methods are supplied in the order
A053,branched,MM; each graph/seed uses the existing seeded pilot shuffle. The
complete72-row vector, source/label/raw hashes and interpreter paths are saved
before freeze. Root owns transport, one start per cohort and verified terminal
retrieval. A failed observer is not authorization to restart or retry a task.

The fixed continuation criterion is conjunctive. On the additional cohort,
all12 branched calls must be timely original-valid successes, no A053 success
may be lost, every class must have a defined nonpositive paired mean Q delta
(branched minus A053), and at least two of the five noncycle class means must
be strictly negative. Paired means use only seeds successful in both arms and
retain their exact seed lists/counts; an undefined class mean cannot pass.
On the original cohort, no fresh A053 success may be lost. Report all its class
and seed regressions regardless of that coverage condition. The known original
planted seed1 A053 failure from056 stays in the historical record; the fresh
observation is neither a retry replacement nor permission to reset it. If the
replication criterion fails, stop expansion without tuning this operator.

For every instance/seed, report all three statuses, original-valid Q/ACL,
maximum chain, within-chain variance, solver/CPU/process times and >10× timing
ratios. For each cohort/class, report successful ACL mean and sample variance
(undefined below two), matched-seed differences and exact denominators. Report
all MM gaps separately; never pool host times, best seeds, historical MM or
unequal-success means as paired estimates. Keep059 separate from the planned
original seed0 repetition. Reconcile each wrapper's base Q against fresh A053
and its admitted stage savings; retain local caps, failed proposals, certificate
versus commit counts, stage/gate costs and uncredited partial evidence.

Reuse059 saved receipt arithmetic and the unchanged042 physical oracle; use
057's audited original-label provenance for the additional cohort. No new
constructor tests, saved-final gate, exact optimizer or physical trace replay
is needed. This tests whether the same bounded mechanism helps additional
archived representatives and another seed. All records remain exposed; distinct
normalized hashes do not prove held-out data or new isomorphism classes. Two
seeds and one additional representative per family cannot establish an all-class
or population claim. A positive replication only supports separately reviewed
broader measurement, not automatic MM promotion.

Terminology clarification before replication: the global state is already a
collection of connected branch sets and can express the MM mappings. The
pure-path proposal's *local representation* omitted off-route side-contact
support. The26/26 unshortened feasibility observations show that this local
proposal restriction was repaired; they do not establish a lack of capacity
in the global minor representation. Remaining gaps can still reflect the
bounded move neighborhood, fixed routes/owners, acceptance or construction
cost. The059 numerical results and final report bytes stay unchanged.
