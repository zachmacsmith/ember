# 052: filled requirements versus original requirements with soft guidance

Exploratory protocol saved before the corpus screen, 2026-09-08. The [candidate hypothesis](../soft_fill_guidance_hypothesis.md), six focused checks and four fixed reach calls precede this comparison. A052 completes all four reach cases, including cycle126 at Q143, while wheel128 worsens and distance maps consume roughly half its work. These mixed results justify measuring the broader reduced-core quality tradeoff; they do not promote the candidate.

Run one control and one treatment per each of the unchanged 34 development structures (35 family memberships), ideal Z12, seed 0 and 60 seconds. Preserve the exact source, target and selection bytes from042. Both methods run as separate isolated processes on hyde03 with the existing paired task ordering. Control is fixedA050 `native-reduced-core-repair`, configuration `{}`. Treatment is A052 `native-original-soft-guide`, configuration `{}`. This compares the retained filled-requirements research line with the complete original-requirements/soft-guidance change. It does not isolate guidance alone. LiteralA051 and MM are not run; no outputs are pooled.

Reuse the audited worker deadline, dependency guard and original embedding validators. Freeze all source and task identities before staging. Reuse the detached cluster controller and observe the same handle after network interruption; observation failure never authorizes a restart. Record every success, ordinary failure, cap stop, error, timeout and valid late output. Failed/late partial Q is diagnostic only. Keep solver and process time separately, and form timing ratios only for these same-host pairs.

The saved-result screen must independently validate complete original minors and retained original partial minors. For A052, check immutable original physical requirements, original induced-core vertex/edge counts, filled ordering journal, at most one core call, no physical fill cleanup, insertion/repair coverage and original deadline. Reconcile guide setup and BFS counts with their records and enclosing work totals; they are overlapping costs, not extra time to add to solver wall. Reuse existingA050 checks as applicable; no shared correctness infrastructure changes or exhaustive intermediate-state audit is required for this screen.

Report per-input outcomes and Q/ACL, successful common-pair macro ACL, total Q, wins/losses/ties, and all failures/time. Compare full-input quality against the saved048 baseline only as a historical quality comparison. Any loss of completion prevents promotion. A mean-ACL reduction with preserved coverage warrants a subsequent frozen seed/generalization experiment; a broad win, variance reduction, speed advantage and novelty remain unproved. Runtime or work-cap regressions may reject this policy despite a local quality gain. No fixed input-specific guidance or post-observation allowance change is permitted.
