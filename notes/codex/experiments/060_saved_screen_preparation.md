# 060 saved replication screen preparation

The analyzer adapts059's saved row/identity checks to two six-input cohorts
with two seeds. It imports059's `stage_check.py` directly at its unchanged
SHA71b11a6d…; there is no new branch/physical checker. It reuses056's status,
timing and exact ACL-moment functions,042's original-minor oracle, and057's
original-label provenance function. The latter still verifies all35 old057
inputs; the additional six must match those records exactly. For originals,
042 still checks all34 frozen053 originals and the new six match the059 bytes.
No candidate module, solver or saved-final search is imported or called.

Each archive independently must be terminal, hash-verified and match its fixed
36-task vector. Both full source maps must equal059's unchanged snapshot.
The analyzer retains all72 rows, per-seed three-arm quality/times, class means,
sample variances, exact common-seed denominators, all MM gaps, stage receipts
and all failures. Host timing totals remain in separate cohort summaries.
Within-embedding chain variance is distinct from the two-seed sample variance.
Wrapper base-versus-fresh-control Q stays separate from stage savings. Added
aggregation implements exactly060's fixed gate: additional12 timely valid
outputs, no lost control success, all defined class paired means nonpositive,
and at least two noncycle class gains; original no lost control success.
An audit error or undefined additional class mean cannot advance.

Original seed0 versus059 is a separate historical-quality readout, never a
replacement or a pooled timing sample. The historical056 planted seed1 record
is preserved separately from its new outcome. Partial/diagnostic maps never
receive credit; full-minor validity of such a map is not proof about a partial
requirement graph. Unobserved work stays unknown. No new correctness tests are
needed for the unchanged oracle/stage checker; the new aggregation uses the
already checked056 rational formulas. Only static syntax/readback checks ran.
Root reviews these frozen bytes before one saved analysis on both terminal
archives; there are no live or outcome reads in preparation.
