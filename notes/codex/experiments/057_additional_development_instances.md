# 057: test the retained mechanism on additional archived instances

Pre-observation protocol, 2026-09-08. This is baseline measurement within track A,
not a fourth candidate. Freeze A053 and stock MM, two solver seeds (0,1),60s each,
serial paired calls on one spare host. Keep all outcomes and times. No candidate
refinement or per-graph configuration is part of this experiment.

Selection uses only the immutable original012 corpus ledger. For every family,
consider original READY records with 0<n≤512 and the necessary ideal-Z12 node/
edge count bounds. Exclude every normalized topology hash in the017 readiness
panel. Select the minimum exact rational size distance max(n,128)/min(n,128),
then source hash and integer ID. If none remains, record missing coverage;
do not substitute a new generator or relax a bound. Group identical normalized
records into one task and preserve every family membership. Preserve all012
input errors separately. Candidate inputs contain topology only.

These are additional archived development records. All inherited data remain
exposed development data, and a different normalized hash does not prove a new
isomorphism class. Report size/count differences and identity limits. This tests
whether current deficits recur beyond the single readiness representative; it
is not held-out confirmation. Fresh-structure generation and final confirmation
remain governed by the separate generalization protocol.

Hypothesis: if short-chain deficits reflect a general placement/search limitation,
similar relative ACL deficits should recur across additional instances in several
classes. If signs change strongly across representatives or seeds, prioritize
instance-level structural measurements instead of treating the first class label
as causal evidence. Two seeds give only weak variability evidence. Source success,
mean ACL, sample variance, paired-common-seed ACL and timing remain separate,
with no aggregate dense gains offsetting a regression. Missing/late/failed results
receive no embedding credit and retain their time cost.

Reuse the audited corpus loader, pilot, native/MM isolation and independent
original-label validator. The selected graphs and exact task order will be frozen
before execution. Host/environment preparation may proceed independently; all
MM/candidate timing pairs must execute on the same host. No original generator,
new correctness infrastructure, or exhaustive trajectory replay is needed.
