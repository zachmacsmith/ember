# Corrected converter in the fixed spectral pipeline

Predeclared2026-09-08, before any corrected full-pipeline call. This experiment
is conditional on root review of035's class-assignment oracles, actual seating,
state growth and runtime, and passing relevant native integration tests.

## Fixed treatment and inputs

Run the existing `native-search-joint1-contacts-spectral` configuration, seed0,
60seconds, on all34distinct original017readiness inputs and the same idealZ12.
Keep singleton policy `legacy`:033did not support promotion of direct singleton
relocation. The only algorithm source change relative to033's frozen control
is035's corrected class-interval assignment in `field.py`, plus narrowed
docstrings. Freeze the complete source, original source/target bytes and task
matrix before execution. No graph filtering or favorable-seed selection.

Use the local isolated native environment and one worker at a time, with the
pilot's existing cold per-task JIT cache, thread settings, original-graph
validator, forbidden-library checks and process watchdog. The local033controller
and its final worker are already verified absent. This experiment contains
34new calls, no MM comparator and no independent alternative constructor.

## Comparisons and their limits

Primary paired quality reference: all34spectral-control observations from033
on the same local host. Revalidate those original embeddings and preserve their
artifact hashes. Report every per-input status, qubit/ACL delta, constructed and
pruned counts, converter misses, completion counts, and final refinement result.
Do not score partial output or a timeout as a short valid embedding.

The reference observations were collected before this corrected run, rather
than interleaved. Solver/process timing ratios are descriptive across-run
observations and may include host-load/time effects. They are not a controlled
same-run timing estimate of the converter change. The paired untraced tiny-line
timings in035 isolate that operation but cannot establish full-pipeline speed.
032's hyde03timings remain separate; no local/remote timing ratio is formed.

Report unchanged versus changed upstream layout diagnostics. Differences before
conversion can arise from numerical or deadline effects and prevent attributing
every downstream difference solely to class assignment. Same seed is not a proof
of identical floating-point trajectories. Physical validation and source/config
identity checks are required regardless of observed quality.

If comparison with historical MM quality is included, retain its original run,
seed and success/timeout statuses explicitly. These development sources are
already observed; no outcome estimates generalization to new family instances.
One seed cannot estimate across-seed ACL variance.

## Decision and self-critique

The recurrence correction has a validity/optimality argument for its restricted
class-interval problem. A corrected assignment can change seating, completion,
pruning and later contacts, so it need not improve final ACL. More seated arms
may increase raw qubits while eliminating an incomplete conversion; that is
coverage, not a quality regression by itself. Final valid qubits are decisive.

Keep all regressions, failures and time overruns. A substantial state/time
increase in035requires review before launch; no arbitrary DP state cap may be
silently inserted into an exactness claim. The pipeline watchdog bounds exposure
but does not turn a late call into timely success. Correct class feasibility
does not fix missing endpoints, defective lanes or every converter limitation.

Do not use this result to choose old versus corrected conversion per input.
Any next revision is one globally fixed algorithm. If the corrected solver
reduces final quality broadly, investigate the mechanism or replace the class
solver with a separately specified feasible heuristic; do not hide the known
capacity defect inside a preferred-family exception.

Status: protocol saved. Source freeze and launch await completed035review.

## Review before freezing

Root reviewed the completed035 report and full source diff. The corrected helper
agrees with257 declared oracle cases and a separate431,901-case exhaustive root
check; all14 feasible physical fixtures are seated, and both infeasible fixtures
retain safe partial output. The109-test integration run plus276 subtests passes.
On the difficult nine-arm Z12 witness, peak states rise30 to240 and mean warm
time0.293 to1.816 ms. This6.21-fold slowdown is material, but the observed absolute
cost supports this bounded full-pipeline measurement without a new state cap.
No broad runtime guarantee follows. The source freeze will use final field SHA
`a055553988ba3db17ef3588986c1719070c3fcfae4bbfcce7a7084ffe45c7690`.

## Frozen source and verified launch

Independent preflight passed all46 source files per revision, all37 unchanged
source/selection/target records, all34 exact control configurations, and absence
of forbidden embedders in the isolated native environment. Only `field.py`
differs from033's frozen source. Before launch all output/cache directories
were empty and the prior local controller/final worker were absent.

Frozen commit: `18ab7590e1267c6281e4c8c4443fa00b73887228`.
Source: `56338bafc7038d75001fe9fd36b067f246dc13ab796a2fdd29ded59b3226f243`.
Target: `38cde794d3c1461054a45b5a660d157737b019c19cb9a7b38e2027730e3d5938`.
Preflight file-map digest:
`5428dc63d9a38e4b05ac4f5bffb34951a9d666b2790f361cd644f42489813721`.

The controller started at2026-09-08T04:05:41.556109Z. Root verified controller
38178 and native worker38191 alive at04:05:48UTC, under exec session16497.
No result had finalized at that first check. Preflight evidence is in
`results/codex/036-independent-review`; stdout/stderr and actual OS liveness
are in `results/codex/036-launch`. Root owns process monitoring; an observation
timeout is not terminal status or permission to restart. Independent final
quality analysis begins only after verified completion.
