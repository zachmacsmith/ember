# A066 complete screen: exact pruning and paired shortening

2026-09-10, before execution. Root reviewed both isolated production files, unchanged donor preparation/reconstruction/commit contracts, all four focused test groups, and14 frozen bindings. An independent static peer review found one post-admission receipt-status edge, corrected before the first frozen test run. Checks passed once; no development constructor calls have occurred. [Algorithm contract](../tracks/a_066_constructor_contract.md) gives hypothesis, pseudocode, self-critique and exact pruning proof; [implementation](../tracks/a_066_implementation.md) preserves source and check identities.

Freeze `066-paired-boundary-transfer` on hyde06, idealZ12, Transfer003. Exactly nine development encodings in order: g0001 ER80, g0006 planar80, g0013 grid128, g0017 singleton control80, g0201 WS100, g0202 SBM100, g0021 grid relabel, g0102 BA100, g0203 planar100. These are eight structures; the last two are new to this inherited refinement sequence but exposed by other tracks. All six diagnostic structures, including four negatives, remain. No untouched confirmation or across-seed evidence is claimed.

Five separate arms: A066 `native-paired-boundary`, constant pruning-only `native-pruned-boundary`, unchanged A065 `native-compiled-boundary`, retained A061 `native-branched-path`, pinned MM. Seed0,60 seconds each, exactly45 calls, existing isolated native/MM environments3c7aa36653394e61. Pair timing on06 using shuffled sequential method order. Each candidate starts with one fresh A061 construction and evolves that one state; no saved diagnostic or MM map initializes it, no outputs are combined.

Retain the original global deadline and min(1s,0.05×timeout) final reserve. All packing, exact domains, certificates, compilation, discarded queries, invalidation, cleanup and output stay charged. Counters do not stop search. An exact-empty domain closes only a proved futile query until the next state epoch; this is a logical bound rather than an artificial work cap. Record pair/pruning time, precise changed chains, exact-obstruction counts, pair admissions and global cache invalidation. Missing/interrupted receipts remain unknown. A066/pruning differ by one boolean, never adaptive selection between complete algorithms.

Reuse the independently audited original-graph validator and the reviewed isolated comparison-label adapter; only input scope/module identity changes. Complete success/Q/ACL/within-chain variance and every failure/time row remain authoritative. Across-seed variability remains unknown. Report all paired A066-versus-pruning and A066-versus-A065 regressions; mean family gains cannot erase an input loss.

The frozen continuation criterion requires preserving every paired A065 and pruning-only success, no Q increase against either on common successes, and lower final Q than pruning alone on at least two distinct structures. All MM deficits remain explicit; these inherited deficits are not redefined as new policy regressions. If pair moves occur without final gains over pruning alone, stop this integration. If only pruning helps, record that result and retain the causal distinction. Any subsequent bounded follow-up must explain a completed failure specifically; local witness count or more completed visits alone is insufficient. No universal runtime ratio gate; roughly-MM-order runtime with quality first remains the objective. Root owns one stage/start, terminal retrieval and original audit. Stop after complete results for the next mechanism decision.
