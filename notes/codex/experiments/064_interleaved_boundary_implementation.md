# A064 implementation and focused checks

2026-09-09 UTC, before the first check execution. Root authorized implementation
under [policy](064_interleaved_boundary_policy.md), corrected before code to
`355fd8f93f8826ee99b42203ab7dfa5c87ec8002633a54eb05cb8ecc61c09822`.
The original draft and the screen-only correction binding are preserved in
`results/codex/interleaved-boundary-checks/`. Root's nine inputs include grid;
wheel remains an untested transfer/variance risk for the next broader screen.

Only two isolated modules implement A064. The scheduler imports the unchanged
A063 context, meter and stop exception; its graph normalization, seeded ranks,
donor protection, root ranking, shared-tree reconstruction, certificate and
bundle methods are reused directly. The wrapper function body is unchanged
from A063 except its public name and algorithm/version metadata; its operator
import points to A064. A061 dispatch remains exactly once under the original
deadline. No default, shared constructor, validator or harness file changed.

One complete root is attempted per owner round slot. Per-owner query domains
remain cached within one incumbent epoch. A commit invalidates all caches and
closures, preserves the next round slot, and prepares an epoch-close event
before the atomic incumbent publication. Only same-epoch closure of every
owner can end the search normally. No no-commit-round, root-prefix, distance,
work or memory gate was added. The tiny test's controlled query stubs test
schedule semantics only; returned tiny maps also pass the unchanged independent
oracle, and real P3-in-P5 donor transfers exercise actual publication/invalidation.

Live cache accounting uses seven fixed counters, with current/peak payload
counts and the existing worker's process RSS for actual memory evidence.
Per-visit state copies contain these fixed-size counters, never the whole
incumbent. Full embedding copies occur only on strict-Q candidate certification,
as in A063. Large cache arrays are absent from diagnostic history. Query wall/
CPU sum active visits; parked elapsed spans remain separate. Discarded cache
storage is cleared under a measured disposal phase even when the deadline has
already expired. Pre-check source review caught and corrected one reporting
issue: final cleanup must not overwrite the original deadline stop phase when
it also observes expiry. No check or candidate had run before this correction.

Exactly four declared new-risk groups are in
`results/codex/interleaved-boundary-checks/checks.py`: deferred-root/exhaustion
semantics; commit rotation/epoch invalidation; cache/publication deadlines and
stale-state fatal errors; and active-cost/payload accounting plus wrapper/import
binding. The independent oracle and AST-extracted original validator are reused.
The no-external-embedder guard is extracted from the already frozen A063 pilot
snapshot, avoiding incidental registry edits during parallel track work. Actual
A061 calls are stubbed; the real wrapper is imported only. There are no corpus,
new-panel, MM or complete-constructor calls in these groups.

Freeze source, checks and reference hashes in `preparation.json` before the
first invocation. Each attempt has its own invocation, stdout, stderr, outer
status, checks log and summary. Preserve all failures and fixes; rerun only
affected groups if needed. Root reviews this evidence before panel execution.
