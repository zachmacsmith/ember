# A063 implementation and focused checks

2026-09-09 UTC. The accepted [policy](063_mobile_boundary_policy.md) is preserved
unchanged at `523e073288537fc9e14c78e08a0308d8113796c1e3b47c39f193e53b698f548b`.
This record precedes the first check execution. Only the two isolated
`factored/mobile_boundary_*.py` modules and reviewer-owned check/evidence files
are new. No shared native, A053/A061, branch, pilot or existing validator changed.

The wrapper calls unchanged A061 once and uses the remaining original wall
allowance, with a one-second finalization reserve for the 60-second screen.
Cooperative stops retain only an earlier certified map, followed by the existing
original-label validator under the original deadline. Internal errors return no
creditable embedding; the last map is diagnostic. All preprocessing, root search,
reconstruction, copying, certification, index refresh and receipt preparation
are timed. Work counters never deny work. Source/target tie ranks use distinct
fixed seed namespaces. The public descriptor is
`mobile_boundary_construction.py:mobile_boundary_embed`, empty config.

Owner/contact indexes use one occupied-adjacency scan. Reciprocal physical
witness endpoints are protected before simultaneous neighbor pruning. Every
old focus-chain site must remain reachable from all reduced neighbors after a
completed root search. Full-domain distance sums rank roots; actual total Q,
including changed neighbors, controls admission. BFS extensions start from the
whole growing tree and can satisfy several contacts with shared sites. The
original constraints remain authoritative; no fill or pending-lift requirements
enter this completed-state operator.

For d neighbors, root generation performs d BFS traversals of the free target
domain, with O(target vertices) transient distance storage plus aggregate arrays.
Each attempted root can perform up to d shared-tree BFS traversals; all eligible
roots may be tried. There is no speculative root/work/pass cap. After each Q
decrease, later visits use the updated incumbent; a complete no-gain pass ends
the deterministic search. Full original validation is still potentially costly
and is reported separately, rather than claimed linear because the witness
index is linear. Unsuccessful root reconstruction has separate per-query wall/
CPU totals from full-target root generation and smaller-Q reconstructions.
These nested subtotals must not be added to the exclusive stage totals again.

One test-description clarification is required by the already declared policy:
in the shared-junction fixture, root0 initially grows T={0,1}, but the subsequent
contact-preserving prune removes unprotected root0 and returns {1}. The check
asserts one shared extension/junction and the final singleton. It does not
disable pruning to preserve the sketch's intermediate tree. No algorithm policy
or numerical parameter changes for this clarification.

Before-check ordering clarification, accepted by root: `qindex` is the canonical
rank in supplied target-node order; it is not an original numerical node label.
Canonical ranks choose the reciprocal physical witness, donor anchor/spanning
root, leaf-pruning order and returned site order. `qtie` is a separate once-seeded
permutation rank used for BFS adjacency/source ordering, ranked-root ties and
extension endpoint ties. `vtie` similarly orders source neighbors and owner-pass
ties. Logical-edge orientation uses canonical source rank. This states the
intended distinction between the policy's "target-rank" and "seeded target-rank"
clauses; root flagged ambiguous wording, not a validity bug. No search choices
or accepted policy bytes changed, and no checks had run before this clarification.

Exactly six new-risk groups are in
`results/codex/mobile-boundary-checks/checks.py`: actual-Q stem transfer;
shared-junction reconstruction/pruning; reciprocal/cyclic/articulation/singleton
donors; no-gain labels/order/immutability; interrupted/fatal publication paths;
and wrapper dispatch/import/failure/timeliness. They use only fixed tiny graphs,
the AST-extracted unchanged original validator and existing saved-graph oracle.
All A061 calls in wrapper execution are stubs. The real wrapper check imports
only, using the audited no-external-embedder guard and native-environment absence
check. No corpus, generated panel, saved-state diagnostic, MM or constructor
call is part of these checks.

Source/check/reference hashes are frozen in
`results/codex/mobile-boundary-checks/preparation.json` before invocation. Every
attempt gets a fresh directory, logs, summary and outer invocation status. Any
failure is preserved before correction. Result evidence will be recorded
additively after the declared check run, then root reviews before any screen.
