# A062: dependency-ready lifting, fixed complete-constructor screen

Prospective plan accepted with the [exact policy](../ready_frontier_lifting_hypothesis.md)
and [before-code accounting clarification](../ready_frontier_lifting_accounting.md).
No physical candidate call precedes runtime freeze. The six tiny new-risk groups
check logical R edits, recorded dependencies, deterministic keys, no retry,
rollback and bounded selection publication; they are not a reach screen.

One fixed change to inherited construction: choose among rows whose full
recorded filled-neighbor dependencies are placed, minimizing the number of free
sites adjacent to every such neighbor, then legacy reverse index. A zero count
does not establish insertion infeasibility. Reducer/core/transfer/ordinary/
blocked repair/acceptance are unchanged; the measured branched-path stage follows
unchanged. Scheduler work shares the original construction budget/deadline.

Use HYDE06's existing `3c7aa36653394e61` native/MM Python3.11.2 environments,
one thread and a fresh process/empty task JIT cache for each call. Exact input
order: `ember_34404`, `ember_2430`, `ember_1584`, `ember_32367`, `ember_1829`,
`ember_4756`; seeds0/1, 60s, methods supplied as `native-branched-path`,
`native-ready-branched-path`, `mm`, all `{}`. The unchanged per-input/seed
`Random(f'{graph}:{seed}')` arm shuffle creates36 serial calls. Root owns the
one runtime freeze/stage/start and terminal verified retrieval.

Four inputs reuse the exact042/061 originals, normalized records and labels;
wheel2430 and hypercube4756 reuse the audited057 additional provenance. All six
derive from the same immutable012 ledger. The combined selection only selects
these existing entries; it generates no graph or new label mapping. Target
bytes are identical to042,057 and061. Family metadata stays evaluator-only.

The fixed gate requires no lost fresh A061 success, no per-structure mean-Q
regression on common successful seeds, at least one fresh control lifting
failure recovered, a Q gain on another noncycle structure, and no >10× fresh
A061 solver/process ratio. If known control failures do not recur, prevention
remains untested; no extra seed or substitute input is added. A timing/error
or incomplete original validation cannot supply a gain. MM results are fresh
same-host comparisons with every failure and time retained. Historical057/060
failures are context, never substituted control outputs.

Report all36 statuses, Q/ACL/max-chain/within-chain variance, CPU/solver/process
times, original-label validation and class/seed MM gaps. Separate changed base Q
from unchanged branch-stage savings. Reconstruct trial/partial R using actual
committed journal indices; verify readiness, unique fill deletion and final
R=original. Keep complete/partial selection receipts and conservative word-work
charges distinct from completed bigint operations. Empty/one-ready keys stay
null where uncomputed. Scheduler wall is inside inherited construction wall.

The mechanism discriminator is actual readiness/order divergence before a
different complete result, with unchanged physical moves and admission rules.
Native-core failures remain out of scope; hypercube keeps that limitation
visible. Few direct options can mean a cheap longer-chain insertion, and early
fill release can remove helpful geometry. Negative or costly complete outcomes
end this fixed policy; no key, cap, input or route tuning follows. Positive
results would remain exposed six-record evidence requiring separate broader
measurement, not a class-population or universal MM claim.
