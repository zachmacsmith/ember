# 054: focused comparison of degree-two and degree-three reduction

Exploratory protocol, before this screen's candidate outcomes. The
[pre-code hypothesis](054_degree_two_reduction_hypothesis.md), source-only
comparison and eight-call reach gate precede this experiment. All reach
outputs validate. The three unchanged reductions reproduce their controls;
wheel improves191→190 Q while solver wall rises6.000→8.045s. This mixed
result supports measuring the changed cores, not promotion.

Select every existing053 input whose core or journal differs between the
two thresholds in `a054-degree-two/source-comparison001/summary.json`:
13 structures. Add the first three unchanged structures in original manifest
order: `ember_22972`, `ember_2229`, `ember_4755`. Preserve original relative
order throughout. This fixes16 structures/17 memberships from reduction
differences alone; no054 corpus embedding outcome enters selection. Save
the complete34-row inclusion/exclusion ledger before freezing the run.

Run fixed053 `native-site-transfer` and054 `native-degree-two`, both `{}`,
once per selected input: seed0,60 seconds,32 independent processes on hyde03.
Use the audited paired task ordering, Z12 target, worker/import guard and
detached controller. Preserve exact042/053 graph, target and selection bytes;
the harness already supports a declared subset of the unchanged selection.
No MM call, new graph, alternate output, changed allowance or family dispatch.

The purpose is a quick threshold ablation. Report all16 per-input outcomes,
the13 changed inputs and three controls separately, original full/partial
validity, Q/ACL, work/cap stops, solver/process time and same-host pair ratios.
Unexplained control differences require investigation and remain recorded.
The18 omitted inputs have no new054 embedding observation: do not impute
their quality or report a new34-input mean. Historical048 quality, if shown,
uses only this same selected set and no historical timing.

Reuse the original042 embedding oracle without editing it. Its all34-input
identity check may run against the immutable053 archive; require exact byte
equality for every selected new graph and target before using those original
label maps. Reuse the existing journal/repair/transfer receipt checks, with
the independently reconstructed threshold2 journal and identity admitted for
054. Retain every failure, timeout, valid late output and missing result.
Intermediate unsaved chain/pool states are not reconstructed or inferred.

A coverage loss or unfavorable quality/cost tradeoff rejects this fixed
threshold policy. A useful improvement warrants a subsequent full-scope
check; it cannot establish all-class superiority, seed variance, novelty or
MM-scale runtime. No post-outcome subset, threshold or cap adjustment. Observe
the same detached handle after connection loss; failed observation never
authorizes restart.
