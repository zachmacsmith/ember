# A063 focused implementation gate

2026-09-09 UTC. All six predeclared check groups passed on the first execution
in 1.231 seconds, with zero errors/failures and no attempted prohibited imports.
All eleven source, policy, validator and harness bindings remained unchanged.
The isolated native environment has no minorminer, _minorminer or busclique.
No panel, remote job, saved-state diagnostic or complete constructor was run;
the new local operator was exercised on tiny supplied maps, A061 was stubbed
in wrapper execution, and the actual wrapper was imported without calling it.
The invocation's broad “no candidate calls” field should be read in this precise
scope, not as saying the tiny operator itself was never executed.

| Declared group | Observed evidence |
|---|---|
| Actual-Q stem transfer | Hardware path-five/source path-three improves Q5 to Q3; total-Q receipts include neighbor changes. |
| Shared junction | One tree extension meets three neighbor obligations; contact-preserving prune returns the shared singleton. |
| Donor safety | Reciprocal witnesses survive simultaneous trimming; cyclic, articulation and singleton donor cases remain connected and nonempty. |
| Stable state | No-gain return, arbitrary labels, deterministic ordering and untouched input pass. |
| Publication boundaries | Interrupted copy/ranking/tree/certificate/index/precommit paths reject unfinished changes; an earlier Q3 commit survives cooperative stop; a later fatal error returns only a diagnostic map. |
| Wrapper | One inherited call, final validation and actual return clock govern credit; failed/late/fatal maps stay uncredited; audited import guard passes. |

The canonical-index versus seeded-tie clarification was saved before checking,
without source changes or changes to the accepted policy bytes. The shared-junction
check also distinguishes its intermediate tree from its already-specified final
prune. Neither is an algorithm correction or an outcome-selected policy change.
The original policy and the before-check implementation record remain frozen.

The public adapter is `mobile_boundary_construction.py:mobile_boundary_embed`
with `source, target, *, seed=0, timeout=60., deadline=None` and empty config.
It calls unchanged A061 once, then the mobile-boundary operator on that incumbent.
There is no new work/root/pass denial cap. All-target root generation and failed
root reconstruction have separate event and wall/CPU receipts; nested query totals
must not be added again to exclusive stage costs. Full certification cost remains
explicit and may dominate. These tiny checks establish the stated new invariants,
not broad reach, runtime equivalence to MM, or an improvement guarantee.

Evidence: [summary](../../../results/codex/mobile-boundary-checks/attempt001/summary.json),
[check log](../../../results/codex/mobile-boundary-checks/attempt001/checks.log),
[preparation](../../../results/codex/mobile-boundary-checks/preparation.json), and
[final manifest](../../../results/codex/mobile-boundary-checks/manifest.json).
Root owns the pending complete-constructor panel and registry/transport decisions.
