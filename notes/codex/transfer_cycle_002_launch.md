# Transfer002 research decision and launched screens

2026-09-09 22:02 UTC. **A061 remains retained.** Its complete
[class table](mm_gap_retained_a061.md) and every previous regression remain
unchanged; no new all-class, across-seed or confirmation claim is supported.

The completed B028 diagnostic distinguishes computation from query semantics.
All six complete route projections and attachments exactly match B027, and
both cold processes finish successfully. Root reviewed the reader and verified
31 archive bindings. The first compilation and small-query regressions remain:

| Measured scope, same hyde02 host | B027 seconds | B028 seconds |
|---|---:|---:|
| Full cold packet | 15.777 | 14.887 |
| Full process | 19.31 | 18.56 |
| Five postcompilation queries, routing and adaptation | 9.374 | 2.454 |
| Same scope excluding K100 | 2.424 | 1.154 |

Compilation dispatches cost5.867s, initial typed allocation1.068s and lazy
import0.633s, all charged. Grid/honey queries are slower in this packet because
each diagnostic engine repacks Z12; a complete constructor packs once but still
pays every per-query allocation/conversion. This is a reason to test the whole
constructor, not a speed or quality claim against MM. The
[six-input protocol](tracks/b_028_constructor_screen.md) freezes24 separate
B028/B027/A061/MM calls on02, seed0/60s. It requires recovered complete successes
across families, including a newly generated instance, before broader testing.

A064's36-call A064/A063/A061/MM screen is live on06. Its latest observation
shows20 finalized calls and a live controller/worker; those statuses have not
yet received the independent output audit. The
[prepared passive reader](experiments/064_interleaved_boundary_analysis_preparation.md)
will distinguish wider owner coverage from improved complete Q, and account for
invalidated work. The input-only initializer and output adapter passed source
review; post-copy checks bind the actual runtime source to reviewed hashes.

C013 is rejected: its complete tiny star22 call stops at20 introduced singleton
chains, and all-owner rebuilding repeats the exclusion. Four other positive
tiny controls achieveACL1 and the impossible control fails as expected; all six
calls remain. **Correction:** actual Z2 maximum degree is19, not20 as stated in
the preserved C013 note/frozen protocol; actual Z12 maximum is20. Independently
validated evaluator-only witnesses prove star22's optimumQ23 on both and K2,10's
optimumQ12 on Z2. Root independently rechecked all three maps and vertex-count/
degree lower bounds; no candidate receives a witness.

This explains a concrete missing capability: a source hub may need a larger
branch set before all of its neighbors have been introduced. More retraction
cannot help if placement recreates the same forbidden shape. The separately
reviewed [C014 policy](tracks/c_014_capacity_policy.md) permits connected growth
and requires enough distinct free boundary sites for remaining original
neighbors. This could address hub and crowded-contact deficits through one
general mechanism, but transfer is unproved. Only its isolated implementation,
focused checks and two tiny complete controls are authorized so far; a larger
screen depends on those outcomes. Stop C013's singleton-first policy and retain
the failed C012 frontier policy as rejected.

Both launched complete screens use snapshot
`6e27c49abfb07a431e8b8c93896c45632f6ca672c102fab4216e5bbcd36bf9ca`.
A manifest is `ae16c4249b92ef81a7bc5262263111bc143ae61eb91d8edef64bd9996f303512`;
B manifest is `14faf948c02b6c553fe3a4893f02e7cdcd541dd80c3d0d760d8b3e99d4d9f5b8`.
Each initialize/stage/start action ran once. Detached tmux/watchdog identities,
task vectors, logs and observations are under `results/codex/transfer-cycle-002/`.
No restart, pooled output selection, MM-derived initialization or candidate
busclique/MM dependency was introduced. Complete all outstanding calls before
interpreting either fixed policy or refining it.
