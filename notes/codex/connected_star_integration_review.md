# Root review of connected-center integration

2026-09-08. The integration gate passes. This accepts an experimental mode for
the predeclared 039 comparison; it does not promote the mode or establish a
quality, speed or novelty claim.

Root read the complete production diff and new integration tests, checked the
author's 36-test and 160-test execution records, and independently ran the new
36-test suite in a fresh output directory. The repeat passed, source hashes
were unchanged, and the import guard recorded no MM/busclique import attempts
or loaded prohibited modules. Artifacts are under
`results/codex/connected-star-integration-checks/root_focused/`; the runner
records source identities and invocation details. Its total audit wall was
3.203109 seconds, which is not a solver benchmark.

The factory constructs exactly one selected search object. The connected mode
uses the existing failed ordinary visit and remaining shared allowance. It
does not invoke the matching proposer, add visits, or impose the matching
core's separate 2,048-unit query ceiling. Ordinary accepted moves, including
equal-Q redundancy improvements, suppress the auxiliary proposal. The final
deadline check precedes publication and committed Q/R/member-growth counters.
Cache refresh follows publication and cannot invalidate an already certified
incumbent if refresh exhausts its allowance.

The tests exercise real connected commits, signed redundancy loss accompanying
strict Q improvement, actual growth of the center, interruption before commit,
interruption during refresh, and an actual Z12 degree-22 center. An actual
native spectral/Z12 smoke call verifies final original-label validity and an
explicit isolate. Eight historical off/matching comparisons against immutable
commit `6044e200ff36c8585938e8113189d9c3200c8a4b` agree after removing time
measurements; the matching cases include active queries and commits. These
checks supplement the independently audited frozen connected core.

The implementation and test hashes in the author's
[implementation note](connected_star_integration_implementation.md) equal the
root execution record. A machine-readable copy of that reviewed six-file map
is saved as `results/codex/039-protocol/reviewed_integration_hashes.json` for the
independent experiment preflight. The integrated source must be committed
before the pilot freezes it, and preflight must bind that revision and these
hashes to the complete source/input/task/environment freeze.

Remaining limits: a single-root search can miss feasible improvements; strict
local Q savings can displace more useful ordinary work; fixture correctness
does not predict aggregate final ACL. The all-input experiment must report
regressions, unsuccessful auxiliary work and incomplete searches. No family
selection or post hoc work-allocation change belongs in 039.
