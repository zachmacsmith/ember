# Transfer001 complete-constructor screen — frozen decision rules

2026-09-09 UTC, before any candidate call on the new panel. Use the22 sanitized
encodings in `results/codex/transfer-cycle-001/panel.json`:20 structures and two
nested relabelings. Every declared generation attempt succeeded. The independent
input review checked originals, bijections and all four feasibility witnesses;
the12 generated structures have no match in the declared490-file exposed index.
That bounded index does not certify global novelty. None is confirmation data.

Each track has66 separate calls, seed0, common60s outer solver envelope, empty
configuration, one fresh process/task, existing isolated native/MM environments:

| Track / host | Declared method order before per-input shuffle |
|---|---|
| A063 / hyde06 | native-mobile-boundary, native-branched-path, mm |
| B026 / hyde02 | mobile-tree, native-branched-path, mm |
| C012 / hyde03 | zephyr-frontier, native-branched-path, mm |

Use the audited initializer's `Random(f'{neutral_graph_key}:0')` method shuffle
and the frozen panel order. The input-provider adapter changes initialization
data only; graph/task/source digests and exact198 intended calls are checked.
Do not pool reference timings across the three hosts or select the best output
across tracks, seeds or relabelings. Each candidate is a separate general policy.
The same-host A061 reference is one fixed retained algorithm, not a portfolio.

Carry the audited worker's no-external-embedder guard, absolute deadlines,
process supervision, existing graph validator and independent saved-output oracle.
For each output, independently check the solver-label minor, then map owner keys
back using the evaluator-only bijection and validate the original graph again.
Only complete, timely, error-free outputs receive credit. Missing, failed,
invalid, crashed and late calls remain in every denominator and all-attempt
solver/CPU/process accounting. A failed known-witness control is still failure;
its witness only rules out target infeasibility. Do not supply it to a candidate.

Report per input: status; Q/ACL on valid outputs; maximum and within-embedding
chain variance; solver/CPU/process time; candidate/control/MM deltas and ratios.
Across-seed ACL variance is undefined in this one-seed screen. Show paired
relabel changes separately, never two independent graph observations. Distinguish
four controls, old anchors, dense sentinel and fresh generation cases in evaluator
reports only. Do not impute ACL to failed calls. No aggregate hides a regression.

The mechanisms have different exploratory questions:

- A063: does changing original-contact boundaries lower complete-outputQ beyond
  A061 on fresh structures? Reconcile baseQ with the fresh control and new-stage
  receipts; report any differing base rather than attributing it to reconstruction.
  A continuation signal requires at least two fresh structures from at least
  two generator families with lowerQ, no lost control success and no common-pair
  Q regression. Report MM comparisons even when A061 improved. The stage is not
  promoted by a gain on the same anchors alone.
- B026: can its own one-state whole-chain construction reach compact valid
  embeddings with exercised boundary mobility and shared paths? A continuation
  signal requires original-valid lowerQ than A061 on at least two fresh structures
  from two families, with at least one also no worse than timely MM. Failures and
  regressions elsewhere block promotion and require a specific explanation for
  any bounded mechanism follow-up. Tiny success or overlap reduction is inadequate.
- C012: does simultaneous birth/contact scheduling produce compact complete
  embeddings beyond repeated local examples? Use the same fresh-structure benefit
  signal as B, and separate contact-creationQ from carryingQ. A finite sweep that
  exhausts ports or grows long carried chains without fresh complete-output benefit
  rejects this policy. A feasible witness alone does not locate the failed cause.

These are exploratory continuation signals, not all-class promotion gates.
Failure remains recorded as failure even if a mechanism receives one bounded
follow-up. Such a follow-up must identify representation, neighborhood, acceptance
or cost as its target explanation and state the observation that could refute it.
Actual generated proposals, admission decisions, exhausted domains and interrupted
work distinguish these cases; no conclusion is drawn from an unvisited alternative.
Do not chain repairs solely because a partial count improves.

No universal3× timing gate applies. The60s envelope deliberately permits quality
investigation. Report per-stage and complete costs, time to first validity and
incumbent quality progress where recorded, plus all repeated>10× solver/process
rows as cost deficits. A60s result on subsecond-MM input remains expensive.
No claim that abstract event counters equal CPU instructions or equal work across
tracks. Existing A061 internal limits remain its fixed historical policy;
new operators have wall boundaries and diagnostic counters. Any later allocation
experiment has a separate frozen identity and preserves this panel's outcomes.

Before launch, root reviews each isolated candidate and only its new-risk checks;
the unchanged worker/supervisor/validators are not exhaustively retested. Exact
source snapshot, run/task vectors, raw inputs and private/public separation are
bound before staging. Detached supervision survives a lost connection; a failed
observation never authorizes a second launch. Retrieve only terminal quiescent
runs and reuse the original oracle for the one saved-output analysis.

Promotion over A061 remains a later per-class, repeat-seed decision that resolves
observed regressions and runtime deficits, followed by separate untouched
confirmation. All-class objective, idealZ12, no-MM/no-busclique, source generality
and one-state/non-portfolio constraints are unchanged.
