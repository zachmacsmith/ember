# Next constructor screens: active footprints and joint stars

2026-09-10 UTC. **Before candidate execution.** Root has reviewed and authorized isolated A068 and C019 implementations and their focused risk checks. Source review precedes the first check invocation; successful checks and a coherent source/input freeze precede remote launch. B's distinct local Zephyr deformation remains design work. The completed [previous-cycle decisions](mechanism_decision_2026_09_10_completed_screens.md) remain unchanged, and A061 remains retained.

## A068: contact-supported native footprint

The [before-code design](tracks/a_native_footprint_review.md), SHA256 `03c9576f0e24bb83e320e81e0430ad8767ffa21d0127c29b917e169be37896b5`, targets a construction representation/packing constraint. The inherited layout reserves both arms even when one carries no contacts, and forces a sole active arm to reach an unused corner. The proposed coherent change removes those claims from support sets, packing, interleaving cost and physical conversion. Dual-arm owners retain their actual corner connection; isolates require a free physical site. This is different from deleting redundant sites after conversion.

Four focused groups cover actual single/dual-arm and isolate geometry; independent tiny interleaving-cost enumeration; consistency between support, packing and conversion plus the booking ablation; and interrupted preparation/publication. Reuse existing hardware helpers, independent oracle and dependency guard. Do not repeat the old exhaustive converter campaign or run development constructors as informal probes.

Proposed run `068-contact-footprint-transfer`, evidence `results/codex/a068-footprint`, host **hyde06**. Inputs are existing Transfer004 g0013, g0014, g0102, g0302, g0309, g0303, g0304, g0307, g0308, g0007, g0011 and g0015: twelve encodings/eleven structures, one seed0, four separate methods:

- `native-contact-footprint`;
- `native-inactive-booking`, with identical new physical conversion and added virtual inactive-arm bookings;
- retained `native-branched-path`;
- pinned `mm`.

This gives **48 cold calls**, each with the original60s outer limit. It is explicitly a **controlled1000-ask experiment**, not a justified default or a measured saturation point. All six reviewed historical cores hit that cap with later improvement unresolved. Both new arms use the same count and record the last improving layout ask/time and every cap stop. They reserve `min(10s, remaining_wall/2)` at native entry for conversion and the remaining pipeline; the observed six complete-call-minus-layout costs were1.756–6.093s. The reserve is a starting empirical allocation, not a guarantee. All remaining wall returns to the unchanged later stages. A binding reserve or deadline is reported, not called equal exposure. No new counter cap or MM multiplier is introduced.

The root's pre-call numerical criterion clarifies the design: no success/Q loss against A061 or the booking ablation; at least one-third closure of a positive paired A061-to-MM Q gap in **two ordinary source families**; and a Q benefit over the booking ablation on two independent structures. Controls remain distinct diagnostic evidence, and the BA relabel adds no family/structure vote. Every individual loss is preserved. These conditions are necessary exploratory evidence, not all-class promotion. A later ask-cap allocation test is conditional on completed evidence and requires its own freeze; it is not authorized automatically here.

## C019: joint compact star construction

The [exact before-code contract](tracks/c_019_joint_star_contract.md), SHA256 `f996952fdb1db4f8ab94d70abbd3adf6f66f2f5d562f96e7b571a69797b27db9`, targets coordinated placement before long chains form. One center and an independent subset of its current singleton neighbors move together. A full-target proxy scan selects one root; one connected growth trajectory evaluates exact conditional weighted leaf assignment. The old internal source-edge gaps enter acceptance cost. Long chains are not collapsed through leaf selection. All original owners remain represented by connected, disjoint chains; contacts may be missing before first validity.

Use one guarded focused packet: tiny weighted assignments checked by direct enumeration, including competition/deficiency/deleted boundary sites; an actual-coupler branching block with a protected long chain; and atomic interruption plus post-incumbent fatal error. Reuse the original oracle and guard. Weighted matching is a polynomial local subroutine, not an embedding IP; A039's cardinality routine is not misrepresented as weighted matching.

Proposed run `c019-joint-star-transfer`, evidence `results/codex/c019-joint-star`, host **hyde03**. Existing Transfer004 inputs g0013, g0302, g0304, g0305, g0307 and g0309, seeds0/1, four separate methods:

- `joint-star-construction`;
- `cardinality-star-construction`, using the same assignment routine with zero real assignment costs while evaluating actual proposal/acceptance costs;
- retained `native-branched-path`;
- pinned `mm`.

This gives **48 cold60s calls**. Only the common wall deadline and documented final reserve limit search. Root, growth and quality-policy termination are explicit proposal rules, not claims of exhaustive neighborhood search. All setup, compilation, rejected proposals, partial/interrupted work, cache costs and final validation remain charged. The latest certified state is retained after an accepted equal-Q/R improvement, although strict-Q trajectory events are recorded separately.

The contract requires timely validity on all12 candidate calls, no row-wise final-Q loss against A061, and substantial complete gains in both seeds on two original structures: one grid/WS/control and one BA/SBM. On each qualifying matched seed, close at least one-third of a positive A061-to-MM gap; if A061 already ties/beats MM, improve A061 instead. Optimal ACL-one ties remain permitted. Evidence for weighted assignment additionally needs a complete Q gain over cardinality-only on a qualifying structure in both seeds. Strong tied outcomes can support studying the simpler algorithm separately, never selecting outputs per input. First-valid Q/time are diagnostic, not an automatic veto of prompt strong final results.

## Shared interpretation and execution

Both screens use the already reviewed Transfer004 panel and original labels, with no new input generation or private witness access. These are development inputs; no untouched confirmation set exists yet. Candidate sources receive no family labels, comparator results or saved embeddings. No MM/busclique call, dependency or fallback is allowed. Each candidate evolves one algorithm state; comparators and ablations are separately measured and their outputs are never combined.

Reuse the audited pilot, freezer, original oracle and multi-seed analyzer. Registration/allowlist additions may be made once entrypoints exist; scheduling, validation, isolation and failure-credit rules remain unchanged. Freeze each coherent source snapshot and every input/method/seed task before staging. Keep timing pairs on the named host; host load and all solver wall/CPU/process costs remain visible. Detached supervision survives network changes, and observation failure never authorizes a restart.

Report complete success, mean ACL with credited support, across-seed variability where measured, within-chain variance separately, time to observed core/full validity and incumbent-ACL trajectories. Do not infer missing intermediate maps, unmeasured clocks, counterfactual gains or a safe early cutoff. A fixed failure stays rejected; a specific completed observation may justify one separately documented mechanism follow-up. Intermediate improvement alone cannot sustain another repair sequence. The final all-class objective and roughly MM runtime scale remain unchanged.
