# Transfer001: complete-constructor screens launched

2026-09-09 04:00 UTC. Three separate general policies now test the strategic
review's proposed capabilities. No panel outcome is credited yet. A061 remains
the single retained algorithm and its class deficits remain unchanged.

| Track | Mechanism | Host | Frozen calls |
|---|---|---|---:|
| A063 | Reconstruct original-contact trees and move neighbor contact boundaries after one A061 construction | hyde06 | 66 |
| B027 | Fresh whole-tree construction with mobile boundaries, overlap prices and exact quality-state recurrence termination | hyde02 | 66 |
| C012 | Fresh Zephyr strip-frontier construction with coordinated contact birth and live-port carrying | hyde03 | 66 |

Each compares separately with fresh same-host A061 and MM calls on the same
22 encodings, seed0, 60-second solver allowance. No output selection combines
tracks. A/C source snapshot is `c183efaeec904db944874eb5b805d204b2acce3f454b4c3d95ba81e7255dafc2`;
B's is `51c9da7056f628203b1d9a47a4dfdf7cec2300518c7a95b12cc2ca73a6d3e3d3`.
All three were staged and started once with detached tmux/watchdog supervision.
Launch observations are in `results/codex/transfer-cycle-001/launch-{a,b,c}`.
The controller's remote state, not a disconnected client, determines completion.

Input generation completed all 20 declared attempts once. The panel contains
12 new generated structures from six families at sizes80/160, three exposed
sparse anchors, one exposed dense sentinel, four new known-embedding controls
and two nested relabelings. All inputs are sanitized and relabeled. Original
labels and witnesses stay in evaluator-private storage outside the repository
and remote bundle. The root input review verified all22 source bijections and
all four witness minors, with no constructor calls. No new generated structure
matched the bounded490-file exposure index; all comparisons resolved. This is
development evidence, not an untouched confirmation set or global novelty proof.

Before screen execution, root reviewed isolated sources and targeted checks.
A passed six new-risk groups; its base calls in checks were stubs. B026 passed
its focused checks and three feasible tiny full calls, but C4 revealed repeated
neutral computation; its infeasible triangle/path call remained a timed failure.
B026's panel was held. The separately documented B027 experiment passed four
focused checks and four paired tiny calls, preserving exact decisions through
the observed recurrence. Root verified25 A,27 B026,27 B027 and22 C evidence
bindings. C passed five groups including five tiny Z2 full constructors.
Tiny checks establish these local properties, not general quality or scaling.

The [B027 amendment](transfer_cycle_001_b027_amendment.md) preserves B026's
original policy and every outcome. It authorizes the pending B replacement only;
already frozen A/C experiments remain unchanged. Four additive registry entries
are the only changes to the audited pilot: all35 previous entries and all other
AST remain identical to4030f0e4. Input adaptation occurs only during local run
initialization. Workers, isolated dependency guards and original validators are
reused. Full source snapshots contain no private maps or witness inputs.

The common saved-output analyzer was reviewed before outcome analysis. Four
static findings were corrected before its first execution: passive-map diagnosis,
missing process-time ratios, interpreter-identity credit and explicit paired
relabel reporting. Its final SHA is
`545a244f64548daf399a82e5496e67b712704ce4737e2f8e0612778719e2cb1c`.
Pre-analysis copies bind both adapter versions and both screen protocols.
No broad infrastructure retest was added for an input-only adaptation.

Next: finish and retrieve quiescent runs, independently validate every original
minor, charge every failure/time, then distinguish useful complete-Q improvements
from representation/neighborhood/acceptance/cost failures. Compare20 primary
structures separately from two nested labels; across-seed ACL variability is
unmeasured. Retain all regressions and per-class gaps. No universal3× gate or
new limit inferred solely from event counts applies.

Checkpoint `b420fb0c` saves the implementation, protocols and launch records.
The staged whitespace check reported two trailing spaces in the already frozen
A063 policy (lines195/207); its hash-bound bytes were preserved. This was a
document formatting finding, not a source or targeted-check failure.
