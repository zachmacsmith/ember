Root accepts specification
`8d870c3a2e16615574ad018eec266258200bd7740b7cbd2450d47a47d299f82f`
for an isolated prototype and focused correctness/cost checks. This is not
acceptance into the native pipeline or authorization of a corpus experiment.
The user has already authorized algorithm development; no additional user
approval is required for this reversible implementation work.

Root read the full revised specification, independent critique and 203-line
standard-library toy oracle. All 15 review-manifest files and four preserved
revision files match. The independent review manifest is
`8b16d8bc193ac8ee519572361cecf0154df73465ab5634f4ca61de89fde0bec7`.
The root verification is saved in
`results/codex/ownership-exchange-spec-review/root_acceptance.json`.

The specification resolves the main avoidable cost: one call shares an entry
copy, ownership map and original-graph validation across a finite ordered group
sequence. Every group uses that same immutable incumbent. Search state resets
per group occurrence and deleted site; work and deadline do not reset. The
routine returns at most one fully validated Q−1 proposal. No cross-call cache,
family dispatch, competitor call, global integer program or portfolio is used.

Root's critique retains three empirical risks. First, complete successor
generation and fine-grained work accounting may dominate the small patch
search. Second, connected-patch admission, chosen-edge restoration and fixed
search limits exclude useful moves; exhaustion proves no global infeasibility.
Third, a local contraction can displace better later reconstruction under a
shared deadline. The existing free-space move must remain an open integration
choice. The prototype must measure total setup/search/certificate cost and
preserve unsuccessful work before any pipeline schedule is selected.

The independent toys establish a reachable two-swap contraction, a deliberate
admission exclusion and the need for shallower-depth revisits. Their 720-assignment
enumeration is an arbitrary-target mathematical check, not Zephyr performance
evidence. Its original-spec binding is preserved; the revised batch semantics
received reasoning review and still require direct implementation tests.

Implementation scope is one dependency-free internal module, focused tests and
an author note. Required tests cover original-edge validity, exact child order,
private inputs/parents, whole-donor admission, singleton swaps, batch isolation,
all meaningful work/deadline interruption stages and final publication using
the last available unit. An independent reviewer will assess the resulting
code and tests. No default, native/contact integration, frozen 042 source,
benchmark schedule or publication claim changes at this stage.
