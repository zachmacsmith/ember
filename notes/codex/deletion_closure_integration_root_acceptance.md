# Root acceptance of final deletion integration

2026-09-08 UTC. Root accepts the implementation for a separate fixed full-pipeline
comparison. It read the complete native/pilot diff, author tests and runner,
implementation note, accepted specification, independent verifier and review.
All 20 author and 36 independent manifest entries match their declared bytes.
No implementation blocker was found. Native SHA256 is
`49a2175874b4b0c65f2ddccde519e8905ae7fc3836e15e2a7c478b37f91e5c57`;
pilot is `fedbdf4dc60c78bb357c93c465eda50e5eb403c888dc4e70f6255bf90666f5b9`.

The author passed 34 focused and 189 broader checks, including one actual tiny
Z3 spectral call. The independent verifier passed twelve groups on the local
native interpreter, including sixteen old/default/off envelope replays and a
different independently validated fixture with construction/prune/contact/cleanup
Q of 9/8/7/4. It tested actual-core interruptions, unchanged shared deadlines,
original final validation, exception/skip costs and preservation of the full
trace. Root checked the verifier's direct original-edge predicate and reference
sweeps; it does not rely on the production cleanup's validity predicate. The
prior exhaustive core review remains separate and was not repeated.

The independent review note SHA256 is
`a88e906d6bddd0da944a54156c1bf4df1db3c1529752fdcf7f5d0d458e8e1ad1`;
its manifest is
`7b262d1c94b7798e022ae8546c4104d9c827042a3decced541b0f454d5b8753d`.
Root hash-verification records are under `results/codex/042-protocol`.
The two reviewer-only failures and the author's two test-wrapper failures remain
preserved; production was not changed to accommodate invalid expectations.

The additional native option is off by default; the original pilot arm retains
its exact configuration. Enabled cleanup receives only the remaining common
time and precedes original-label final validation. Every core trace/flag remains
honest, including an unvalidated-by-core alias whose entry was independently
validated by native. The supported API is deliberately limited to ordinary
contact scoring for this comparison. This is conventional cleanup, not a
novelty claim or a reason to declare the research objective achieved.

Root may now commit and freeze 042 under the accepted protocol. Actual hyde03
synthetic correctness, exact frozen-input/environment preflight and a fresh
empty execution identity still precede its one detached launch. No full-pipeline
cleanup result, mean improvement, runtime gain or generalization is inferred
from this acceptance. The completed 041 snapshot remains unchanged.
