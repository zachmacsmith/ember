# Transfer002: decisions about allocation, routing cost and construction

2026-09-09, before candidate calls. The previous research cycle made progress:
198 terminal calls were preserved and independently checked. The retained
algorithm is still A061; none of these new screens can establish an all-class
win or promotion. The two automatic continuation prompts received during this
cycle are preserved in `PROMPTS_CODEX.md`.

The [input plan](transfer_cycle_002_inputs.md) generated two structures once and
reused seven unchanged encodings. Independent input review passed: bijections,
original edges, sanitized records and the hidden singleton witness reconcile.
The new ER100 and BA100 have no match or unresolved comparison within the
declared 512-file exposure index. This is bounded development evidence.

**A064 allocation screen.** Nine keys in fixed order:
g0001,g0005,g0008,g0004,g0012,g0017,g0013,g0101,g0102. Evaluate A064, A063,
A061 and MM separately on hyde06, seed0, 60 seconds per fresh call, empty
configuration: 36 calls. This expands the initial eight-input proposal by grid
before execution. The [A064 policy](experiments/064_interleaved_boundary_policy.md)
defines the hypothesis and decisions: does visiting more owners while retaining
later roots improve complete embeddings? Compare each result with both A063
and A061; two gains on different families, including a stalled or newly generated
case, justify a broader screen. Improved coverage alone does not. Every Q,
success and variance regression remains visible and blocks promotion. Wheel's
previous late-root gain and variance regression remain untested here.

**B028 routing diagnostic.** Use the separately frozen six saved candidate-state
queries on hyde02. The [B028 policy](tracks/b_028_routing_policy.md) changes only
the exact routing implementation. Both cold processes receive identical query
inputs; all import, compilation, packing and conversion costs count. No complete
constructor is called by this packet. Review the measured speed and overhead
before freezing a small complete-constructor experiment; no automatic extension
or unrecorded warmup is permitted. The later screen will retain fresh paired
B027/A061/MM controls and newly generated inputs.

**C013 construction screen.** Use the same nine ordered inputs as A, C013/A061/MM
separately on hyde03, seed0, 60 seconds: 27 calls after source and targeted checks.
The [C013 policy](tracks/c_013_contact_domain_policy.md) replaces the failed strip
representation and coordinated movement together. Its continuation criterion
requires complete Q gains against A061 on two independently generated development
inputs from different families, at least one no worse than timely MM, and no
lost A061 success on the hidden control. Distinguish the two newly generated
inputs from reused development structures in every report. Domain improvements
or successful private rebuilds alone cannot justify continuation.

**Pre-launch C decision.** The first tiny complete-constructor gate failed on
star22: its degree21 hub remains a singleton on a target with maximum degree20.
All-owner retraction repeats that representation, with complete scans and ample
time remaining. Preserve all six calls and four passing targeted groups.
C013's planned 27-call screen is therefore held and this fixed policy rejected.
A bounded capacity-aware construction policy may be designed separately before
implementation; it receives no automatic authorization from this protocol.

Every run uses the audited pilot and independent original-graph oracle. The new
initializer only supplies selected graph records and adds registry descriptors;
an AST check rejects any other pilot change. Source snapshots and exact task
vectors are verified before staging. Evaluator originals, source labels and
witnesses remain outside candidate bundles. A separate analysis adapter retains
the previous validation and credit rules, selecting this panel and its frozen
method vector. Its numerical summaries do not substitute for mechanism review.

The current native environments on02/03/06 have no MM or busclique package;
the paired comparator environment pins MM0.2.22. Record solver wall/CPU, full
process time, failures and missing times separately. Keep timing pairs on the
same host. A60-second allowance is this experiment's allocation, not a universal
runtime acceptance gate. Report repeated order-of-magnitude deficits and useful
work cut short, then freeze any justified different allocation as a later
experiment. Detached cluster supervision survives network switching; observation
timeouts never authorize restarting an existing run.

Self-critique: this small panel favors rapid mechanism decisions and cannot
estimate across-seed variability or prove generality. All reused graphs have
influenced research. Promising complete constructors must promptly face broader
development structures, sizes, relabelings and seeds, followed by a separately
untouched confirmation set. Algorithms are evaluated independently; no result
selection combines them into a portfolio.
