# Transfer004: fresh development for architecture decisions

2026-09-10, before generation or new candidate implementation. Generate eight structures once with the unchanged Ember generators and input helpers: ER96 p=0.10; BA96 m=4; regular96 d=8; WS96 k=6 beta=0.2; SBM96 with four communities, p_in=0.20 and p_out=0.02; random planar96; singleton and branch known-embedding controls with 96 owners. Also independently relabel the new BA input. These intermediate sizes and new generation seeds supplement the exposed 80/100/160-node examples. They are development data, not confirmation.

Use first 64 bits of SHA256(`ember-codex-transfer004/{namespace}/{case}`) for generation and independent label permutations. Neutral identifiers are g0301–g0308 and g0309 for the BA relabel. Generate each once; preserve a generation failure without replacement or reseeding. Existing generation watchdogs remain 30 seconds, 300 for planar. These limit input generation, not candidate work. The same unchanged helper creates both controls and verifies their witnesses with the original oracle. Witnesses and original labels remain in the evaluator-private sibling directory; candidates receive only sanitized graph records.

Copy the existing Transfer003 panel byte for byte into the reusable public panel, preserving parent identities. Expected total is 36 encodings / 33 structures if every generation succeeds. This defines available inputs, not an experiment: each track freezes its small subset and candidate parameters before seeing any outcome. Include fresh inputs and at least two seeds promptly for promising complete constructors; relabel pairs are not independent structures.

Reuse the existing sanitizer, original-label oracle, bijection checks and bounded exposed-index comparison. Add only identity/cardinality checks for this adapter and the new relabel parent. No embedder is called during preparation. No untouched confirmation set is consumed or claimed; confirmation still needs a separate held-out specification before promotion.
