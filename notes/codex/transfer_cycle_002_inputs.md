# Transfer002 development inputs — before generation

2026-09-09. Preserve Transfer001 and every outcome. This smaller cycle tests
specific mechanisms, not promotion or confirmation. Reuse its sanitized ER80,
SBM80, BA160, Watts–Strogatz80, planar160, singleton-control80 and grid128
encodings: g0001/g0005/g0008/g0004/g0012/g0017/g0013. Their evaluator-private
originals, bijections and hidden witness remain outside every candidate bundle.

Generate exactly two new development structures, once each: ER n100/p0.08 and
BA n100/m3. These change structure, size and degree/density parameters from the
previous panel. Use the same pinned repository generator and NetworkX3.4.2,
with generation seed SHA256(`ember-codex-transfer002/generation/{case}`) truncated
to64bits. Independently permute source labels using the analogous `label`
namespace. Neutral identifiers are g0101 and g0102. No family annotation,
generation parameter or witness enters runtime graph metadata or attributes.

Use the existing Transfer001 generation, sanitization, publication and oracle
helpers by import from their frozen script, through a separate adapter. Each
new generation has a30s watchdog and a preserved invocation/status/wall/CPU
ledger. A failed attempt is not replaced. Verify original-to-solver edge
identity and private/public hashes before any constructor run. Check possible
exact isomorphism against the prior exposed index and Transfer001 when n/m and
degree multiset agree; preserve unresolved comparisons and never reseed a match.

The nine-input panel supports independently frozen method screens. A's proposed
eight inputs are the six original contrasting cases plus the two new structures;
B's six are ER80, SBM80, grid128, singleton-control80 and the two new structures;
C's proposed eight match A. Exact method identities, clocks and task vectors
will be frozen after source/new-risk checks and before execution. This note
does not authorize treating the old outcomes as fresh timing controls.

Self-critique: two new structures give only a cheap transfer check, and these
source families have already influenced design. No relabel pair here replaces
Transfer001's observed sensitivity. A promising mechanism must soon face the
full development panel, additional labels/seeds and a separate untouched set.
