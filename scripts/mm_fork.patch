diff --git a/include/find_embedding/chain.hpp b/include/find_embedding/chain.hpp
index e8e44d3..db1714a 100644
--- a/include/find_embedding/chain.hpp
+++ b/include/find_embedding/chain.hpp
@@ -338,7 +338,19 @@ class chain {
         if (p == -1) {
             p = q;
         } else {
+            // ember fork fix: bound the parent walk.  A corrupt parent
+            // table (stale entries from a masked/previous search) can
+            // cycle forever or run to -1 and index qubit_weight[-1] --
+            // the recorded restrict_chains hang and segfault (notes
+            // s3.60).  The u-only domain mask removes the known cause;
+            // this guard converts any residual corruption into a clean
+            // failure instead of a hang or out-of-bounds write.
+            size_t walk = 0;
+            const size_t walk_bound = parents.size() + 1;
             while (other.count(p) == 0) {
+                if (p < 0 || ++walk > walk_bound)
+                    throw CorruptEmbeddingException(
+                            "corrupt parent path in link_path (domain-restricted search)");
                 if (count(p))
                     trim_branch(q);
                 else
diff --git a/include/find_embedding/embedding.hpp b/include/find_embedding/embedding.hpp
index 685e6c1..e5c8501 100644
--- a/include/find_embedding/embedding.hpp
+++ b/include/find_embedding/embedding.hpp
@@ -86,10 +86,23 @@ class embedding {
 
         for (auto &vC : fixed_chains) fix_chain(vC.first, vC.second);
 
-        for (auto &vC : initial_chains)
+        // ember fork fix (s3.60): clip initial chains to each variable's
+        // restrict_chains domain at ingest (best-effort semantics: the
+        // upstream ctor installed out-of-domain qubits verbatim).  A
+        // chain emptied by clipping is dropped and rebuilt normally.
+        // Unrestricted variables are untouched (accepts_qubit == 1).
+        map<int, vector<int>> use_chains;
+        for (auto &vC : initial_chains) {
+            vector<int> kept;
+            for (auto &q : vC.second)
+                if (ep.fixed(vC.first) || ep.accepts_qubit(vC.first, q)) kept.push_back(q);
+            if (kept.size()) use_chains.emplace(vC.first, std::move(kept));
+        }
+
+        for (auto &vC : use_chains)
             if (!ep.fixed(vC.first)) set_chain(vC.first, vC.second);
 
-        for (auto &vC : initial_chains) {
+        for (auto &vC : use_chains) {
             int v = vC.first;
             auto &c = var_embedding[v];
             int root = vC.second[0];
diff --git a/include/find_embedding/embedding_problem.hpp b/include/find_embedding/embedding_problem.hpp
index cfc3d48..9c7df54 100644
--- a/include/find_embedding/embedding_problem.hpp
+++ b/include/find_embedding/embedding_problem.hpp
@@ -88,13 +88,20 @@ class domain_handler_masked {
     }
     virtual ~domain_handler_masked() {}
 
-    inline void prepare_visited(vector<int> &visited, const int u, const int v) {
+    inline void prepare_visited(vector<int> &visited, const int u, const int /*v*/) {
+        // ember fork fix: mask by u's domain ALONE.  The upstream AND of
+        // u's and v's masks left qubits outside u's domain (but inside
+        // v's) searchable, so link_path could install out-of-domain
+        // qubits into u's chain and walk stale parent entries -- the
+        // recorded restrict_chains hang/segfault (notes s3.60).  The
+        // terminals this search must reach are v's CHAIN qubits, and
+        // dijkstra_initialize_chain force-marks those visited/seeded
+        // regardless of the mask, so u-only masking cannot disconnect
+        // the search from its target.
         vector<int> &uMask = masks[u];
-        vector<int> &vMask = masks[v];
         int *umask = uMask.data();
-        int *vmask = vMask.data();
         int *vis = visited.data();
-        for (int *stop = vis + visited.size(); vis < stop; ++vis, ++umask, ++vmask) *vis = (*umask) & (*vmask);
+        for (int *stop = vis + visited.size(); vis < stop; ++vis, ++umask) *vis = (*umask);
     }
 
     inline void prepare_distances(vector<distance_t> &distance, const int u, const distance_t &mask_d) {
@@ -303,6 +310,13 @@ class embedding_problem_base {
             return weight_table[c];
     }
 
+    //! Ember history-cost fork: the largest finite price a scaled qubit weight
+    //! may take while preserving the overflow guarantee (path sums of up to
+    //! ~exponent_margin prices stay below max_distance).
+    distance_t max_finite_price() const {
+        return max_distance / static_cast<distance_t>(exponent_margin);
+    }
+
     //! a vector of neighbors for the variable `u`
     const vector<int> &var_neighbors(int u) const { return var_nbrs[u]; }
 
@@ -361,6 +375,13 @@ class embedding_problem_base {
             minorminer_assert(var_order_space.size() > 0);
             return var_order_space;
         }
+        // Ember search-guidance fork: if the user supplied a variable order whose
+        // length matches this (sub)problem's variable count, use it verbatim for
+        // every (non-KEEP) pass instead of the strategy-generated order.
+        if (params.fixed_var_order.size() == static_cast<size_t>(num_v)) {
+            var_order_space = params.fixed_var_order;
+            return var_order_space;
+        }
         var_order_space.clear();
         var_order_shuffle.clear();
         for (int v = num_v; v--;) var_order_shuffle.push_back(v);
diff --git a/include/find_embedding/pathfinder.hpp b/include/find_embedding/pathfinder.hpp
index 84c6bf2..b83364b 100644
--- a/include/find_embedding/pathfinder.hpp
+++ b/include/find_embedding/pathfinder.hpp
@@ -89,6 +89,11 @@ class pathfinder_base : public pathfinder_public_interface {
 
     vector<distance_t> qubit_weight;
 
+    //! Ember history-cost fork: per-qubit PathFinder-style history term h_q,
+    //! persistent across passes and restarts within one findEmbedding call.
+    //! Identically zero (and never read) when params.history_alpha == 0.
+    vector<double> hist;
+
     vector<int> tmp_stats;
     vector<int> best_stats;
 
@@ -118,6 +123,7 @@ class pathfinder_base : public pathfinder_public_interface {
               total_distance(num_qubits, 0),
               min_list(num_qubits, 0),
               qubit_weight(num_qubits, 0),
+              hist(num_qubits, 0.0),
               tmp_stats(),
               best_stats(),
               visited_list(num_vars + num_fixed, vector<int>(num_qubits)),
@@ -230,9 +236,10 @@ class pathfinder_base : public pathfinder_public_interface {
                 ep.debug("chain for %d kept during initialization\n", u);
             } else {
                 ep.debug("finding a new chain for %d\n", u);
-                if (!find_chain(emb, u)) return -1;
+                if (!find_chain(emb, u)) return check_stops(-1);  // ember fork fix (s3.60): probe the clock on failure paths
             }
         }
+        update_history(emb);
         return check_stops(1);
     }
 
@@ -241,11 +248,12 @@ class pathfinder_base : public pathfinder_public_interface {
         bool improved = false;
         for (auto &u : ep.var_order(VARORDER_PFS)) {
             ep.debug("finding a new chain for %d\n", u);
-            if (!find_chain(emb, u)) return -1;
+            if (!find_chain(emb, u)) return check_stops(-1);  // ember fork fix (s3.60): probe the clock on failure paths
 
             improved |= check_improvement(emb);
             if (ep.embedded) break;
         }
+        update_history(emb);
         return check_stops(static_cast<int>(improved));
     }
 
@@ -276,7 +284,7 @@ class pathfinder_base : public pathfinder_public_interface {
                 emb.steal_all(u);
                 emb.tear_out(u);
                 if (!find_chain(emb, u, 0)) {
-                    return -1;
+                    return check_stops(-1);  // ember fork fix (s3.60)
                 }
             }
             improved |= check_improvement(emb);
@@ -284,6 +292,7 @@ class pathfinder_base : public pathfinder_public_interface {
         }
         ep.weight_bound = oldbound;
         if (!improved) pushback += (num_vars * 2) / params.inner_rounds;
+        update_history(emb);
         return check_stops(improved);
     }
 
@@ -295,10 +304,11 @@ class pathfinder_base : public pathfinder_public_interface {
         std::fill(qubit_permutations.begin() + 1, qubit_permutations.end(), qubit_permutations[0]);
         for (auto &u : ep.var_order(ep.improved ? VARORDER_KEEP : VARORDER_PFS)) {
             ep.debug("finding a new chain for %d\n", u);
-            if (!find_chain(emb, u)) return -1;
+            if (!find_chain(emb, u)) return check_stops(-1);  // ember fork fix (s3.60): probe the clock on failure paths
 
             improved |= check_improvement(emb);
         }
+        update_history(emb);
         return check_stops(improved);
     }
 
@@ -408,7 +418,11 @@ class pathfinder_base : public pathfinder_public_interface {
                     pq.pop();
                     if (!emb.weight(q)) counts[q]++;
 
-                    if (counts[q] == degree) {
+                    if (counts[q] == degree && ep.accepts_qubit(u, q)) {
+                        // ember fork fix (s3.60): a root outside u's domain
+                        // must not seed a chain build -- the shortening
+                        // phase never consulted the domain handler and
+                        // segfaulted at default chainlength_patience.
                         emb.construct_chain_steiner(u, q, parents, distances, visited_list);
                         unsigned int cs = emb.chainsize(u);
                         if (cs < best_size) {
@@ -525,7 +539,32 @@ class pathfinder_base : public pathfinder_public_interface {
     //! where the weight is `2^(alpha*fill)` where `fill` is the number of
     //! chains which use that qubit
     void compute_qubit_weights(const embedding_t &emb, const int start, const int stop) {
-        for (int q = start; q < stop; q++) qubit_weight[q] = ep.weight(emb.weight(q));
+        if (params.history_alpha == 0.0) {
+            for (int q = start; q < stop; q++) qubit_weight[q] = ep.weight(emb.weight(q));
+        } else {
+            // Ember history-cost fork: price(q) = (1 + h_q) * weight_table[occ(q)],
+            // saturated at ep.max_finite_price() (and left untouched at max_distance)
+            // so path sums keep stock minorminer's overflow guarantee.
+            const distance_t cap = ep.max_finite_price();
+            for (int q = start; q < stop; q++) {
+                distance_t w = ep.weight(emb.weight(q));
+                if (w != max_distance && hist[q] != 0.0) {
+                    double scaled = (1.0 + hist[q]) * static_cast<double>(w);
+                    w = (scaled >= static_cast<double>(cap)) ? cap : static_cast<distance_t>(scaled);
+                }
+                qubit_weight[q] = w;
+            }
+        }
+    }
+
+    //! Ember history-cost fork: once-per-pass subgradient update of the history
+    //! term, h_q <- max(0, h_q + alpha*(occ(q) - 1)) -- rises while contested
+    //! (occ >= 2), holds at exactly-full, decays while free. No-op at alpha == 0,
+    //! so the stock code path (including its rng stream) is untouched.
+    void update_history(const embedding_t &emb) {
+        const double alpha = params.history_alpha;
+        if (alpha == 0.0) return;
+        for (int q = num_qubits; q--;) hist[q] = max(0.0, hist[q] + alpha * (emb.weight(q) - 1));
     }
 
   public:
diff --git a/include/find_embedding/util.hpp b/include/find_embedding/util.hpp
index 7697ab5..48a3ba6 100644
--- a/include/find_embedding/util.hpp
+++ b/include/find_embedding/util.hpp
@@ -145,6 +145,19 @@ class optional_parameters {
     map<int, vector<int>> initial_chains;
     map<int, vector<int>> restrict_chains;
 
+    //! Ember search-guidance fork: a user-supplied variable order (in internal
+    //! variable-index space). Empty => use the built-in VARORDER strategy.
+    //! Honored only when its length equals the (sub)problem's variable count, so
+    //! component subproblems with a different size fall back to the default order.
+    vector<int> fixed_var_order;
+
+    //! Ember history-cost fork: step size for the PathFinder-style history term.
+    //! Each pass, every qubit's history h_q is updated by the subgradient rule
+    //! h_q <- max(0, h_q + history_alpha*(occ(q) - 1)) and the routing price
+    //! becomes (1 + h_q) * weight_table[occ(q)]. 0.0 (default) disables the
+    //! mechanism entirely: the price path is byte-identical to stock minorminer.
+    double history_alpha = 0.0;
+
     //! duplicate all parameters but chain hints,
     //! and seed a new rng.  this vaguely peculiar behavior is
     //! utilized to spawn parameters for component subproblems
@@ -166,7 +179,9 @@ class optional_parameters {
               skip_initialization(p.skip_initialization),
               fixed_chains(fixed_chains),
               initial_chains(initial_chains),
-              restrict_chains(restrict_chains) {
+              restrict_chains(restrict_chains),
+              fixed_var_order(p.fixed_var_order),
+              history_alpha(p.history_alpha) {
 #ifndef CPPDEBUG
         if (verbose >= 4)
             throw CorruptParametersException(
diff --git a/minorminer/_minorminer.pyx b/minorminer/_minorminer.pyx
index 53aad8f..529af47 100644
--- a/minorminer/_minorminer.pyx
+++ b/minorminer/_minorminer.pyx
@@ -331,7 +331,8 @@ cdef class _input_parser:
         names = {"max_no_improvement", "random_seed", "timeout", "tries", "verbose",
                  "fixed_chains", "initial_chains", "max_fill", "chainlength_patience",
                  "return_overlap", "skip_initialization", "inner_rounds", "threads",
-                 "restrict_chains", "suspend_chains", "max_beta", "interactive"}
+                 "restrict_chains", "suspend_chains", "max_beta", "interactive",
+                 "var_order", "history_alpha"}
 
         for name in params:
             if name not in names:
@@ -383,6 +384,12 @@ cdef class _input_parser:
         if z is not None:
             self.opts.timeout = float(z)
 
+        # Ember history-cost fork: step size of the per-qubit history term;
+        # 0.0 (default) leaves the search byte-identical to stock minorminer.
+        z = params.get("history_alpha")
+        if z is not None:
+            self.opts.history_alpha = float(z)
+
         z = params.get("max_beta")
         if z is not None:
             self.opts.max_beta = float(z)
@@ -411,6 +418,16 @@ cdef class _input_parser:
         _get_chainmap(params.get("initial_chains", ()), self.opts.initial_chains, self.SL, self.TL, "initial_chains")
         _get_chainmap(params.get("restrict_chains", ()), self.opts.restrict_chains, self.SL, self.TL, "restrict_chains")
 
+        # Ember search-guidance fork: optional user-supplied variable order. Maps
+        # source-graph labels to internal variable indices (as quickpass does) and
+        # hands them to the C++ search via opts.fixed_var_order.
+        vo = params.get("var_order")
+        if vo is not None:
+            for v in vo:
+                if v not in self.SL:
+                    raise ValueError("entries of var_order must be source graph labels")
+                self.opts.fixed_var_order.push_back(<int> self.SL[v])
+
         self.pincount = 0
         cdef int nonempty
         cdef int pinlabel
diff --git a/minorminer/_minorminer_h.pxi b/minorminer/_minorminer_h.pxi
index ca9c138..228e508 100644
--- a/minorminer/_minorminer_h.pxi
+++ b/minorminer/_minorminer_h.pxi
@@ -58,6 +58,8 @@ cdef extern from "../include/find_embedding/util.hpp" namespace "find_embedding"
     cppclass optional_parameters:
         optional_parameters()
         void seed(uint64_t)
+        vector[int] fixed_var_order
+        double history_alpha
 
         LocalInteractionPtr localInteractionPtr
         int max_no_improvement
