# Minor Embedding Benchmark Framework - Requirements

# Core dependencies
numpy>=1.20.0
pandas>=1.3.0
matplotlib>=3.4.0
seaborn>=0.11.0
networkx>=2.6.0
scipy>=1.7.0

# D-Wave tools (for minorminer and hardware graphs)
minorminer>=0.2.0
dwave-networkx>=0.8.0

# Additional utilities
tqdm>=4.62.0  # Progress bars (optional but nice)

# JIT for the factored pipeline hot loops (s3.114 perf round)
numba>=0.67
